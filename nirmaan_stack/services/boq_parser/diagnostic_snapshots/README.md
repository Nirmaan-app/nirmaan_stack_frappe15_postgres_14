# Diagnostic snapshots

Historical snapshots of `single_area_triage_1_9i.py` output preserved
at sub-phase boundaries for comparison purposes. The live output files
(`single_area_triage_1_9j_output.json` / `.txt`) are overwritten on
each run; this folder holds named copies of significant runs.

Each filename indicates the run context. Treat as read-only reference;
do not regenerate snapshots in place.
