--- **[Prior cycle — v5.73]** **Last updated:** 2026-06-30 IST (v5.73 — **THE RATE-GUIDANCE CLASSIFICATION ENGINE — FIRST CODE LANDED. The electrical category RULE MODULE (`services/boq_category/`) is BUILT + TUNED, the AI voter + eval harness are built, and the rules-vs-AI engine is VALIDATED on a 5-BoQ eval set. ALL classification commits are LOCAL + UNPUSHED — owner gate: nothing pushes until the team's human verdicts validate the tuning direction.** This is a CODE cycle (4 local commits), so per agreement #45 it gets its own Trigger-state dashboard (v5.73, below) and the 3-recent window advances to v5.73/v5.71/v5.70 — **v5.69's dashboard rotates OUT to the archive (→ archive v1.34).** Caveat carried on the dashboard: the commits are UNPUSHED + verdict-gated, so the dashboard certifies BUILT-LOCAL, not SHIPPED. **FULL detail lives in `BoQ_Classification_Engine_Design_Scoping_v0_9.md` §19 (the authoritative as-built record — this handover points there).** **⚠️ VERSION DRIFT — partially refreshed.** This handover's last PUSHED code state remains tip `c615b952` (Abhishek's 7 commits + Madhu's PO work, recorded at v5.72 — the full narrative catch-up reading `e02599e1`+`8558e82c` is still OWED at a future doc cycle, deferred by owner). NEW this cycle: chat-Claude's 4 classification commits stack ON TOP of `c615b952` — feat `87468e2e` + docs `86bce2c1` (the rule module + runner), feat `47908443` + docs `6746d047` (the tuning pass). **Repo working tree is at tip `6746d047`, LOCAL — these 4 are NOT pushed** (owner gate). So origin is still `c615b952`; the local branch is 4 ahead. The handover NARRATIVE body below still lags (describes the v5.71 code state) — the catch-up fold is owed when the classification module pushes. **WHAT THIS SESSION DID (the BUILD arc):** (1) BUILT the tracked electrical rule module `services/boq_category/` — `categories_electrical.json` (12 pricing categories + novel, Option-B descriptions), `rules_electrical.json` (rules seeded from REAL mined corpus vocabulary, each rule_id/source/plain-tagged), `scoring.json` (the tunable confidence model), `runner.py` (pure-Python, NO frappe imports), tests (feat `87468e2e` + docs `86bce2c1`); (2) FINALIZED the confidence model — a computed 0-1 score from signal AGREEMENT (item-keyword on the line + ancestor signal up the tree), bands by threshold in scoring.json (HIGH≥0.75 / MED≥0.45 / LOW / ABSTAIN→novel), per-rule performance tracked separately via rule_id (resolves the design-doc §10.2 confidence sub-fork); (3) BUILT the AI voter (Option B — same context as rules: line + full ancestor chain + notes + category descriptions; classifies INDEPENDENTLY, never sees rule output; reuses the `run_ai_pass` plumbing; opus); (4) BUILT the scratch eval harness (reads committed nodes → rules + AI → per-sheet CSV) — owner decision: the harness SPINE is the future production classifier spine (two-stage promotion, additive edges not a rewrite); (5) LOCKED the 20-column **pricing-editor-view CSV schema** as a REUSABLE contract for all the rate-guidance helpers (reproduces the pricing editor's structural columns in screen order, data columns dropped, classifier columns appended, node_id as the durable training join-key); (6) scope = classify Line Items AND Preambles (Option A — preambles can be priceable); (7) TUNED the rule set — 5 fixes from proving-run findings (junction box, panels false-friends, wiring-for-points, fragment inheritance, industrial-vs-DB), feat `47908443` + docs `6746d047`, tests 14→27, NO regression; (8) VALIDATED — a 5-BoQ eval run (1,697 rows classified incl. 313 preambles): band→AI-agreement is MONOTONIC (HIGH 86 / MED 78 / LOW 62 / ABSTAIN 51, overall 73 post-tuning) = the confidence band is an honest triage lever; vocabulary conformance VERIFIED clean (both classifiers 100% in the 13-category set, exact id, zero drift). **CAVEAT (load-bearing):** rules-vs-AI agreement is NOT the success metric (tuning moved rules partly toward the AI); the team's HUMAN VERDICTS are ground truth. **The canonical electrical eval set is now BOQ-26-00022/00024/00007/00019/00016** (Bain/Springer/META/Aster/Amazon — the old 145/150/166 no longer exist in the corpus). **NEXT ACTION (after the team returns verdicts tomorrow):** (1) calibrate the provisional thresholds against the human verdicts; (2) next tuning round on the known gaps (cabletray-fragment-under-generic-ancestor #1 disagreement 123 lines; preamble header-inheritance; the FIX-4 line-exclusion bug 2 rows) — anchored to verdicts not AI; (3) PROMOTE the harness (stage 1) to a tracked tool, spine factored as the reusable core; (4) PUSH the 4 classification commits (owner gate: after verdicts validate the direction; mechanical-safety triplet first); (5) then the full electrical corpus run (consider sonnet for cost), then HVAC + ELV (classifier reused, per-discipline back-ends). **PK HOUSEKEEPING THIS FOLD (agreement #17):** upload this handover v5.73 (delete v5.72); upload `BoQ_Classification_Engine_Design_Scoping` **v0.9** (delete v0.8); upload the archive **v1.34** (delete v1.33 — it now carries the rotated-out v5.69 dashboard). **DASHBOARD ROTATION THIS CYCLE:** v5.73 gets its own Trigger-state dashboard (below); the 3-recent window advances to v5.73/v5.71/v5.70; **v5.69's dashboard rotates OUT to the archive (→ archive v1.34).** The pricing-editor design doc (v1.18) + formula-builder (v1.4a) are UNCHANGED. **The in-repo `boq-upload-plan.md` was updated by CC this session in the docs commits — it updates IN-REPO as the feature builds, NOT a PK manual-sync item this cycle** (owner). **PARKED:** `patches.txt` (`backfill_cashflow_gap_limited`) stays Abhishek's shared-path — do NOT touch. The full handover-narrative catch-up to `c615b952` (reading `e02599e1`+`8558e82c`) is still OWED at a future code cycle. --- **[Prior cycle — v5.72]** 2026-06-29 IST (v5.72 — **DESIGN/RECON CYCLE — NO CODE LANDED.** The classification-engine recon-gated arc (design-doc §13) was EXECUTED end-to-end — corpus verified clean on-system (31 BoQs / ~13-14 projects / 9 disciplines), line definition LOCKED (`node_type='Line Item'` = 9,005 priceable units), the two-stage model validated on ELV + HVAC (headline: the sheet NAME does almost no work; the preamble-walk is the universal spine), per-discipline hard-problem characterization, the BUILD-SCOPE decision TAKEN (all disciplines, shared front-end, sequential, electrical first), Phase-1 definition LOCKED, existence scan done, all pre-build facts verified. Design doc bumped v0.7 → v0.8. NO repo writes/commits/migrate — read-only recon + design only. This was a CODE-FREE cycle → no Trigger-state dashboard, no rotation (window stayed v5.71/v5.70/v5.69). The §13 recon detail lives in `BoQ_Classification_Engine_Design_Scoping` §15-§18. The build it scoped became THIS cycle's work (v5.73 above).) --- **[Prior cycle — v5.71]** 2026-06-27 IST (v5.71 — **PHASE 5 (THE PRICING EDITOR) — BUILD-SEQUENCE ITEM (5), COPY/PASTE/FILL-DOWN + UNDO/REDO, IS BUILT — AND WITH IT THE PRICING-EDITOR MODULE IS FUNCTIONALLY COMPLETE. THREE PERF/POLISH FIXES (THE AUTOSAVE ROW-IDENTITY MERGE, THE onHistoryChange FLIP-GATE, THE SAVE-STATUS REFLOW FIX) ALSO LANDED — ALL OWNER-CERTIFIED LIVE ON ALL THREE CANONICAL BoQs (145/150/166) AND PUSHED.** Branch `feature/boq-phase-5` tip `48931cdf`, origin in sync — **PUSHED this session** (host PowerShell; eight clean fast-forwards across the session). Item (5) shipped as Slice A clipboard (feat `7d3dff9b`) + a right-click context menu (feat `790a3beb`) + Slice B undo/redo (feat `2f42addd`); the three perf/polish fixes are the row-identity merge (feat `1f0001de`), the `onHistoryChange` flip-gate (feat `c5c34b6f`), and the save-status reflow fix (feat `d2d6a0e2`). ALL FRONTEND-only, NO migrate; each recon-first per #56; the design doc is now **v1.18**. The editor module has ZERO un-built rows; **NEXT MODULE = the rate-guidance helpers (Slice 8), owner-locked order 2 -> 1 -> 3 (rule engine -> rates-from-other-BoQs -> material/PO/margin; all DISPLAY-ONLY, never auto-write).** The new Trigger state v5.71 dashboard below is CURRENT (supersedes v5.70). **[Prior cycle — v5.70]** 2026-06-27 IST (v5.70 — **PHASE 5 (THE PRICING EDITOR) — THE FROZEN-LEFT COLUMNS ARC IS COMPLETE (BUILT AS TWO SLICES), PLUS A RECONCILIATION DOC-0 DEFAULT FIX, ALL OWNER-CERTIFIED LIVE ON ALL THREE CANONICAL BoQs (145/150/166) AND PUSHED. FROZEN-LEFT WAS THE LONG-DEFERRED "STRUCTURAL TWO-PANE SLICE" (parked since v5.66 after attempt 1 proved cell-level multi-column sticky-left structurally unworkable); IT IS NOW BUILT RIGHT VIA A TWO-PANE SPLIT. BUILD SEQUENCE: FROZEN-LEFT (the deferred structural slice) DONE; (5) COPY/PASTE/FILL-DOWN + UNDO/REDO IS NOW THE ONLY REMAINING OWNER-LOCKED PHASE-5 ITEM.** Branch `feature/boq-phase-5` tip `e59450ec`, origin in sync — **PUSHED this session** (host PowerShell; three clean fast-forwards across the session — frozen-left Slice 1 `185b2a19..fd841657`, Slice 2 `fd841657..ae2ef63f`, doc-0 fix `ae2ef63f..e59450ec`). Each piece recon-first per #56; ALL THREE are FRONTEND-only, NO migrate. **WHAT LANDED THIS SESSION (each recon-first per #56; all frontend, NO migrate; on `feature/boq-phase-5`):** **(1) FROZEN-LEFT SLICE 1 of 2 — two-pane split + measure-at-freeze heights + wrap/clip/tooltip** (feat `b3a6952c` + docs `fd841657`) — FRONTEND, view-layer. **Revives the deferred frozen-left as the TWO-PANE structural slice** (the only recorded working alternative — attempt 1's cumulative cell-level `sticky left` across multiple columns was proven structurally broken, doesn't track horizontal scroll; do NOT reintroduce it). A page-owned per-sheet **`frozen` toggle** ("Freeze columns"/"Unfreeze", `Pin`/`PinOff`, loud sky-600 when on) in the `!isGridOnly` top-ribbon cluster (so grid-only general-specs sheets — rendered by SheetDataGrid — never get it); reset on tab switch. **When frozen+measured the grid renders TWO tables in a flex row:** a FROZEN pane (the 5 anchors a0..a4 = Excel Row / Sl.No / Parent / Classification / Description, `overflow-hidden`, width = summed anchor `colWidths`, vertical scroll DRIVEN) + a SCROLLING pane (descriptors + Remarks, owns `overflow-x`+`overflow-y`, `onScroll` mirrors `scrollTop` to the frozen pane). **Widths come from the SAME `colWidths` map** (each pane renders its own `<col>` slice — NO duplicate width state). The sticky header works in BOTH panes. **When unfrozen, ONE single table renders byte-for-byte as before** (the split is gated on `split = frozen && rows.length>0 && rows.every(measured)`; structural decisions key on `split`, not `frozen`). **MEASURE-AT-FREEZE ("Fork A", owner-locked):** a `useLayoutEffect` captures each row's NATURAL single-table `<tr>` height at the freeze transition (via the always-registered col-0 cell -> `.closest("tr").getBoundingClientRect()`, `Math.ceil`) into a `rowHeights` state keyed by the stable `row.row_index` — it runs WHILE the single table is still mounted (split deferred until all rows measured), post-layout/pre-paint, so the user never sees an unmeasured split frame; the captured height is applied to BOTH panes so rows stay aligned by construction. **Each row gets a per-row SCALAR `rowHeight` prop** (never the whole map -> memo-safe; added to `PricingGridRowProps` + `pricingRowPropsAreEqual` alongside a new `pane` discriminator); the Description inner wrapper is clipped to it with `overflow:hidden` (text still WRAPS via `break-words`, clips from the top) + a native `title={description}` tooltip (the grid's idiom, NO shadcn Tooltip) for full-text-on-hover. **KEYBOARD NAV crosses the pane seam** by construction — ONE shared `cellRefs` map keyed by `navKey(row,col)` across both panes, `nextCell` unchanged as a single coordinate space (live-certified: arrow/Tab across the Description<->descriptor boundary works as before — no Slice 1.5 nav fix needed). `focusCell`/`jumpToRow` are split-aware (retarget the scrolling pane). The grid's `key={sheetName::version}` remount resets heights for free on sheet/version switch (so collapse + version-view flow through the SAME `displayRows` into both panes; read-only history stays read-only; lock/override unchanged). **KNOWN LIMITATION at Slice 1 (closed in Slice 2):** a column resize while frozen could leave captured heights stale. NO new pure helper -> NO test added; vitest 339 (PricingGrid 129, unchanged), tsc 3175, in-container build exit 0. Live-certified on all three BoQs (alignment held, nav crossed the seam) + an automated Playwright/host-Chrome pass proved 0px drift over 114 rows.
**LIVE-CERT (owner, this session):** all three pieces owner-certified live on all three canonical BoQs (145/150/166). Frozen-left: freeze splits, the 5 anchors hold on horizontal scroll, the two panes stay row-aligned incl. tall wrapped Descriptions, vertical scroll couples, the tooltip shows full text, arrow/Tab nav crosses the seam, manual row-resize grows both panes together + survives re-freeze (Option A), a column resize while frozen re-measures captured rows without clobbering manual ones (flash-free), the freeze-boundary border is a crisp line, unfreeze restores the single table. Doc-0: on an unpriced sheet the review-strip count drops + amount cells show their formula value + totals rise; a genuine non-zero divergence still shows its badge + strip entry + chooser (the signal preserved); no false integrity banner on a mixed sheet. **ALSO (extra rigor this session):** a Playwright -> host-Chrome harness drove the live `:8080` dev app and measured pixel-perfect (0px) two-pane alignment across 114 real rows — at rest, after a manual row-resize, and after a column-resize re-measure — turning the "aligned by construction" claim into a measured fact. (Non-mutating run; no DB writes. The scratch harness lives OUTSIDE the repo.) **FROZEN-LEFT — THE ATTEMPT-1 LESSON, PAID BACK (record so the structural call is not re-litigated):** attempt 1 (v5.66) bundled frozen-left with column-resize and used cumulative cell-level `sticky left` across the 5 anchor columns; a red-box diagnostic PROVED it structural (multi-column sticky doesn't track horizontal scroll), two CSS fixes failed + were reverted, and it was deferred. Attempt 2 followed the recorded working alternative — a TWO-PANE split — and de-risked the one genuine unknown (does the two-pane fight resize's wrap-and-grow row-height?) with a Fork-A decision (measure natural heights at the freeze transition, apply identically to both panes) so the panes align by construction, plus the owner-accepted lever to drop auto-grow if needed (NOT needed — measure-at-freeze + manual row-resize covered it). NO bundling (each slice its own feat+docs + its own live-cert), so neither could put resize at risk. The frozen-left §14 row in the design doc flips DEFERRED -> BUILT; the Column-Resize Spec §6 frozen-left deferral is now RESOLVED. **BUILD SEQUENCE (owner-locked, advanced):** ✅ (1) collapse/expand → ✅ (2) collapse/expand-all → ✅ (3) lock/unlock edits → ✅ (4) version-view + copy-forward → ✅ **FROZEN-LEFT (the deferred structural two-pane slice — DONE this session, taken out of the deferred set)** → **(5) copy/paste/fill-down + undo/redo** (NOW the ONLY remaining owner-locked Phase-5 item). Finalize/revert stays retired-for-now. DEFERRED/not-building-now: annotation-visibility toggle (dropped), rate-guidance helpers (Slice 8 arc). **Frozen-left is no longer in the deferred set — it is built.** **PK HOUSEKEEPING THIS FOLD (agreement #17): upload this handover v5.71 (delete v5.70); upload `BoQ_Pricing_Editor_Design_Scoping` v1.18 (delete v1.17); upload the archive v1.33 (delete v1.32 — it now carries the rotated-out v5.68 dashboard). The in-repo docs (`boq-upload-plan.md` + `frontend/.claude/context/domain/boq-frontend.md`) were updated by CC per slice this session (root/frontend `CLAUDE.md` + `boq-backend.md` untouched — frontend-only) — re-upload `boq-upload-plan.md` + `boq-frontend.md` if syncing PK copies.** The `BoQ_Session_Preamble_v1_0.md` (the four-fixed-instruments session preamble created this session) is ALREADY UPLOADED to PK — future handover prompts reference it. The formula-builder doc (`BoQ_Amount_Formula_Builder_Design` v1.4a) is UNCHANGED. **DASHBOARD ROTATION THIS CYCLE:** v5.71 gets its own Trigger-state dashboard (below); the 3-recent window advances to v5.71/v5.70/v5.69; **v5.68's dashboard rotates OUT to the archive this fold (-> archive v1.33).** **OWED-DOCS LIST RESOLVED THIS FOLD (several struck after owner review):** Wizard Design v1.29 — **NOT OWED / STRUCK** (the wizard closed at commit; version-view + copy-forward is EDITOR surface, already recorded in the editor design doc — the earlier "v1.29 owed" entry was a miscategorization). Column-Resize Spec §6 touch-up — **NOT OWED / STRUCK** (the frozen-left resolution is already folded into the editor design doc, which is the live record). Performance Scorecard entry — **STRUCK from the fold list** (it is the owner's personal weekly-review reference, not session state a future chat reads). AsBuilt Doc Plan — optional, not done. **BANKED (smaller separate item):** the page-level O(rows) recomputes (`rowFlags`/`pricedCount`/`byRowIndex`/`displayRows`) remain un-memoized — a minor editor-perf cleanup the row-identity merge left out of scope. The session-end performance note was logged at session close. --- > **[NOTE — v5.70 is a CODE cycle]** v5.70 records the FROZEN-LEFT arc COMPLETE (two slices — Slice 1 two-pane split + measure-at-freeze `b3a6952c`, Slice 2 manual row-resize + column-resize re-measure + border `5b263b7f`) PLUS a reconciliation DOC-0 default fix (`342c8a37`) — all BUILT + owner-certified live on all three BoQs (145/150/166) + PUSHED on `feature/boq-phase-5` (tip `e59450ec`, origin in sync; three clean fast-forwards). Frozen-left was the long-DEFERRED structural slice (parked since v5.66 after attempt 1's cumulative cell-level sticky-left proved structurally broken); attempt 2 shipped the recorded two-pane alternative with Fork-A measure-at-freeze heights so the panes align by construction, keyboard nav crosses the seam via one shared cellRefs map, and a Playwright pass measured 0px drift over 114 rows. The doc-0 fix is a narrow amendment to the Cluster B D1 default — on a doc≈0 amount cell the formula value wins silently (no badge/strip/chooser), which clears the noise on unpriced BoQs and moves totals (intended); ONE line in the single resolver `resolveDivergence`, frontend-only, non-zero divergences unchanged. All three FRONTEND-only, NO migrate. vitest 339->341->349; tsc 3175 throughout; frontend gates run IN-CONTAINER. The design doc is now v1.17. NEXT = (5) copy/paste/fill-down + undo/redo (the LAST owner-locked Phase-5 item). The new Trigger state v5.70 dashboard below is CURRENT (supersedes v5.69). **Last updated:** 2026-06-26 IST (v5.69 — **PHASE 5 (THE PRICING EDITOR) — BUILD-SEQUENCE ITEM (4) DONE: VERSION-VIEW + COPY-FORWARD BUILT AS TWO SLICES, OWNER-CERTIFIED LIVE ON ALL THREE CANONICAL BoQs (145/150/166), AND PUSHED. THE EDITOR SPINE + EXPORT + ERGONOMICS/LIFECYCLE WERE DONE BY v5.68; THIS ADDS THE READ-SIDE VERSION-HISTORY BROWSER AND THE WRITE-SIDE COPY-FORWARD. BUILD SEQUENCE ADVANCES TO (5) COPY/PASTE/FILL-DOWN + UNDO/REDO (THE LAST OWNER-LOCKED PHASE-5 ITEM).** Branch `feature/boq-phase-5` tip `185b2a19`, origin in sync — **PUSHED this session** (host PowerShell; both slices batched into one push after copy-forward live-certified; the remote had been stale at the old `698a5fca` lineage, so the push landed as `* [new branch] feature/boq-phase-5 -> feature/boq-phase-5` — harmless, same result, tracking re-established). Each slice recon-first per #56; NEITHER slice carried a migrate (both write/read through the existing `BoQ Cell Pricing` doctype). **THE BRANCH RE-CUT THIS SESSION (record so the base shift does not read as loss):** at session open the branch was re-cut. Abhishek merged the old `feature/boq-phase-5` (the v5.68-recorded `698a5fca`) into develop via **PR #1042**, then deleted + recreated the branch fresh on the new develop base **`761c4bf3`**. CASE A confirmed (good): the v5.68 slices (collapse/expand `c3eda175`, collapse-all `29d39d60`, lock/unlock `b1601789`) are SAFE in develop via that PR merge, and the new local branch contains everything. Abhishek's docs-split landed in this base: root `CLAUDE.md` 553→275 lines, `frontend/CLAUDE.md` 1715→437 — the per-slice detail moved to NEW on-demand domain docs **`.claude/context/domain/boq-backend.md`** (repo root) + **`frontend/.claude/context/domain/boq-frontend.md`**. NEW git hooks landed too: `guard_claude_md.py` BLOCKS changelog-style appends to `CLAUDE.md` (redirects per-slice detail to the domain docs) + a commit-msg conventional-commit gate. **The base recorded in the v5.68 dashboard (`698a5fca`) is SUPERSEDED — the live base is `761c4bf3`.** **WHAT LANDED THIS SESSION (each recon-first per #56; two slices of build-seq item 4, version-view first then copy-forward; NO migrate either; on `feature/boq-phase-5`):** **(1) Slice 1 of 2 — VERSION-VIEW (the read-side history browser)** (feat `184caed3` + docs `863dceb5`) — FULL-STACK, NO migrate. A read-only history browser for the pricing editor: a version dropdown selects an OLDER committed version of a sheet and shows it read-only WITH that version's own saved pricing. **A NEW dedicated ribbon ABOVE the top ribbon** (its own outermost band — shows on ALL sheet types incl. grid-only, since it sits above the `{!isGridOnly}` bottom-ribbon gate); the dropdown tags the current version "Current (live)" (NO date, the only editable one) and labels earlier versions by their last-pricing-change date (a committed-but-NEVER-priced earlier version — a COMMON case: VRF, Electrical v1/v2, HVAC v1–v3 — falls back to `committed_at` + a "never priced" affordance). Selecting an earlier version drops the WHOLE editor into a clearly-marked read-only history mode. **BACKEND (additive read paths only — the live-editor hot path byte-for-byte UNCHANGED):** the hot-path problem was that `get_priced_rows`/`get_committed_rows` are welded to the CURRENT version (`get_committed_rows` resolves the BoQ Sheet + nodes by `is_current=1`); the pricing + grid reads were ALREADY version-parameterized (`get_sheet_pricing`/`get_committed_sheet_grid` take `committed_version`) — only the NODE read was hardwired. So: `review_screen.py` extracted the node-read + row-assembly tail of `get_committed_rows` into a private `_assemble_committed_rows(...)` (the CURRENT path passes `{boq, sheet, is_current:1}` — byte-for-byte the prior query, proven by `test_review_screen` 232 unchanged) + NEW whitelisted `get_committed_rows_at_version(boq, sheet, committed_version)`; `pricing.py` extracted the overlay-merge of `get_priced_rows` into a private `_merge_overlays(...)` (behavior-preserving) + NEW whitelisted `get_version_priced_rows(...)` mirroring `get_priced_rows`' shape but forcing read-only posture (`editable=False`, `lock_info=None` — a historical read NEVER touches the single-editor lock); `commit_gate.py` NEW whitelisted `get_sheet_versions(boq, sheet)` — the dropdown's version list, SOURCE-OF-TRUTH = the committed GRID tier (the existing "what versions exist" authority; covers grid-only sheets + versions the node tier lacks), reusing `_latest_change_by_sheet_version` for the per-version last-change labels. **FRONTEND:** NEW `VersionRibbon.tsx`; version state in `SheetPricingPage.tsx` rides the EXISTING `locked` choke (`locked = ... || isViewingHistory` — one-line add; every `onSave*` + `disabled={locked}` collapses to read-only by construction; `pricingRowPropsAreEqual` UNTOUCHED); types in `boqTypes.ts`. **S5 FINDING (reported, NOT guarded — informs copy-forward):** the WRITE endpoints accept an arbitrary `committed_version` and resolve the cell without requiring `is_current` — a crafted POST could write an OLD version. BY DESIGN (each version legitimately carries its own pricing lifecycle — recon Q3); version-view adds no write, so no new exposure. Tests: `test_pricing` 158→166, `test_commit_gate` 27→33, `test_review_screen` 232 (unchanged — refactor byte-for-byte), vitest 323→330, tsc 3175 flat, build exit 0. Live-certified VV1–VV4 on all three BoQs. **(2) Slice 2 of 2 — COPY-FORWARD (the write-side)** (feat `4d741c6c` + docs `185b2a19`) — FULL-STACK, NO migrate. From the read-only version-history view, the user selects priced rows on an OLD version and copies their RATES into the CURRENT version. RATES ONLY — never structure / amount / qty. THREE per-row outcomes, ALL shown in a review-before-apply dialog BEFORE any write: (1) EXACT-MATCH FAILS (`source_row_number` OR `description` differs) → HARD SKIP, non-overridable, server-enforced; (2) match + dest EMPTY → clean copy; (3) match + dest already priced → CONFLICT (per-row overwrite/keep + bulk "overwrite all"/"keep all", default leans KEEP). **BUILD SHAPE = OPTION B (owner): server-side plan + ATOMIC apply** — the correctness-critical logic (exact-match + rate-role column re-resolution) lives server-side as the single source of truth; apply is one transaction, full rollback on any error. **BACKEND (`pricing.py` only):** behavior-preserving extraction of `save_cell_price` into `_resolve_and_guard_cell` + `_write_cell_price_record` (no commit — proven byte-for-byte: `test_pricing` 166 ran green AFTER the extraction, BEFORE the new code); the shared priceability predicate `_node_priceable_without_override`; the CF3 safety rule `_current_rate_column_index` (the RESTRICTED rate-role-only inverse `{(area, rate_kind): col_letter}` — re-resolve the target column by SEMANTIC, NEVER the bare source col_letter, since column roles drift across versions; non-rate roles excluded by construction because a generic inverse is ambiguous — `append_to_notes` maps one role+area to several letters; a duplicate rate-key throws); NEW whitelisted `get_copy_forward_plan(boq, sheet, from_version)` (READ-ONLY classifier) + `apply_copy_forward(boq, sheet, from_version, decisions)` (WRITE, ATOMIC — RE-DERIVES the plan server-side so a client outcome/target/rate is NEVER trusted; sheet-level gates checked once; ONE lock acquire + ONE commit; `try/except → rollback` makes a mid-batch failure leave NOTHING written). **TWO OWNER DESIGN CALLS (this session, both built):** (a) **priceability RE-GATE** — copy-forward writes WITHOUT the override; a source-priced row that is non-priceable in the CURRENT structure is a 4th HARD-SKIP reason (`non_priceable`), shown + never written (highlight discrepancies, never silently fix); (b) **default selection** — clean rows pre-ticked to copy, conflicts pre-ticked but defaulting to KEEP (the destructive overwrite needs deliberate per-row or bulk intent). **FRONTEND:** NEW `CopyForwardDialog.tsx`; the "Copy rates forward" trigger in `VersionRibbon` (new optional `onCopyForward` prop, shown ONLY in history mode — the ONE write action reachable from history; it writes to CURRENT, not the viewed version, so the slice-1 read-only spine + `locked` choke + `pricingRowPropsAreEqual` are ALL untouched); on apply → a transient summary, return to live, `mutate()` the live read. **LIVE-DATA CAVEAT:** outcomes 1 (non-match) + 3 (conflict) do NOT occur in live data today (the only priced old version, Electrical, is identity-stable and current v6 is unpriced), so they are covered by SEEDED tests, not live data — the conflict + non-match paths are lightly exercised on real data until manufactured. Tests: `test_pricing` 166→176 (+10 `TestCopyForward`: all outcomes + counts + overwrite/keep + outcome-1-never-written + column-drift re-resolution + atomic rollback + crafted-client-ignored + guards), vitest 330→339, tsc 3175 flat, build exit 0. Live-certified including a MANUFACTURED outcome-3 conflict (priced a current cell an old version also priced). **FRONTEND-GATES-IN-CONTAINER NOTE (carried, unchanged):** the win32-arm64 host cannot start vitest — the installed rolldown bindings are linux-arm64 (an existing test fails identically; environmental, not this work). So BOTH slices ran vitest / tsc / vite build IN-CONTAINER (`frappe_docker_devcontainer-frappe-1`); the counts above are from those in-container runs. **LIVE-CERT (owner, this session):** both slices owner-certified live on all three canonical BoQs (145/150/166) before the single batched push — version-view VV1–VV4 (Electrical's multi-version dropdown, a never-priced version, a grid-only multi-version sheet, the round-trip back to editable) and copy-forward (clean copies land in current; a manufactured conflict overwrites/keeps; the round-trip history→copy→back-to-live-editable holds). Green tests + Checklist B were treated as necessary-but-insufficient. No live-cert carries. **PUSH (resolved — the deferred-resync question is CLOSED):** the two slices were committed but UNPUSHED through the session (owner batched the push to after copy-forward certified). The mechanical-safety triplet at push time confirmed local `185b2a19` four commits above `761c4bf3`, with `origin/feature/boq-phase-5` stale at the old `698a5fca` (a different lineage already absorbed into develop via PR #1042). `git push -u origin feature/boq-phase-5` succeeded as `* [new branch]` (the old remote ref had been cleaned up) — harmless, same result; the branch now tracks origin at `185b2a19`, in sync, tracking re-established. Build-sequence item (4) DONE. **FINALIZE / REVERT — STILL RETIRED (unchanged):** the inert `is_finalized` hooks stay DORMANT. The "what signals 'pricing is done' / which version carries forward" question that the version-view + copy-forward scoping was meant to own is now ANSWERED by construction: "done" is system-inferred (the most recent pricing per committed version), and copy-forward is USER-INITIATED (the user picks the source version + the rows), so no explicit "done" milestone is needed. The finalize/revert ceremony stays parked, not revived.
**BUILD SEQUENCE (owner-locked, advanced):** ✅ (1) collapse/expand → ✅ (2) collapse/expand-all → ✅ (3) lock/unlock edits → **(4) version-view + copy-forward** (NEXT — own SCOPING pass first; that scoping OWNS the "what signals 'pricing is done' now that finalize is retired" question) → **(5) copy/paste/fill-down + undo/redo.** Finalize/revert stays retired-for-now (the inert `is_finalized` hooks stay dormant). DEFERRED/not-building-now: annotation-visibility toggle (dropped), frozen-left (structural two-pane slice), rate-guidance helpers (Slice 8 arc). **PK HOUSEKEEPING THIS FOLD (agreement #17): upload this handover v5.68 (delete v5.67); upload `BoQ_Pricing_Editor_Design_Scoping` v1.15 (delete v1.14); upload the archive v1.30 (delete v1.29 — it now carries the rotated-out v5.65 dashboard; v5.64 was already archived at v1.29); re-upload `boq-upload-plan.md` + `frontend/CLAUDE.md` + root `CLAUDE.md` (all carry this session's three slices' docs commits — root CLAUDE.md changed too, because lock/unlock is full-stack).** The formula-builder doc (`BoQ_Amount_Formula_Builder_Design` v1.4a) is UNCHANGED this session — no re-upload. **DASHBOARD ROTATION THIS CYCLE:** v5.68 gets its own Trigger-state dashboard (below); the 3-recent window advances to v5.68/v5.67/v5.66; **v5.65's dashboard rotates OUT to the archive (→ archive v1.30); v5.64 (already archived at v1.29) is stubbed to its v1.29 home this fold, reconciling the v5.67 fold which had left it inline.** --- > **[NOTE — v5.68 is a CODE cycle]** v5.68 records three post-deliverable Phase-5 slices — collapse/expand (`c3eda175`), collapse/expand-all (`29d39d60`), and lock/unlock edits (`b1601789`, FULL-STACK + MIGRATE) — BUILT + owner-certified live on all three BoQs (145/150/166) + PUSHED on `feature/boq-phase-5` (tip `698a5fca`, origin in sync; three fast-forwards `c339c5d8..698a5fca`). Collapse reuses a new `collapse.ts` leaf with the row memo untouched (chevrons flip via `CollapseContext`); collapse-all is a bottom-ribbon toggle reusing that engine; lock/unlock is the pricing twin of the review-screen "Finalized" freeze (additive `BoQ Sheet.is_locked` + `_guard_sheet_not_locked` in the six save_* endpoints + a top-ribbon teal toggle; server-enforced, the owner reversed an initial frontend-only call by anchoring to the Mark-Finalized pattern). test_pricing 151→158; Vitest 307→323; tsc 3175 throughout; one migrate (the cashflow-patch wart is Abhishek's, pre-existing). The design doc is now v1.15. NEXT = (4) version-view + copy-forward (own scoping pass first — it owns the "what signals done" question). The new Trigger state v5.68 dashboard below is CURRENT (supersedes v5.67). **Last updated:** 2026-06-25 IST (v5.67 — **PHASE 5 (THE PRICING EDITOR) — SLICE 5 (THE EXCEL WRITE-BACK — THE DELIVERABLE) IS BUILT, LIVE-CERTIFIED ON ALL THREE CANONICAL BoQs (145/150/166), AND PUSHED. THE WHOLE EDITOR SPINE IS NOW BUILT. ALSO THIS SESSION: THE HUB FOOTER REWORK + THE EDITOR TOOLBAR TWO-RIBBON REORG (= TOOLBAR PART 2 LAYOUT). FINALIZE/REVERT RETIRED FOR NOW; BUILD SEQUENCE RE-LOCKED.** Branch `feature/boq-phase-5` tip `49366849`, origin in sync — **PUSHED this session** (host PowerShell, 0-behind fast-forward `c306c9a3..49366849`, coordinated with Abhishek). Seven slices landed, all owner-certified live + pushed. **WHAT LANDED THIS SESSION (each recon-first per #56; on `feature/boq-phase-5`):** **(1) Slice 5a — Excel write-back BACKEND** (feat `43e89bf4` + docs `f6bd7fe3`) — the deliverable's engine. NEW module `api/boq/wizard/export_writeback.py` + endpoint `export_priced_workbook(boq_name, sheet_names)` (a SIBLING module, NOT folded into the 2076-line pricing.py per the >500-line split rule). **COPY-ON-WRITE** (S3 fetch → `shutil.copy` → stamp+save ONLY the copy; the original temp + the S3 object are NEVER written — the client's file is immutable). `openpyxl.load_workbook(copy, data_only=False)` (the `data_only=True` value-trap — loads cached values, discards formulas on save — explicitly avoided). **RATES ONLY with PER-CELL FORMULA SKIP:** a rate cell whose `cell.data_type == 'f'` (e.g. a VRF combined-rate `=SUM(supply,install)`) is LEFT UNTOUCHED and REPORTED (the user is told which columns we left alone). **COLOR → `openpyxl PatternFill`** (the 8 token→hex map decided in `_COLOR_HEX`: red `FFC7CE` / orange `FFD9A0` / yellow `FFEB9C` / green `C6EFCE` / blue `BDD7EE` / purple `E1D5F7` / pink `FBD4E4` / grey `D9D9D9`) — a real background fill, not a border. **REMARK → a "Nirmaan Remarks" TRAILING column** one past the TRUE data edge (rightmost MAPPED col from `column_role_map`, NOT the inflated openpyxl `max_column` — a recon surprise: empty styled cells push `max_column` far past real data) with a hard empty-column safety check that THROWS rather than overwrite. **POST-SAVE FIDELITY ASSERTION** (re-opens the saved copy, asserts formula / merge / worksheet / defined-name counts unchanged — non-vacuous, proven to FAIL on each invariant diverging). NEW additive `BoQ Sheet.last_exported_at` (Datetime, stamped via `frappe.db.set_value` not `doc.save` — the list-JSON `area_dimensions` gotcha). RETURN = **base64-in-JSON** (the file-only `frappe.response.filecontent` idiom can't also carry the skipped-formula report, so one JSON response carries the file bytes + the metadata; large-file note: revisit a streamed transport only if BoQ files ever grow multi-MB — 145 is ~713 KB). `test_pricing` 126→145. Live-verified end-to-end on all three BoQs (166's 4585 defined names round-tripped). **(2) Slice 5a AMENDMENT — priced-cell verification highlight** (feat `95f07c47` + docs `0b0b7eaf`) — owner-requested: an ALWAYS-ON Nirmaan-facing "which cells did we write" aid. A muted-teal solid `PatternFill` (`_PRICED_HIGHLIGHT_HEX = "B7E4D8"`, a SEPARATE constant from the 8 user tokens, distinct from user green `C6EFCE` / blue `BDD7EE` so a system mark never reads as a user tag) on every rate cell the write-back ACTUALLY stamped. **RULE 1 stamped-only:** `_stamp_rates` now returns `(skipped, written)`; the highlight is driven by `written`, so a SKIPPED formula rate cell gets NO teal — teal doubles as the live signal of the rates-only + formula-skip rule. **RULE 2 system-wins:** `_apply_priced_highlight` runs AFTER `_apply_colors`, so on a collision the teal overrides a user color tag on a stamped rate cell (owner-decided: the verification aid must be EXHAUSTIVE over written cells — the ONE place a system fill beats a user fill; this is a verification aid, not a client decoration). **RULE 3:** only stamped RATE cells (never remark/amount/remark-column). `test_pricing` 145→151. Live-verified on 145 (E24/H24/E34 teal, amount F36 not, version-isolated — old-version pricing correctly excluded). **(3) Slice 5b — Download priced tender HUB UI + staleness** (feat `b215c277` + docs `d3539e5b`) — FULL-STACK (small additive backend, NO schema/migrate). **Backend:** `commit_gate.get_committed_state` now ALSO returns per committed sheet `last_exported_at` (FREE — folded into the existing `is_current=1` BoQ Sheet lookup) + `pricing_changed_since_export` (a bool: `max(priced_at over BoQ Cell Pricing, colored_at over BoQ Cell Color, remarked_at over BoQ Cell Remark)` for the sheet's CURRENT commit_version > `last_exported_at`, or content-exists-but-never-exported → True; **VERSION-ISOLATED** via `_latest_change_by_sheet_version` grouping by `(sheet_name, committed_version)` — an OLD version's timestamp never marks the current version stale; existing return keys UNCHANGED). `test_commit_gate` 20→27. **Frontend:** a NEW "Download priced tender" hub button (gated on `committedMap.size`, DELIBERATELY distinct from the renamed "Export Parsed BoQ" draft-review export) → NEW `PricedTenderDialog.tsx` (mirrors CommitDialog's committedState rows + ExportWorkbookDialog's self-contained download; all finalized rows ticked by default; grid-only general-specs rows SHOWN but DISABLED "no rates to write"; per-row last-exported / changed-since sub-line) → NEW `downloadBlob.ts` (`base64ToBytes` PURE + unit-tested + the exportReviewXlsx Blob/anchor download tail) decodes `content_base64` → Blob → download; a skipped-formula acknowledge-note names the formula-rate columns left untouched; a per-sheet "priced since last export" amber chip on `SheetCard` (rides the existing `committedState` prop). Vitest 303→307 (+4 `base64ToBytes`), tsc 3175 (0 new). `export_writeback.py` UNCHANGED — 5b only CALLS it. **(4) Hub footer rework** (feat `3c06e35e` + docs `cbf6bb49`) — FRONTEND, presentational. The hub footer's 6-button row was too cluttered → 4 visible (Parse / Re-parse / Commit / Tendering) + an "Export" overflow menu holding the 2 export actions; **"Export Finalized" RENAMED → "Export Parsed BoQ"** (button + the `ExportWorkbookDialog` title; this disambiguates it from "Download priced tender" — the two exports are different things and must never read as duplicates); the moved menu items show a muted disabled-reason line (option c — a disabled Radix menu item can't carry a hover-tooltip, so the reason goes inline); `size="sm"` compaction. tsc 3175 (0 new), Vitest unchanged. **(5) Editor toolbar two-ribbon reorg (= TOOLBAR PART 2 LAYOUT — this DISCHARGES the Part-2 row)** (feat `26c4abe7` + docs `49366849`) — FRONTEND, presentational, MOVE-ONLY (zero behavior change). `SheetPricingPage.tsx`'s single crowded toolbar split into TWO ribbons sandwiching the sheet-tab strip: a **TOP ribbon** (Back / Full screen / Summary / Review (N) / Price any row / Save now + `ml-auto` status text — document/screen actions) ABOVE the tabs, a **BOTTOM ribbon** (Show unpriced / Search group / Columns / Show: Spacers·Notes·Subtotals — grid view-controls) BELOW. The bottom ribbon preserves the SAME `{!isGridOnly}` gate (a grid-only general-specs sheet renders NO bottom ribbon — owner-confirmed behavior preserved); coupled groups (search+arrows+counter, Columns popover, Show toggles) kept as intact JSX subtrees; Summary/Review kept PLAIN (no pressed state added — that would be a behavior change). All backing state is local `useState` so the move severs no closure. tsc 3175 (0 new), Vitest 307 unchanged. A future **Lock/unlock-edits** button will join the TOP ribbon (its own slice — see the build sequence). **LIVE-CERT (owner, this session): BOTH owed Slice-5 live-certs DONE.** (i) The VRF formula-skip LIVE proof — a VRF combined-rate cell priced in the editor, re-exported, confirmed it appears in `skipped_formula_columns`, stays a formula in the downloaded file, and carries NO teal. (ii) The uncommitted / grid-only sheet unchanged check. Both passed on real data; neither is a carry. **The Slice-5 arc is CERTIFIED, not just built.** **FINALIZE / REVERT — RETIRED FOR NOW (owner, 2026-06-25).** The separate "finalize a pricing version as done + revert" ceremony is REMOVED from the active build sequence; **lock / unlock edits takes its slot.** The inert `is_finalized` hooks (declared-but-not-wired on `BoQ Cell Pricing` / `BoQ Cell Dismissal` / `BoQ Cell Amount Formula`) STAY DORMANT — not ripped out — because the "what signals 'pricing is done' / which version carries forward" question is now DEFERRED to the version-view + copy-forward SCOPING (answered there with the full picture, before that build). Lock/unlock is a guard against accidental edits, not a milestone; the milestone concept is parked, not deleted. **BUILD SEQUENCE RE-LOCKED (owner, 2026-06-25, post-Slice-5):** **(1) collapse / expand** (single-row; carries the parent-jump reveal-then-scroll upgrade) → **(2) collapse / expand ALL** (the bulk version, split out as its own step) → **(3) lock / unlock edits** (takes finalize/revert's old slot; a NEW top-ribbon toggle that gates EVERY mutation path — rate / color / remark / "Price any row" / any future fill-paste — a REAL feature needing its own recon [inventory every edit path first] + design pass, NOT a move) → **(4) version-view + copy-forward** (own SCOPING pass first — and that scoping OWNS the "what signals done now that finalize is gone" question) → **(5) copy / paste / fill-down + undo / redo.** Toolbar Part 2 (layout) DROPPED OFF the front — it landed this session as the two-ribbon reorg. **Finalize / revert REMOVED from the spine** (retired-for-now, see above). **DEFERRED / not-building-now:** annotation-visibility toggle (dropped), frozen-left (structural two-pane slice), rate-guidance helpers (Slice 8 arc). **PK HOUSEKEEPING THIS FOLD (agreement #17): upload this handover v5.67 (delete v5.66); upload `BoQ_Pricing_Editor_Design_Scoping` v1.14 (delete v1.13); re-upload `boq-upload-plan.md` + `frontend/CLAUDE.md` + root `CLAUDE.md` (all carry this session's docs commits). The formula-builder doc (`BoQ_Amount_Formula_Builder_Design` v1.4a) is UNCHANGED this session (not touched) — no re-upload needed. The archive (v1.28) is UNCHANGED this fold.** The pre-Slice-5 parser-fix check (`231f3361`) is now MOOT — Slice 5 built + live-certified on the current committed data; the qty-bearing-preamble re-commit path remains a separate future follow-up. ---
**BUILD SEQUENCE RE-LOCKED (owner, 2026-06-25, post-toolbar-Part-1 + ergonomics):** **(1) Slice 5 — Excel write-back** (NEXT; the riskiest unproven piece; color→`openpyxl PatternFill`, remark→trailing Excel column; recon EARLY + thorough) → **(2) collapse / expand** (the slice right after Slice 5; collapse-all optional/droppable if troublesome; carries the parent-jump reveal-then-scroll upgrade) → **(3) toolbar Part 2 — layout rework** (button size/font/overflow/clustering, "Price any row" to overflow) → **(4) finalize / revert** → **(5) version-view + copy-forward** → **(6) everything else = copy/paste/fill-down + undo/redo.** **DEFERRED / not-building-now:** annotation-visibility toggle (dropped), frozen-left (structural two-pane slice), rate-guidance helpers (Slice 8 arc — each its own arc). **PRE-SLICE-5 CHECK (logged, owed before Slice 5 builds):** confirm Abhishek's parser fix `231f3361` ("preserve preamble quantities") did NOT shift the committed-data shape on the three canonical BoQs (145/150/166) — Slice 5 writes committed data back to Excel, and preamble quantities flow through that path. A three-BoQ committed-data-shape check (the three-BoQ rule). **DESIGN/PK CHANGES THIS CYCLE:** the Pricing-Editor design is bumped to **`BoQ_Pricing_Editor_Design_Scoping` v1.13** (§14 stale rows corrected — parent-jump + column-resize + search/column-hide/row-filters + Excel-coord-anchoring → BUILT; frozen-left → DEFERRED with the structural reason; annotation-toggle → DEFERRED/not-building; the build sequence re-locked; the branch re-baseline + shared-branch reality recorded; the §4 surface text annotated with build status; the toolbar NOTE updated [Part 1 done]; the stale "End v1.11" footer brought current to v1.13). The column-resize spec is bumped to **`BoQ_Column_Resize_Spec` v1.1** (marked BUILT; the "resize and frozen-column independent" line corrected; the `table-fixed`/`colgroup` foundation + the frozen-left structural deferral recorded). The formula-builder doc (`BoQ_Amount_Formula_Builder_Design` v1.4) is UNCHANGED by this arc (nothing touched the formula feature) — its cross-reference to the design doc reads "v1.12"; a cosmetic version-number bump to v1.13, not substantive. **PK housekeeping (agreement #17): upload v5.66 (delete v5.65); upload `BoQ_Pricing_Editor_Design_Scoping` v1.13 (delete v1.12); upload `BoQ_Column_Resize_Spec` v1.1 (delete v1.0); re-upload `boq-upload-plan.md` + `frontend/CLAUDE.md` + root `CLAUDE.md` (current at `fa84756e` — they carry the toolbar/jump/flash/resize docs commits; in-repo docs are updated per-slice by CC, so they are current, not part of this PK fold's stale-row work); upload archive v1.28.** **DASHBOARD ROTATION THIS CYCLE:** v5.66 gets its own Trigger-state dashboard (below); the 3-recent window advances to v5.66/v5.65/v5.64; **v5.63's dashboard rotates OUT to the archive (→ archive v1.28).** **STANDING NOISE:** unchanged (`.claude/settings.local.json` + `nirmaan_stack/patches.txt` + the synthetic parser fixtures + the 3 untracked root artifacts — `BOQ-26-00145_SOW_faithful.csv`, `Handover doc.pdf`, `tendering-won.patch`; the stray `# currentDate` blob at the tail of `frontend/CLAUDE.md` is intact + must survive edits). NEW noise note: a stray `~$`-prefixed editor lock file (`frontend/.claude/plans/~$q-upload-plan.md`) appeared once in `git status` during this arc — an Office/editor temp file, untracked, harmless, cleared on its own; a `~$*` gitignore is a candidate if it recurs. **OWNER ACTION:** the branch re-baseline is Abhishek's (done); the next push of the re-cut `feature/boq-phase-5` is coordinated with him. The `backfill_cashflow_gap_limited` migrate wart remains Abhishek's (informed, his to fix). **MANDATORY FIRST ACTION at next resume (agreement #41):** Checklist C audit against the Trigger state v5.66 dashboard below; paste a fresh `git log` and confirm whether the branch tip is still `e9833dc5` (or moved, if Abhishek pushed). **NEXT:** Slice 5 — Excel write-back (recon EARLY + thorough; the one real unknown left), preceded by the pre-Slice-5 parser-fix check. The handover master-narrative body below the dashboards still lags the editing arcs — a near-term housekeeping fold, tracked. Recon-first on all build. The AS-BUILT doc remains DEFERRED.) > **[NOTE — v5.66 is a CODE cycle + a §14 stale-row correction fold]** v5.66 records toolbar Part 1 (feature parity — search / column-hide / row-filters) + parent click-to-jump (`f8163aa7`) + the 3s landing flash (`a17ab0ca`) + column resize (`397f6d5d`→`4bb40ea4`) BUILT + owner-certified live on all three BoQs (145/150/166) + PUSHED on `feature/boq-phase-5` (arc tip `fa84756e`). Frozen-left was attempted bundled with resize, found structurally unworkable (cell-level multi-column sticky-left doesn't track horizontal scroll), reverted, and DEFERRED to a dedicated two-pane structural slice (scheduled, not dropped). Excel-coord-anchoring confirmed BUILT; annotation-visibility toggle DEFERRED/not-building. Abhishek then RE-BASELINED the branch onto develop (local now `e9833dc5`, SHARED — he works in parallel; his fuzzy-search `987f2b23`, parent-chain read-views `47375d78`, and parser preamble-qty fix `231f3361` are in). The design doc is now v1.13; the column-resize spec is v1.1 (BUILT). The new Trigger state v5.66 dashboard below is CURRENT (supersedes v5.65). NEXT = Slice 5 (Excel write-back), recon EARLY, preceded by the pre-Slice-5 parser-fix three-BoQ check. **Last updated:** 2026-06-24 IST (v5.65 — **PHASE 5 (THE PRICING EDITOR) — CLUSTER B (FORMULA-VS-DOCUMENT RECONCILIATION) IS BUILT, OWNER-CERTIFIED LIVE ON ALL THREE CANONICAL BoQs (145/150/166), AND PUSHED. THIS CLOSES THE SLICE-4 ARC.** A full-stack code cycle (one MIGRATE slice — a new doctype) on top of the v5.64 4b-A computed-flag layer, on `feature/boq-phase-5`. **Repo tip now `1f199f74`** (origin IN SYNC, HEAD = origin; the 2-commit stack `923dfb83..1f199f74` pushed in ONE fast-forward push `b2c1b1fa..1f199f74` from host PowerShell, #53). **NOTE — this v5.65 fold is the long-deferred PK CATCH-UP fold: the handover header had been lagging since 4b-A; v5.65 records Cluster B AND the full inter-session arc the in-repo docs carried but PK never folded — the editor-perf fix, the asymmetric Preamble qty-bearing gate, the mandatory amount-formula gate, the amount-column formula-status badge, and the 4c full-screen editor — all of which landed between the v5.64 tip `9c45c9b9` and this cycle. See "THE INTER-SESSION ARC" below.** **WHAT CLUSTER B IS:** when a committed (DOCUMENT) amount and the formula-computed amount DIVERGE for the same amount cell, the editor FLAGS the divergence (mismatch only — never auto-fixes; the tendering document is CLIENT-OWNED, §0), lets the user CHOOSE per cell which value wins (keep-document / use-formula), stores that choice stickily per committed version in a NEW per-CELL overlay doctype, and routes the CHOSEN value to BOTH the grid cell AND the rollup/summary/grand-total (single source of truth — what the cell shows is what totals). It DEPENDS on the mandatory formula gate (universal formula coverage makes the comparison universal) and layers on F4's display-only architecture. **FOUR OWNER DECISIONS (LOCKED at recon, built exactly):** **(D1) DEFAULT = DOCUMENT.** A divergent cell that is unset (or keep_document) shows the DOCUMENT value in BOTH the grid and the rollup, highlighted until resolved; take_formula shows the formula value. This DELIBERATELY REVERSES the prior formula-wins display (§0 — silently showing the computed number over the client's is the auto-fix the governing principle forbids). **(D2) VISUALIZATION = BOTH CHANNELS, strong cue.** (a) a STRONG, easy-to-spot in-cell cue — a solid VIOLET `ReconcileBadge` pill + a two-option chooser popover labelled with both numbers (a DISTINCT channel: background=priced tint / left-border=color annotation / gutter=4b-A flag marker are all taken); a RESOLVED choice renders a MUTED grey pill (still visible, no nag); (b) a NEW "divergence" `ReviewEntry` kind in the unified strip carrying the unresolved count, click-jumps via `scrollToRow`, drops out when resolved. **(D3) INVALIDATION = AUTO-RESET, SURGICAL, PER-CELL, COLUMN-AWARE.** A rate save clears a stored choice ONLY on amount cells (on that row) whose formula REFERENCES the edited rate operand (NOT the whole row — more surgical than the per-row dismissal re-arm); a formula save/remove clears that column's cells (remove = silent); a re-commit mints a new committed_version so old choices orphan by version (no live code). No "survive but warn" path. **(D4) ROLLUP SINGLE SOURCE.** The chosen value resolves ONCE inside `pricingRollup.rowOwnAmount`, so Option-1 (tree) and Option-2 (flat) both read the same `ownByIdx` and the reconciliation integrity guard stays balanced by construction. **HOW IT WAS BUILT — Cluster B (recon-first #56; one feat + one docs):** **(feat `923dfb83`)** — **Backend:** a NEW doctype **`BoQ Cell Reconciliation Choice`** (`nirmaan_stack/nirmaan_stack/doctype/boq_cell_reconciliation_choice/`; istable:0, track_changes:1, autoname `BRCC-.YY.-.#####`; bare-stub controller) mirroring `BoQ Cell Dismissal`'s freeze-and-supersede model. **IDENTITY = (boq, sheet_name VERBATIM #152, excel_row, col_letter, committed_version)** — PER-CELL (carries `col_letter`, the difference from the per-ROW dismissal). `choice` Select keep_document/take_formula; "unset" = the ABSENCE of a current record (the default -> document wins, D1). Own freeze-and-supersede triple `choice_version`/`is_current`/`chosen_at` + `chosen_by` + an inert `is_finalized`; one-current-per-cell ENDPOINT-enforced. Endpoints in `pricing.py`: `save_cell_reconciliation_choice` (resolve committed row [no priceability gate, like `save_cell_color`], lock-on-write AFTER resolve / BEFORE freeze+insert [reject-mutates-nothing], freeze prior via `frappe.db.set_value` then `doc.insert`; a blank/None/"unset" `choice` CLEARS) + `get_sheet_reconciliation_choices` (bare whitelist read) + a flat per-CELL `reconciliation_choices` key `[{excel_row, col_letter, choice}]` merged into `get_priced_rows`. **The surgical re-arm machinery (D3):** `_committed_descriptors` (refactored out of `_committed_amount_descriptors`); `_bind_ref` (wildcard leaf -> area; mirrors frontend `amountFormula.bindRef`); `_pick_formula_record` (override>wildcard; mirrors `pickFormula`); `_node_refs_rate` + `_amount_col_depends_on_rate` (the operand-tree walk — direct rate-leaf match + transitive amount-refs-amount, cycle-guarded — mirroring the F2 evaluator so the re-arm matches EXACTLY which amount cells a rate feeds); `_rearm_cell_recon_choices` wired into `save_cell_price` AFTER the price insert + the dismissal re-arm, BEFORE the single commit; `_rearm_column_recon_choices` wired into BOTH `save_amount_formula` paths (save + clear). The shared operand helpers mirror the frontend `amountFormula` precedence/binding — kept in sync, NOT imported (the one-way dependency rule). **Frontend:** a NEW pure leaf **`reconcile.ts`** (the ONE place the comparison + resolution live, so grid/strip/rollup agree by construction): `RECON_EPSILON_ABS`(0.01)/`RECON_EPSILON_REL`(1e-9) + **`amountsEqual`** (the SHARED tolerance — `pricingRollup.ts` now imports it for its Option-1-vs-Option-2 integrity guard, the private constants removed there, so ONE epsilon everywhere), `amountsDiffer`, `resolveDivergence` (the D1 rule), `reconChoiceKey` + `buildReconChoiceMap`. A LEAF (imports only types) so PricingGrid/priceability/pricingRollup all import it with NO cycle (PricingGrid can NOT import pricingRollup — the cycle `reconcile.ts` exists to dodge). Divergence fires ONLY when `evaluateAmountCell` yields `cell.kind === "value"` (a real computed number; not_yet/broken/no-formula never flag — the F1 fact: the mandatory gate guarantees coverage, NOT a number). `PricingGrid.tsx` gains the `ReconcileBadge` component + detection in the amount block + a `reconChoices` -> useMemo'd per-sheet `reconChoiceMap` threaded to the memoized row (added to `PricingGridRowProps` + `pricingRowPropsAreEqual` — memo intact). `priceability.buildDivergenceEntries` (one entry per row, unresolved diverging cols, resolved drops out). `pricingRollup.rowOwnAmount` gains the choice param (D4). `SummaryPanel` + `SheetPricingPage` thread `reconChoices`; `handleSaveReconChoice` mirrors `handleSaveDismiss` (withheld when locked -> static read-only pill). New wire types `ReconChoice`/`ReconciliationChoiceRef`/`ReconChoiceSaveArgs` + the `reconciliation_choices` key on `GetPricedRowsResponse` in `boqTypes.ts`; the strip's per-entry dismiss button is WITHHELD for a divergence entry (its kind is not a dismissal token — the chooser IS its resolution). **(docs `1f199f74`)** — all three in-repo docs updated (plan + root CLAUDE + frontend CLAUDE). **TEST/BUILD (bench-verified in-session, NOT quoted from any doc):** **backend `test_pricing` 110 -> 126** (the NEW `TestReconciliationChoice` = 16: CRUD + freeze-and-supersede + the exactly-one-current invariant; keep_document vs take_formula distinct; clear/unset; NEG non-existent cell/sheet/boq/missing-args/invalid-token; the SURGICAL re-arm proven non-vacuous — a rate save on col E clears F@34 [references E] but NOT I@34 [qty-only] nor F@35 [different row], and a rate save on the unreferenced col H clears nothing; formula-remove clears the column silently; formula-save re-arms the column). **frontend Vitest 245 -> 264** (NEW `reconcile.test` 12 + `pricingRollup` +4 [document-default, take_formula, keep_document, mixed-set Option-1==Option-2 balances] + `priceability` +3 [divergence fires only on kind==="value"]). **tsc 3175 baseline / 0 new / 0 in touched files** (NOTE: the prompt's "3178" was STALE — CC verified the live baseline is 3175; corrected here). **Vite build exit 0.** **bench migrate creates `BoQ Cell Reconciliation Choice` (23 columns = 12 custom + 11 Frappe standard, verified directly in the DB).** **MIGRATE WART (flagged, NOT ours, NOT blocking — a new loose thread):** `bench migrate` does NOT run fully clean — it aborts on a PRE-EXISTING, UNRELATED patch error: `nirmaan_stack.patches.v3_0.backfill_cashflow_gap_limited` has no `execute` (from commit `4cc217f8`, a cashflow rename — Abhishek's, arrived via the develop merge; `git diff HEAD -- patches/` is empty, untouched by this slice). CC correctly STOPPED (the "don't touch patches" rule) and verified the Cluster B doctype's table was created anyway (model/schema sync runs BEFORE post-model-sync patches) and the TEST DB auto-migrates fine (126 backend tests green). **Owner ACTION: Abhishek informed — his to fix on his side.** Logged here as a tracked-but-owner-handled loose thread, not a C2 carry. **THE INTER-SESSION ARC (folded into PK for the first time at v5.65 — landed between the v5.64 tip `9c45c9b9` and Cluster B; the in-repo docs carried these all along, PK had not):** **(1) editor-perf fix** (`9f1ec2b1` + `910352d6`) — PricingGrid ROW-LEVEL memoization; the recon REFUTED the v5.64 "page-level flag recompute on activeCell" hypothesis — the real cause was ZERO memoization, every cursor move re-rendered all rows. **(2) asymmetric Preamble-only qty-bearing rate-edit gate** (fix `672dae0f` + docs `31369808`) — owner-locked: a Line Item is ALWAYS rate-editable (a zero-qty Line Item is a valid rate-only line); a Preamble is editable ONLY if qty-bearing ("Definition A" = qty anywhere: scalar `qty_total` OR any `qty_by_area` non-zero); the override unlocks both. A DELIBERATE, recorded divergence: this gate's qty definition is intentionally LOOSER than `isPriceableLine` (which drives flags/count) — do NOT "align" them. OWNER DECISION banked: a zero-qty rate-only Line Item is deliberately EXCLUDED from the N-of-M priced count + needs_rate (a genuine quirk, NOT a bug; `isPriceableLine` stays unchanged). **(3) mandatory amount-formula gate** (feat `172ac735` + docs `0ec97fcc`) — every amount column MUST have a declared formula before ANY rate is editable; the gate is ABSOLUTE (the "Price any row" override does NOT bypass it — `formulasComplete` sits OUTSIDE the override on both client and server); per-COVERAGE predicate via `pickFormula` (a wildcard default covers all per-area columns of its kind; zero amount columns = trivially complete); shared `_formula_target_matches_column`/`_formula_covers`/`isAmountColumnCovered` (one source of truth, badge<=>gate by construction); a "Declare amount formulas to enable rate entry" banner when incomplete; fixed 7 existing backend test classes whose shared fixture had amount columns but no formulas (Option 1: declared formulas in each affected setUpClass, contained to test_pricing.py). REVERSES the F1–F4 "formula optional" property (deliberate). **(4) amount-column formula-status badge** (feat `fb0d25c8` + docs `cca150b0`) — a leading amber(pending)/green(covered) ƒ badge in front of each amount column header that IS the formula-builder trigger (status + action merged); the old far-right preview line removed (that relocation fixed a layout/visibility render bug — NOT a data bug; the recon REFUTED the wildcard-miss hypothesis); a pending amber header tint; read-only branch preserved. **(5) full-screen / maximize editor (Slice 4c)** (feat `1894012d` + docs `b2c1b1fa`) — an in-app maximize (root className toggle to `fixed inset-0`, NOT native Fullscreen, NOT a Dialog/portal). THE LOAD-BEARING RULE: one JSX tree, only the wrapper class flips, so expand/collapse NEVER remounts the grid -> drafts/cursor/lock/all state survive; `expanded` is a per-grid prop, never a per-row prop (perf memo intact); Esc-to-exit via a defaultPrevented-guarded window listener; toggle rendered outside the `!isGridOnly` gate (works on read-only sheets). **THE DEVELOP MERGE (this arc):** Abhishek merged + rebased work into develop; Nitesh's local feature branch briefly drifted onto develop's tip during a bare `git pull origin develop` (the perf fix APPEARED lost — root cause was BRANCH-POINTER DRIFT, not data loss; the fix was always safe on origin). Recovered via `git reset --hard origin/feature/boq-phase-5` then a deliberate `git merge origin/develop` (brought ~8000 lines of Abhishek's AI-validation work in cleanly, merge commit `cbb93b16`, no conflicts). LESSONS now standing rules: the two-lane model (Nitesh only on feature/boq-phase-5, Abhishek on his own branch, sync THROUGH develop via PR); the mechanical-safety triplet (`git fetch` + `git status` + `git log --oneline -8`, pasted, drift-checked) before ANY push/pull; NEVER a bare `git pull origin develop` on the feature branch — always `git fetch` then a deliberate `git merge origin/develop`; the GitHub "delete head branch on merge" pattern is harmless (recreates on next push). **THE TEST-COMMAND CHANGE (post-merge — record so the broken path is not re-tried):** the raw `python -m unittest` parser/pricing path now FAILS at import post-merge — the merged `boq_ai_assist` opens a log at module load. **Use `bench --site localhost run-tests --module nirmaan_stack.api.boq.wizard.test_pricing` for backend.** Vitest in-container: `MSYS_NO_PATHCONV=1 docker exec ... node node_modules/vitest/vitest.mjs run` (host node_modules is platform-mismatched; CC runs it in the container). **THE SLICE-4 ARC IS NOW COMPLETE:** 4a (remarks+color) BUILT; 4b-A (computed flags — Cluster A) BUILT; 4b-ACKNOWLEDGE (the per-row dismiss layer) BUILT; the asymmetric gate + formula gate + badge + 4c full-screen BUILT; **4b Cluster B (formula-vs-document reconciliation) BUILT + certified + pushed = ARC CLOSED.** The Phase-5 critical path remaining: **toolbar/header rework Part 1 (feature parity)** -> **Slice 5 (Excel write-back — the riskiest unproven piece, produces the actual priced-workbook deliverable; recon EARLY; color -> `openpyxl PatternFill` fill, remark -> trailing Excel column)** -> **toolbar/header rework Part 2 (layout)** -> finalize/revert (6) -> version-view + copy-forward (7). **OWNER DECISIONS THIS CYCLE (LOCKED):** **(a) the four Cluster B decisions** D1–D4 above (built). **(b) TOOLBAR RE-SEQUENCE SQUARED (resolves the v5.64 tension).** The owner SPLIT the toolbar work into two parts, which dissolves the conflict between "owner: toolbar before Slice 5" and "design doc: toolbar layout after the export button exists": **Part 1 = bring the REVIEW-SCREEN toolbar FEATURES into the pricing editor (search / column-hide / etc.) — do this NOW (next), no dependency on the export button.** **Part 2 = the LAYOUT rework (decrease button size, reduce font, move non-essential controls incl. "Price any row" to overflow, group into clusters) — do this AFTER Slice 5, so it is laid out ONCE with all buttons (incl. export) present.** Both prior statements were right about different halves; no overrule. **(c) the migrate wart is Abhishek's** (informed; tracked, not a C2 carry).
- If you find no issues at all, return an empty array [].- CRITICAL: output the JSON array and NOTHING else. Do not write any explanation, reasoning, or text before or after the array. Your entire response must be the array itself, starting with [ and ending with ]. ## The sheet to review Sheet name: {SHEET_NAME}Rows (JSON):{ROWS_JSON}``` **§9:** no new findings this arc (the AI bugs — blank-badges, edit_log from-value, modal-parity, run_ai_pass freeze — were caught + fixed in-arc; they are registered as failure modes in the Failure-Mode Register v1.5, not §9). **§3:** the AI arc's per-slice records live in `boq-upload-plan.md` + both `CLAUDE.md` files; a consolidated "Phase 4 — the AI auto-mapping arc (AI-1 → AI-3c-2d)" §3 record is owed at the next §3 fold, deferred to the as-built doc. **PK housekeeping (agreement #17): upload v5.59 (delete v5.58); upload `BoQ_Phase4_AI_AutoMapping_Design` v1.2 (NEW to PK — the canonical AI-surface design + the verbatim prompt) + `BoQ_Phase4_AI_Pass_Prompt` v1.1 (the verbatim-prompt doc); upload Failure-Mode Register v1.5 (delete v1.4); re-upload Wizard Design v1.27 (light touch — a pointer to the AutoMapping design); upload archive v1.22. The in-repo `boq-upload-plan.md` + BOTH `CLAUDE.md` files now carry the full AI arc — re-upload all three.** **DASHBOARD ROTATION THIS CYCLE:** v5.59 gets its own Trigger-state dashboard (below); the 3-recent window advances to v5.59/v5.58/v5.54 (v5.55/v5.56/v5.57 were docs-only or rode prior dashboards) — three dashboards present, so NO rotation OUT this cycle. **OWNER ACTION:** none owed (Abhishek's Phase-3 develop merge happened; the AI arc lives on `feature/boq-phase-4`). **MANDATORY FIRST ACTION at next resume (agreement #41):** Checklist C audit against the Trigger state v5.59 dashboard below. **NEXT (owner-confirmed):** a BREAK, then the **BoQ editor screen** — a NEW feature surface, starting with a proper scoping conversation (what it is, how it relates to the review screen + the locked designs, wizard-scope-discipline) before any recon/build. The optional `boq_ai.log` token-logging polish is a small parked follow-up. The AS-BUILT doc remains DEFERRED.) > **[NOTE — v5.59 is a CODE cycle]** The v5.59 CHANGES above records the ENTIRE Phase-4 AI auto-mapping arc (AI-1 → AI-3c-2d) BUILT + TESTED + PUSHED on `feature/boq-phase-4`. Repo tip moved to **`3310d321`** (origin in sync). This is the first handover fold of the AI arc — v5.58 and below predate it and framed Phase 4 as not-yet-started. The new Trigger state v5.59 dashboard below is CURRENT (supersedes v5.58). **Last updated:** 2026-06-18 IST (v5.58 — **THE "NEEDS ATTENTION" VISIBILITY ARC (F1–F4) IS BUILT, MANUALLY VERIFIED ON REAL DATA, AND PUSHED. Every failure/staleness signal the backend captures is now SHOWN to the user — persistently (hub card) and transiently (completion modals). This CLOSES the Phase-3 wizard; Abhishek's develop merge TOMORROW = the genuine Phase-3 completion.** A real code cycle (8 commits: F1 + F2 + F3/F4 each feat+docs, plus F4 verify-only), recon-first each, on `feature/boq-phase-3-new`. **Repo tip now `bfd0d4e8`** (was `88c9f75b`); `origin/feature/boq-phase-3-new` IN SYNC at `bfd0d4e8` (pushed this cycle, fast-forward `88c9f75b..bfd0d4e8`). **RECLASSIFICATION (owner decision — record so it does not read as drift):** the visibility arc is **part of the Phase-3 CLOSE, not Phase-4 prep.** The v5.57 framing called the robustness bundle "pre-Phase-4 hardening" and listed Abhishek's merge as owed NOW; the owner reframed it: the wizard is not *done being usable* until the user can SEE these failures, so the F-arc completes Phase 3, and **the merge is deferred until the F-arc is also pushed (it now is) → Abhishek merges tomorrow = Phase-3 completion.** The robustness bundle (1a/1b/Slice 2, v5.57) + this visibility arc together are the Phase-3 close. **WHAT THIS ARC IS — the user-facing payoff of #166 + the commit-failure signal:** 1a/1b/F1 made the backend CAPTURE failure/staleness signals correctly (reactive parse-failure stamps, proactive `get_stale_sheets`, durable commit-failure stamps); the F-arc finally SHOWS them. **WHAT LANDED (recon-first each; F1 backend → F2/F3/F4 frontend):** **(1) F1 — persist commit-failure (BACKEND)** feat `5c095b34` + docs `fb42d5a8`. `commit_boq` previously built an in-memory `failed[]` + `frappe.log_error` but PERSISTED NOTHING per-sheet, so a hub card had nothing durable to read across sessions. F1 adds two additive nullable `BoQ Sheet Draft` fields — `commit_failure_reason` (Small Text) + `commit_failure_at` (Datetime, read_only) — a PAIR, NO category Select (commit exceptions are heterogeneous/freeform, already flattened by `_commit_failure_reason`; a Select would be mostly "Other"). THE LOAD-BEARING ORDERING in the per-sheet `except`: `frappe.db.rollback()` FIRST (UNCHANGED Slice-5 orphan-prevention) → `_record_commit_failure` (set_value the draft, a DIFFERENT doctype, so the rollback doesn't sweep it) → an EXPLICIT `frappe.db.commit()` (REQUIRED — `commit_boq` has no trailing commit; a LAST-sheet failure would otherwise lose the stamp). Cleared on a successful re-commit via `_clear_commit_failure` folded into `_commit_one_sheet`'s success commit. `bench migrate` clean (runtime columns verified via `has_column`). `test_commit_pipeline` **44→49** (+5), incl. the KEYSTONE `test_f1_stamp_survives_rollback_and_no_orphan` (a post-freeze last-sheet failure: the prior version stays current [orphan-prevention intact] AND the stamp survives the rollback that discarded the freeze) + the load-bearing T2 orphan-prevention test still green. **(2) F2 — hub-card "needs attention" indicator (FRONTEND)** feat `1f1828d4` + docs `224e830b`. A consolidated chip on each `SheetCard`, COLLAPSED by default, CLICK-TO-EXPAND, showing whichever of THREE per-sheet signals apply: STALE CONFIG (from `get_stale_sheets` — reason, NO timestamp), PARSE FAILURE (`parse_failure_*` — category + reason + timestamp), COMMIT FAILURE (`commit_failure_*` — reason + timestamp). **Color (locked): RED when any failure stamp is present, AMBER when only stale-config.** Copy: "⚠ N issue(s)" (the de-duped distinct-line count). Three files: `boqTypes.ts` (+5 optional failure fields on `BoQSheetDraft` + `StaleSheet`/`GetStaleSheetsResponse`), `BoqHubPage.tsx` (ONE new `get_stale_sheets` fetch → `staleMap` mirroring `committedMap`; `mutateStale` wired into the parse-success/commit/save refresh sites so the indicator clears when fixed; passed at BOTH `<SheetCard>` render sites), `SheetCard.tsx` (the chip + inline-expand reusing the `editingLabel` idiom + `fmtCommittedAt`). **THE DE-DUP RULE (the one real render-logic subtlety):** 1a + 1b describe the SAME staleness with BYTE-IDENTICAL reason text (shared-helper guarantee); when a stale reason is present AND `parse_failure_category === "Config stale"` AND `parse_failure_reason === staleReason`, the two collapse into ONE "Stale config" line (carrying the parse timestamp). Other parse categories + commit failures are always their own line. Empty-state guards on the REASON STRINGS (not category) — healthy cards show nothing. The two failure stamps ride the existing BOQs payload (no new fetch); only `get_stale_sheets` is a new call. **(3) F3/F4 — completion modals highlight failures with reasons (FRONTEND)** feat `bfa71098` + docs `bfd0d4e8`. **F4 (`CommitResultsModal`) = ALREADY SATISFIED at Slice 5 — VERIFY-ONLY, no code change** (it already renders a destructive "Failed (N)" section with `{sheet_name} — {reason}`). **F3 (the parse-completion `AlertDialog` in `BoqHubPage`) = the real work:** the failed line went from `parseResult.failed.join(", ")` (bare names) to a per-sheet `<ul>/<li>` list (`AlertTriangle` + destructive `{name} — (category) reason`). DATA SOURCE: the `boq:parse_run_done` socket payload carries names only; the reason lives in the persisted `parse_failure_*` on the draft, read at render time via `boq.sheet_drafts.find(sd => sd.sheet_name === name)` (VERBATIM #152) — NO new fetch. **FRESHNESS FALLBACK (correctness requirement, not optional):** the modal opens immediately on parse-done while `mutate()` refetches async; the worker COMMITS the stamps BEFORE publishing, so once the refetch lands the open modal re-renders fresh — but the first render can be pre-mutate, so F3 guards with `reason && (...)` → renders the NAME ALONE in that window (never blank/undefined). **notParsed stays NEUTRAL** (names-only, muted) — stale-config drops land here and their reason is shown PERSISTENTLY on the F2 card, NOT duplicated in the transient modal. **TEST COUNTS (bench-verified):** `test_commit_pipeline` **49** (F1: 44→49); frontend slices tsc-clean (0 wizard-file errors, 3177 baseline unchanged) + Vite build exit 0. **MANUAL TEST — COMPREHENSIVE, on real `BOQ-26-00145` / `Lights` (capture-and-restore each, sheet restored to baseline after):** Test 1 (STALE) — amber chip + expand "Stale config" + reason (no timestamp); re-parse → Lights in the NEUTRAL not-parsed modal line (NOT red failed); chip persists (config still stale) ✓. Test 2 (PARSE FAILURE, direct stamp) — RED chip + expand "Parse failed (Parser error)" + reason + timestamp; `wizard_status` stayed `Parsed` (orthogonal) ✓. Test 3 (COMMIT FAILURE, direct stamp via `_record_commit_failure`) — RED chip + "Commit failed" + reason + timestamp, NO category label (freeform pair) ✓. Test 3b (MULTI-SIGNAL + DE-DUP) — stale + a byte-identical "Config stale" parse stamp + a commit stamp → chip "⚠ 2 issues" (NOT 3 — the de-dup collapsed stale+parse into one line), expand = exactly [Stale config, Commit failed] ✓. Empty-state confirmed: restore cleared all → chip GONE. The VISUAL render that CC could not confirm headlessly is now owner-confirmed live. **DESIGN DECISIONS (record):** commit-failure field = a PAIR not a triple (no category); chip RED-on-failure/AMBER-on-stale; de-dup keys on byte-identity; stale-config stays NEUTRAL in the parse modal (reason on the card, not duplicated); F3 uses a name-only freshness fallback; NO per-signal action buttons (show + expand only); NO shared failed-list component (two call sites, different data sources). **§9:** no new findings. **§3:** the F-arc records live in `boq-upload-plan.md` + the CLAUDE.md files (each slice committed its own docs); a consolidated "Phase 3 — the needs-attention visibility arc (F1–F4)" + the Phase-3-complete close-note are owed at the next §3 fold (with the still-owed v5.52/v5.53 records), deferred to the as-built doc. **PK housekeeping (agreement #17): upload v5.58 (delete v5.57); upload Failure-Mode Register v1.4 (delete v1.3); upload Wizard Design v1.27 (delete v1.26); upload archive v1.21. The in-repo `boq-upload-plan.md` + BOTH CLAUDE.md files (root from F1; frontend from F2 + F3/F4) now carry the F-arc docs commits — re-upload all three.** **DASHBOARD ROTATION THIS CYCLE:** v5.58 gets its own Trigger-state dashboard (below); the 3-recent window advances to v5.58/v5.57/v5.54 (v5.55 + v5.56 were same-day docs-only micro-cycles riding v5.54's dashboard — they carry no dashboard of their own); **v5.51's CHANGES + Trigger-state block rotates OUT to the archive (→ archive v1.21).** **OWNER ACTION OWED: Abhishek merges `feature/boq-phase-3-new` → develop TOMORROW = the genuine Phase-3 completion** (the F-arc is pushed; nothing else blocks it). **MANDATORY FIRST ACTION at next resume (agreement #41):** Checklist C audit against the Trigger state v5.58 dashboard below. **NEXT (owner-confirmed):** Phase 3 CLOSES on Abhishek's merge → **PHASE 4 (AI auto-mapping)** — design conversation first, then recon-gated slices (NEW branch per convention). The AS-BUILT doc remains DEFERRED to Phase 5. Recon-first applies to all build work.) > **[NOTE — v5.58 is a CODE cycle]** The v5.58 CHANGES above records the "needs attention" visibility arc (F1–F4) BUILT + MANUALLY VERIFIED (live on real data) + PUSHED. Repo tip moved `88c9f75b` → **`bfd0d4e8`** (origin in sync). This arc, together with the v5.57 robustness bundle, is the Phase-3 CLOSE; Abhishek's develop merge tomorrow completes Phase 3. The visibility arc was RECLASSIFIED from "pre-Phase-4 prep" to "Phase-3 close" (owner decision). The new Trigger state v5.58 dashboard below is CURRENT (supersedes v5.54). **Last updated:** 2026-06-18 IST (v5.57 — **THE PRE-PHASE-4 ROBUSTNESS BUNDLE IS BUILT, TESTED (automated + LIVE on real data), AND PUSHED. All 3 FIX-before-Phase-4 items shipped.** A real code cycle (6 commits: 3 slices × feat+docs), recon-first each, on `feature/boq-phase-3-new`. **Repo tip now `88c9f75b`** (was `73bca969`/`a0feda2a`); `origin/feature/boq-phase-3-new` IN SYNC at `88c9f75b` (pushed this cycle — the branch was NEW on origin, so `* [new branch]`). **Abhishek OWES the develop merge** (`feature/boq-phase-3-new` → develop, same as PR #1008 did for the prior stack). **WHAT LANDED (the locked plan executed in order: Step-1 recon → 1a → 1b → Slice 2, each recon-first):** **(1) Slice 1a — parse reason-bundle (reactive #166)** feat `e4b1fefc` + docs `03ed57ef`. Persists the per-sheet parse-failure REASON to three NEW additive `BoQ Sheet Draft` fields (`parse_failure_category` Select [Config stale / Parser error / Insert error] / `parse_failure_reason` Small Text / `parse_failure_at` Datetime). Three IN-SCOPE failure cases (sheets that were eligible + attempted + went wrong): STALE config drop (via NEW fields-only `_record_parse_failure`, NEVER touches `wizard_status`), Parser error, Insert error (the latter two fold into the existing `"Parse failed"` `_set_draft_status(extra_fields=...)`). Cleared on successful parse. Skip/Hidden/Ineligible/general-specs/empty-success are NOT failures → no reason (correctly never flagged). `frappe.log_error` handle appended when available (`_reason_with_ref`), never fabricated. PURELY ADDITIVE: status + rows untouched; done-event payload BYTE-FROZEN. `test_parse_run` 86→93. **(2) Slice 1b — config staleness detection (proactive #166)** feat `32e31a2a` + docs `e4bcc63d`. **VERDICT = validate-on-read (recon approach B), ZERO SCHEMA** — NO version-stamp/fingerprint/migration: a field-name fingerprint MISSES the dominant real staleness (ColumnRole Literal token renames + `_AREA_COMPATIBLE_ROLES`/validator changes, which is what actually caused #166); a deep fingerprint can never capture imperative validator logic. So 1b runs the SAME `SheetConfig.model_validate` the parser runs, via a NEW SHARED `_validate_sheet_blob(blob, sheet_name)` helper (the single source of truth, injects `sheet_name` — the #1 cry-wolf trap — and is shared with `assemble_mapping_config` Rule 3 so reactive 1a + proactive 1b can NEVER diverge). NEW read-only `get_stale_sheets(boq_name)` endpoint (routing-parity with assemble; UNCONFIGURED ≠ stale; pure read, writes nothing). `test_parse_run` 93→102; 1a regression green (refactor behavior-preserving). **#166 NOW FULLY SOLVED (reactive 1a + proactive 1b).** **(3) Slice 2 — commit round-trip reconciliation (output-fidelity)** feat `01c7dbaf` + docs `88c9f75b`. An AUTOMATIC, HEADLESS, every-commit check at the tail of each per-sheet write, BEFORE the per-sheet `frappe.db.commit()`; divergence `frappe.throw`s → existing Slice-5 per-sheet isolation → `failed[]` (NO new plumbing, isolation boundary UNCHANGED). **LOCKED SCOPE = OUTPUT-FIDELITY, not logic-correctness:** verifies ONLY that the value each transform PRODUCED was COPIED to the DB faithfully; does NOT judge correctness (valid parent / cycles / sane rates / level-monotonicity = VALIDITY = tendering = OUT OF SCOPE — NO coherence checks built). ONE check type, two anchors: COPIED fields → `stored == transform(review-row source)` (reusing the in-hand `node_rows`); DERIVED (EXACTLY TWO: `parent_node`, `level`) → `stored == CAPTURED producer output` (`name_by_idx.get(eff_parent_by_idx.get(idx))` and `docs_by_idx[idx].level`), NEVER re-running the producer (the self-confirming trap). Numerics rounded via `frappe.get_precision` (money 2dp / qty 3dp). Suppress-set encoded exactly (blank-qty=0, placeholder desc, flat-stays-flat, gen-specs 0 nodes, minted id + versioning + path + boq EXCLUDED, note→Other money-null, −1 sentinel, bool coercion). `test_commit_pipeline` 33→44 (+11: 6 must-fire + 5 must-not-fire). **CLOSED DECISION: user-override-on-divergence REJECTED** (a capture divergence is a bug not a judgment call; an override would confirm corruption; auto-discard-and-report is the only behavior consistent with P1 — full reasoning in Register v1.3). **TEST COUNTS (bench-verified in-session):** `test_parse_run` **102**, `test_commit_pipeline` **44**, parser **597** unchanged. **LIVE TESTING — COMPREHENSIVE, on real `BOQ-26-00145` (cold-start ritual; capture-and-restore on the cert workbook):** **1a** — staged a genuinely-stale config on the `Lights` sheet (one role token → invalid), ran `assemble_mapping_config`; recorded `parse_failure_category="Config stale"` + a DIAGNOSTIC reason NAMING the exact bad token + `parse_failure_at`; `wizard_status` stayed `Parsed`, 15 review rows untouched. **1b** — `get_stale_sheets` flagged ONLY `Lights`; the four other data sheets (Electrical/HVAC/Fire Fitting/PA) NOT flagged (no cry-wolf); 1b reason BYTE-IDENTICAL to 1a's stored reason (shared-helper guarantee proven live). `Lights` then RESTORED to baseline exactly (config + cleared failure fields; re-verified clean, no longer flagged). **Slice 2** — a real `commit_boq(["HVAC "])` on the Finalized HVAC sheet: committed as **v4** (114 nodes, 996 grid rows, prior v3 frozen via freeze-and-supersede), `failed[]` EMPTY → the reconciliation ran live in the commit path and passed clean on real faithful data. (HVAC left at v4 — owner decision; v3 preserved frozen, non-destructive.) Day-N census earlier showed 0 divergences across 625 committed nodes. **DEFERRED (unchanged, still tracked in §Deferred Actions):** DA-1 per-sheet parse isolation, DA-2 concurrency (R-series), DA-3 tendering integrity checks. The Slice-2 N1/N2 findings stay OUT OF SCOPE (validity = tendering). **§9:** no new findings. **§3:** the bundle's three sub-phase records are captured in `boq-upload-plan.md` (each slice committed its own plan-doc + root CLAUDE.md docs commit); the still-owed v5.52/v5.53 §3 records remain deferred to the as-built doc. **PK housekeeping (agreement #17): upload v5.57 (delete v5.56); upload Failure-Mode Register v1.3 (delete v1.2). The in-repo `boq-upload-plan.md` + root `CLAUDE.md` now carry the 1a/1b/Slice-2 docs commits — re-upload both (frontend `CLAUDE.md` UNCHANGED, no frontend this bundle).** **NO dashboard rotation** (same-day; 3-recent window stays v5.54/v5.53/v5.52; Trigger state v5.54 remains CURRENT — annotated below). **OWNER ACTION STILL OWED: tell Abhishek to merge `feature/boq-phase-3-new` → develop.** **MANDATORY FIRST ACTION at next resume (agreement #41):** Checklist C audit against the Trigger state v5.54 dashboard. **NEXT (owner-confirmed):** the pre-Phase-4 bundle is DONE → **PHASE 4 (AI auto-mapping)** is now unblocked — design conversation first, then recon-gated slices (NEW branch per convention). The AS-BUILT doc remains DEFERRED to Phase 5. Recon-first applies to all build work.) ---
 **Last updated:** 2026-06-18 IST (v5.54 — **PHASE 3 (THE WIZARD) — COMPLETE. The commit arc is closed: Slice 6 (the formalized dogfood) is DROPPED by owner decision, not built. E6 is MET. This is a DOCS-ONLY phase-close cycle — no code, no commits; the repo is unchanged from v5.53 (`feature/boq-phase-3-new` @ `a0feda2a`).** The decisive deliverable — "parsed BoQ data commits to the DB, proven end-to-end with a user-facing flow" — is DONE: the pipeline (v5.52, dogfood-proven on real BoQs 145/150/166), the user-facing commit UI + per-sheet partial-failure reporting (v5.53), all live-certified and pushed. **WHY SLICE 6 IS DROPPED (record the reasoning so it is not re-raised as owed):** Slice 6 was scoped as "the formalized, repeatable dogfood as the E6 exit proof." A read-only recon established two decisive facts: (1) the dogfood PROOF is already banked (v5.52 ran it on real BoQs 145/150/166, caught + fixed two real bugs synthetic tests missed, re-verified clean + owner eyeball) — so Slice 6 adds ZERO new test COVERAGE; and (2) NOTHING of the v5.52 dogfood apparatus is committed on disk (it was transient in-container scripting; only an untracked sample CSV remains) — so Slice 6 would be BUILD-FROM-SCRATCH of a regression harness, not "package what exists." On those facts the value collapses: a hermetic seeded harness is near-circular with the existing 33 `test_commit_pipeline` unit tests (marginal); the genuinely valuable real-corpus deep check CANNOT be a committed CI test (non-hermetic, needs a clean corpus, run by hand) — so it is a MANUAL procedure runnable on demand, and that procedure is DOCUMENTED in `BoQ_Dogfood_Test_List_v1.md` (the method survives without committed code). **DECISION (owner): drop Slice 6; do NOT build it.** The deep real-data check can be re-run by hand from the documented method if/when needed (the natural moment = before Phase 7 procurement linkages, the first consumer that can break on a committed-data change). E6 was never gated on Slice 6 — the proof was banked at v5.52. **PHASE 3 IS THEREFORE COMPLETE** (the wizard: upload → parse → review/edit → commit, proven end-to-end). **PHASE-NUMBERING RECONCILIATION (IMPORTANT — this corrects a drift that actively misled; the canonical scheme is the original Phase 0–7 roadmap):** the handover's working headers have called the recent arcs "Phase 4" (the structural-audit schema rebuild) and "Phase 5" (the commit arc). **Those are NOT the original-roadmap Phase 4/5.** Per the canonical Phase 0–7 roadmap (`boq_roadmap_detailed`): **Phase 0** Foundation ✅, **Phase 1** Data model (now three-tier + committed grid layer) ✅, **Phase 2** Parser engine ✅, **Phase 3** the Wizard ✅ (THIS — and the entire "commit arc" we have been calling "Phase 5" lives INSIDE Phase 3 as its commit sub-arc; the structural-audit schema rebuild we called "Phase 4" was the Phase-1 data-model reshape that fed it), **Phase 4** = AI auto-mapping (NEXT — pre-fill column roles + row classifications so the human just verifies; absorbs the 6 deferred Cycle-3 walkthrough findings + the parked fuzzy-logic items [Triage Register Group E / §9 #99]), **Phase 5** = post-commit edit UI with audit trail via Nirmaan Versions (distinct from both the wizard's review-screen edit loop AND the commit arc), **Phase 6** = read views / search / export (BoQ-list tab already pulled forward), **Phase 7** = procurement linkages (5 sub-phases). **Going forward this handover uses the canonical Phase 0–7 numbering.** The "Phase 5 commit arc" label in the v5.51/v5.52/v5.53 CHANGES blocks below is retained as historical text but should be READ as "the Phase-3 wizard commit sub-arc." **§9 NEW/CLOSED/UPDATED:** none (docs-only). **Test counts:** UNCHANGED from v5.53 (`test_commit_gate` 18, `test_commit_pipeline` 33, `test_review_screen` 158, parser 597 — no code touched this cycle; BENCH-VERIFY the derived wizard total before quoting). **FAS / Fire Fitting corpus drift — DROPPED as an item (owner):** the recon found FAS + Fire Fitting committed pre-X (semantically incomplete vs the post-X node tree), flagged at v5.52 as "re-commit owed-but-separate." Owner decision: this is THROWAWAY TEST CORPUS, not real client data — NO re-commit needed now; re-commit ad-hoc only if a later test needs clean data. Struck from the carried items entirely (not parked). **§3:** the two owed sub-phase records (the v5.52 commit-pipeline record + the v5.53 commit-UI record) remain owed at the next §3 fold; ADD a "Phase 3 — wizard COMPLETE (commit arc closed, Slice 6 dropped)" close-note when they land. **Per agreement #45:** v5.54 carries its own CHANGES (this header) + Trigger state v5.54 + EOS inline; v5.53 + v5.52 remain recent prior cycles; **v5.50's CHANGES + Trigger-state block rotates OUT to the archive this cycle (→ archive v1.20; 3-recent window now v5.54/v5.53/v5.52).** **PK housekeeping (agreement #17): upload v5.54, delete v5.53; upload `BoQ_Phase5_Commit_Arc_Plan_v1_5.md` (the arc-plan CLOSE — replaces v1.4, keep only v1.5); upload archive v1.20; re-upload the roadmap (`boq_roadmap_detailed`, refreshed: Phase 3 → Done, Slice 6 struck, Phase 4 AI → next). The in-repo `boq-upload-plan.md` + root/frontend `CLAUDE.md` are UNCHANGED since v5.53 (no code this cycle) — re-upload only if PK's copies predate `a0feda2a`. Wizard Design v1.25 UNCHANGED (Slice 6 was a test-formalization, not a design surface — its dropping changes no design statement). PENDING (after Phase 3 close, owner): remove `BoQ_Wizard_Design_Spec_v1_0.md` from PK (superseded).** **OWNER ACTION STILL OWED:** none. **MANDATORY FIRST ACTION at next resume (agreement #41):** Checklist C audit against the Trigger state v5.54 dashboard below (expected PASS — Phase 3 closed clean). **NEXT (owner-confirmed sequence): (1) the DEFERRED-ITEMS TRIAGE REGISTER sitting** (the dedicated one-by-one FIX-NOW/DEFER/RETIRE pass, owed since v5.47, now first up — it doubles as the first scoping of Phase 4's AI backlog, since Triage Group E = the parked fuzzy-logic items the AI phase absorbs); **(2) PHASE 4 — AI AUTO-MAPPING on the review/config screen** (the original-roadmap AI phase: pre-fill column roles + row classifications, the human verifies; + the 6 deferred Cycle-3 walkthrough findings; design conversation first, then recon-gated slices — same opening move Phase 3's commit arc had); **(3) onward per the roadmap** — Phase 5 (post-commit edit UI), Phase 6 (read/export), Phase 7 (procurement linkages). Branch/integration: keep building + pushing to origin on `feature/boq-phase-3-new`; Abhishek handles the merge into live (as he did the rebase). Recon-first still applies to all Phase-4 build work.) --- > **[NOTE — historical, v5.53 fold]** The v5.53 top-matter begins at the v5.53 "Last updated" line below. The v5.54 CHANGES above is a docs-only phase-close cycle (Slice 6 dropped, Phase 3 marked complete, numbering reconciled) — no code changed; the repo state described in the v5.53 block below (`a0feda2a`) is still current. **Last updated:** 2026-06-18 IST (v5.53 — **PHASE 5 — THE COMMIT ARC: THE USER-FACING COMMIT UI + PER-SHEET PARTIAL-FAILURE REPORTING ARE BUILT, LIVE-CERTIFIED, AND PUSHED. A user can now drive the commit pipeline from the hub — pick eligible sheets, see committed badges + timestamps, re-commit with a naming warning, and get an explicit results modal enumerating committed + failed sheets. Only the formalized dogfood slice (Slice 6) remains.** A build+certify span on the rebased branch **`feature/boq-phase-3-new`** (see BRANCH RENAME below), every slice recon-gated, the 8-commit Slice-4/5 stack pushed `964e14d0..a0feda2a` (origin IN SYNC at `a0feda2a`, ahead 0). **BRANCH RENAME (record so the lineage shift does not read as loss):** Abhishek rebased the entire Phase-5 stack onto the integrated develop/release train; the work now lives on **`feature/boq-phase-3-new`** (was `feature/boq-phase-3`). The Phase-5 commit hashes through X changed (identical messages, NEW SHAs) — the old `3f5d2932` tip is now `cc72f9e2` on the new base; the pipeline commit detail in the v5.52 block below still describes the same work, only the SHAs moved. **WHAT LANDED (the user-facing commit surface + the reporting contract):** **Slice 4a — `get_committed_state` read endpoint** (feat `964e14d0` + docs `4545161e`, BACKEND): a read-only `@frappe.whitelist()` `get_committed_state(boq_name)` in `commit_gate.py` returning `{"committed_state":[{sheet_name, committed_at, commit_version}]}` per sheet from the `BoQ Committed Sheet Grid` `is_current=1` rows (the authoritative `committed_at` source — written for both dispositions, one current row per sheet by the freeze-and-supersede invariant); `sheet_name` VERBATIM (#152); same missing/unknown-BoQ guard as `get_committable_sheets`; PURE read (no dedup — the one-current invariant is pipeline-enforced). The gate returns ELIGIBILITY; this returns committed-STATE; the modal needs BOTH. `test_commit_gate` **13→18**. **Slice 4b — the hub commit UI** (feat `53645ab7` + docs `0c152d2c`, FRONTEND): four pieces wired onto the proven engine + the 4a read — (1) a global **"Commit"** button (4th footer sibling, disabled when nothing is commit-eligible, from the gate NOT committed-state); (2) **`CommitDialog.tsx`** (NEW) mirroring `ExportWorkbookDialog` (checklist) + `ParseRunDialog` (two-step), opens with NOTHING ticked (deliberate selection), each row showing its last-committed date/time; (3) the per-card **"Committed" badge + date/HH:MM** on `SheetCard`, shown ALONGSIDE the status pill (DUAL MARKERS), for finalized AND general-specs alike; (4) a **"Committed: N"** hub footer tally. Timestamps via the wizard's `slice(0,16)` pattern. **THE RE-COMMIT CONFIRM MOVED HERE (from the planned Slice 5):** if the ticked subset includes an already-committed sheet, a B-lite step-2 warning NAMES those sheets + their last-committed date/time, states the prior version is frozen (not lost), Go-back / Commit-anyway — so re-commit is guarded from the first user-facing commit. LIVE-CERTIFIED on `BOQ-26-00145` (LC1–LC8, incl. a real re-commit advancing the timestamp + version). **Slice 5 (backend) — `commit_boq` reports per-sheet committed + failed** (feat `09714041` + docs `d5ba4ba0`): a partial-failure recon found the engine was ALREADY per-sheet (each `_commit_one_sheet` commits before the next → earlier sheets durable) but a mid-batch failure RAISED and aborted the rest, LOSING the breakdown. The per-sheet loop now wraps each sheet in `try/except`: on failure it calls **`frappe.db.rollback()` — MANDATORY (catching the exception SUPPRESSES Frappe's request-level rollback; without the explicit rollback the failed sheet's freeze-before-write [prior `is_current`→0] + partial new writes stay pending and the NEXT sheet's `frappe.db.commit()` FLUSHES them, ORPHANING the sheet — prior frozen, new incomplete, NO `is_current=1` version)**, records `{sheet_name, reason}` in a new `failed[]` (`_commit_failure_reason` — best-effort message + safe fallback, never empty), `frappe.log_error`s the traceback, and CONTINUES. Return envelope now `{boq_name, committed, failed}` (`failed=[]` on full success). **MIXED state (some committed, some failed) is a VALID resting outcome** — NO all-or-nothing wrapper; the commit-per-sheet boundary + `_commit_one_sheet` + the three tier-writers UNCHANGED; the sheet-not-in-workbook check moved inside the per-sheet try (absent eligible sheet → `failed[]`); the upfront gate re-check stays a whole-call throw. `test_commit_pipeline` **27→33** (+6 incl. **the LOAD-BEARING orphan-prevention test T2 — a re-commit whose new write fails post-freeze; PROVEN to FAIL (`AssertionError: 0 != 1`) when the rollback line is neutered, then restored**). **Slice 5 (frontend) — the commit-results acknowledge modal** (feat `ab4a390b` + docs `a0feda2a`): **`CommitResultsModal.tsx`** (NEW), acknowledge-only, mirrors the parse-completion modal (single OK + escape). After `commit_boq` resolves, `CommitDialog` hands the `{committed, failed}` envelope up via `onCommitted(result)`; the hub stores it, fires the 4b mutates, opens the modal — a one-line summary ("Committed N sheet(s)." / "Commit failed for N sheet(s)." / "Committed N; M failed."), a committed list (sheet + `v{n}`, success styling), a failed list (sheet + reason, destructive styling), each shown only when non-empty. Closes the UX gap the owner flagged at Slice-4 cert (commit no longer ends in a silent close). HAPPY-PATH LIVE-CERTIFIED on `BOQ-26-00145` (re-commit → results modal shows the committed sheet + new version, OK dismisses, hub reflects it); the failure-enumeration path is covered by the Slice-5 backend tests (T1/T4/T5/T6) + code inspection (no real-data mutation to manufacture a live failure — owner-agreed). **TWO OWNER-CONFIRMED DESIGN PIVOTS from the original Slice-4/5 plan (record so they do not read as drift):** (a) **committed-ness is a SEPARATE marker + timestamp, NOT a `wizard_status` value** — the original Slice 4 said the card flips to a "Committed" *status*; as built it is an ORTHOGONAL badge shown ALONGSIDE the status pill (a sheet is simultaneously e.g. Finalized AND Committed), same reasoning as the `parse_in_progress` separate-field decision (overloading the status Select would corrupt the lifecycle axis); (b) **the re-commit confirm moved from Slice 5 into the Slice-4b modal**, freeing Slice 5 to become the per-sheet partial-failure reporting work. **DURABLE-DECISION CANDIDATES (memory):** committed-ness = separate marker not wizard_status; `commit_boq` returns `{committed, failed}` with orphan-safe per-sheet isolation (catch-rollback-continue) and mixed-state-valid. **§9 NEW this cycle:** none new as numbered findings. **§9 CLOSED:** none. **§9 UPDATED:** none. **Test counts (bench-verified this cycle, 2026-06-18):** `test_commit_gate` **18**, `test_commit_pipeline` **33** (`test_boq_nodes` 74, `test_review_screen` 158, parser 597 — unchanged, untouched layers; the full wizard total is DERIVED — BENCH-VERIFY before quoting). **PROCESS this cycle:** recon-first held — a Slice-4 surface recon, a post-merge re-recon after Abhishek's rebase (confirmed all Slice-4 frontend surfaces MATCH/MOVED-only, BoQ logic byte-identical), and the partial-failure recon BEFORE Slice 5 (which caught the mandatory-rollback orphan trap before a line was written; the Slice-5 backend test then proved it non-vacuously by neuter-and-fail). Chat-Claude MISS owned: was WRONG TWICE on `commit_boq`'s transaction model from memory (claimed atomic/all-or-nothing; it is per-sheet) and built a long debate on it before reading the code — the owner's correction ("commit is anyways a per-sheet action!!!") forced the recon that settled it; the lesson re-banked = read `commit_pipeline.py` before reasoning about its transaction boundaries. Owner sharp calls: the per-sheet correction; choosing the B-lite re-commit warning and pulling the confirm into Slice 4b (closing the unguarded-re-commit window); holding the line on per-sheet partial REPORTING; declining to manufacture a live failure by mutating real data. **§3:** a consolidated "Phase 5 — the commit UI + partial-failure reporting (Slices 4a/4b/5)" sub-phase record should be ADDED below the Phase-5-pipeline record at the next §3 fold (together with the still-owed v5.52 "Phase 5 — the commit arc (pipeline + dogfood + level-less + X)" §3 record). **Per agreement #45:** v5.53 carries its own CHANGES (this header) + Trigger state v5.53 + EOS inline; v5.52 + v5.51 remain recent prior cycles; **v5.49's CHANGES + Trigger-state block rotates OUT to the archive this cycle (→ archive v1.19; 3-recent window now v5.53/v5.52/v5.51).** **PK housekeeping (agreement #17): upload v5.53, delete v5.52; upload `BoQ_Phase5_Commit_Arc_Plan_v1_4.md` (replaces v1.3 — keep only v1.4); re-upload `boq-upload-plan.md` + root `CLAUDE.md` + frontend `CLAUDE.md` (current as of `a0feda2a` — all three carry tonight's Slice 4/5 commits); upload archive v1.19.** **DISCHARGED — the `append_to_notes` owner action is NO LONGER OWED (record why, so it is not re-added):** v5.52 carried "amend `BoQ_Review_Screen_Locked_Design_v1_0` to record `append_to_notes` as a DISPLAY role." That amendment was ALREADY MADE — the Review-Screen-Locked-Design doc's THIRD ADDENDUM (added at v5.51) records the APPEND-TO-NOTES DISPLAY REVERSAL and explicitly states it DISCHARGES the owner action the doc owed. v5.52 stale-carried it; it is struck here as COMPLETED (not forgotten). **PENDING PK REMOVAL (logged, NOT this cycle — owner decision):** `BoQ_Wizard_Design_Spec_v1_0.md` (the superseded 2026-05-24 up-front spec) is to be REMOVED from PK after Slice 6 lands and Phase 5 closes — keep it until then. **OWNER ACTION STILL OWED:** none carried (the `append_to_notes` action is discharged above). **MANDATORY FIRST ACTION at next resume (agreement #41):** Checklist C audit against the Trigger state v5.53 dashboard below. **NEXT (owner-confirmed):** **Slice 6** — the formalized, repeatable dogfood slice as the E6 EXIT proof (the dogfood PROOF is already banked; test list = `BoQ_Dogfood_Test_List_v1.md`). This is the ONLY remaining commit-arc slice. Recon-first still applies. F1's "new general-specs doctype" remains SATISFIED — the grid layer IS the general-specs home.) --- > **[NOTE for the editor folding this into the live handover]** Everything ABOVE this line is the new v5.53 top-matter. Below it, the v5.52 "Last updated" block becomes the most-recent-prior cycle (keep it inline). The new **Trigger state (v5.53 standing dashboard)** has been PREPENDED before the v5.52 dashboard, demoting v5.52's dashboard to "(SUPERSEDED by v5.53 above)". The v5.49 CHANGES + dashboard rotated OUT to archive v1.19 per agreement #45 (3-recent window now v5.53/v5.52/v5.51). **Last updated:** 2026-06-17 IST (v5.52 — **PHASE 5 — THE COMMIT ARC: THE PIPELINE IS BUILT, DOGFOOD-TESTED ON REAL DATA, AND PUSHED. Real reviewed BoQ data now commits to the four-tier committed schema across all tiers, correctly, on three real workbooks — the E6 dogfood proof is banked.** The decisive Phase-5 build span on `feature/boq-phase-3`, every slice recon-gated, the whole arc pushed `2b9c6d89..3f5d2932` (origin IN SYNC at `3f5d2932`, ahead 0). **WHAT LANDED (the commit pipeline, real data flowing):** **Slice 2 — the commit gate** (feat `b93ec41c` + docs `c5f72358`): `compute_committable_sheets` (pure) + `get_committable_sheets` (whitelisted); eligible = Finalized OR general-specs; general-specs is POINTER membership in `BOQs.general_specs_sheets` (NOT `wizard_status == "General specs"`); overlap → `general_specs` wins; returns `{sheet_name, disposition}`. **Slice 2.5 — committed-tier capture-only reshape** (feat `49b77635` + docs `428471c2`): removed `_compute_amounts`/`_recompute_parent_rates_from_areas`/`_apply_rate_fallback`/`_compute_child_amounts` from the write chain, dropped `read_only` on the four amount fields, `is_rate_only` no longer auto-set — the committed tier is now a TRUTHFUL CAPTURE store, all computation deferred to tendering; ALL structural invariants kept. **Slice 3a — grid foundation + doctype rename + column-config snapshot + single-open + the commit shell** (feat `82eba717` + docs `965789be`): renamed `BoQ Committed General Specs` → **`BoQ Committed Sheet Grid`** (+ row child) + a grid-only/grid+nodes discriminator; the single-open file path builds each committable sheet's FAITHFUL GRID (`{row_number, cells:{col_letter:value}}`, every row in position); per-parent-row grid versioning; a committed **`BoQ Sheet`** per sheet (incl. grid-only general-specs) + the **column-config snapshot** (`column_role_map`/`column_headers`/`area_dimensions` from the draft `sheet_config` — the linchpin for rendering + future Excel write-back); the `commit_boq(boq_name, sheet_subset)` shell with internal dispatch + per-sheet transaction + SHARED `commit_version` + server-side gate re-check. **Slice 3b — the node tree + three-tier versioning + combined_rate→warning + provenance** (feat `2a810d15` + docs `eacd932d`): the node write-body for Finalized sheets (read review rows verbatim, `resolve_effective()`, the P4-3 field map [WORD-ORDER REVERSAL + Float→Currency], BOQ Nodes + `BOQ Node Qty By Area` children against the capture-only controller); per-node `commit_version` + `is_current` + the centralized is-current accessor; the **row_index→committed-node-name two-pass parent wiring** (insert all, then wire — a node's name isn't known until insert); `attached_notes`/`is_rate_only`/`is_synthetic`/`append_notes_raw` carried; combined_rate relaxed throw→WARNING (parent + child — a capture-only tier must not reject reviewed data); provenance (review-row name + a commit-minted stable id via `frappe.generate_hash`). **THE TWO-LAYER MODEL realized:** every committable sheet → a faithful GRID (visual record, all rows); finalized sheets ALSO → a NODE TREE (semantic record). General-specs = grid-only. **THE DOGFOOD (the E6 gate) — RAN on real BoQs 145/150/166 and CAUGHT TWO REAL BUGS the seeded build-tests missed.** The dogfood = commit-and-inspect + a full per-sheet CSV ROUND-TRIP (parser-side built from review rows via `resolve_effective` vs DB-side built from the committed grid+nodes, diffed on an outer join over Excel rows, with expected transforms classified out so only real gaps surface). **BUG 1 — LEVEL-LESS PREAMBLE GUARD** (fix feat `34a71f38` + docs `b4094f5c`): `_build_node_pass1` stamped a flat `level=1` on ANY level-less preamble, which BREAKS the tree when a child is also level 1 (parent==child level — Electrical row 18 "Note:" → 1 over its level-1 children). FIX: `_real_preamble_level` + `_compute_levelless_preamble_levels` (computed once per sheet, passed into pass 1) — a level-less preamble WITH children → `max(0, min(child levels) − 1)`; CHILDLESS → the sheet's shallowest defined level; childless on a NO-levels sheet → 0. Preamble level constraint relaxed `<1`→`<0` in `boq_nodes.py`. Three live cases (Electrical row 18, low side row 191, Fire Fitting row 8 → all 0). `test_boq_nodes` 71→72, `test_commit_pipeline` 19→23. **BUG 2 / "X" — COMMIT ALL CLASSIFIED ROWS EXCEPT SPACER AS SEMANTIC NODES** (fix feat `711a792b` + docs `3f5d2932`): the node tree committed only preamble + line_item, so note/subtotal_marker rows lost their discrete classification + parent in the semantic record. X commits EVERY classified row EXCEPT spacer as a node, so the node tree is a COMPLETE SEMANTIC MIRROR of the reviewed BoQ. **NODE SHAPE = OPTION A (owner-locked):** `node_type` stays the PRICEABILITY axis (`Preamble`/`Line Item`) + a NEW neutral 3rd value **`Other`**; a NEW field **`row_class` (Data)** carries the FULL effective classification (the taxonomy axis). A future pricing walk stays EXACTLY `node_type in (Preamble, Line Item)`, unchanged + without needing the taxonomy. note/subtotal_marker/header_repeat → `Other`; **SPACER alone stays grid-only** (pure layout, no semantic content; the grid preserves its position). Q8 level-guard fix: `_compute_levelless_preamble_levels` counts ONLY priceable children (a note/subtotal child would wrongly pull a level-less preamble to 0). `test_commit_pipeline` 23→27, `test_boq_nodes` 72→74. Migrate clean. **THE CORRECTED UNDERSTANDING — NOTE TEXT WAS NEVER LOST (record so the alarm is not re-raised).** The dogfood first appeared to show notes losing their pricing context; the X recon proved this FALSE. The PARSER ALREADY rolls each descendant note's text onto its nearest-ancestor preamble's `attached_notes` (84 preamble review rows carry these; Electrical's "LT CABLES" carries all 7 of its descendant notes incl. rows 26/27), and Slice 3b carries that verbatim onto the committed preamble node — so the pricing-side upward walk always had note context. The "lost notes" alarm was a false positive from reading the RESOLVED COLUMNS, not `attached_notes`. **X therefore must NOT re-attach** (a second walk would DOUBLE the text); X commits the note as its OWN discrete classified/positioned node and leaves the parser's attachment alone. The `attached_notes` verification column added to the re-verify round-trip makes this visible going forward. **DELIBERATE STRUCTURAL-AUDIT REVERSAL (record so it is not mistaken for drift):** the original §0.A disposition (NOTE/SUBTOTAL_MARKER/HEADER_REPEAT → grid-only, "Path 2 not Path 1") was REVERSED by X on corrected evidence from the dogfood — the node tree now holds those rows (as `Other`), because the dogfood proved the semantic record was incomplete without them. This is a reasoned reversal on new evidence, NOT character/decision drift. **RE-COMMIT + RE-VERIFY CLEAN.** All five finalized sheets on 145/150/166 were re-committed under the corrected pipeline (answer-key computed by a separate read-only recon FIRST, then checked against it — recon-first separation), and a rebuilt round-trip (DB-side reading notes/subtotals from nodes; the T1 note→grid_only transform vanished; + the `attached_notes` verification column) came back CLEAN on all five — ZERO genuine value gaps, the only legitimate non-matches being spacers (grid-only), DB-only structural title/banner rows, and the accepted per-area 0.0==absent normalization. The spacer categories sum EXACTLY to the answer-key spacer counts (a strong consistency proof). LT CABLES `attached_notes` = exactly 7 on both sides, not doubled. **Owner eyeball pass (round-trip CSVs + general-specs grid CSVs vs source): CORRECT.** **C3 dogfood watch CLEARED — real data through the schema, both layers, proven.** **LIVE COMMITTED CORPUS (post-re-commit):** BOQ-26-00145 → SOW + MEP Make List (grid-only, 0 nodes), Electrical (194 nodes, v2), HVAC (114, v2), Fire Fitting (6, v2); BOQ-26-00150 → make (grid-only), low side (149, v3); BOQ-26-00166 → Assumptions & Exclusions (grid-only), VRF System (121, v3). Node totals = priceable (unchanged from pre-X) + the added note/subtotal Other nodes; zero spacer nodes. **DATA-STATE CAVEATS:** BOQ-26-00145 'FAS' is now Parsed (no longer finalized) and carries lingering OLD committed data from an earlier dogfood commit — explicitly OUT of scope for this re-commit (revisit if re-finalized); Lights/Washroom (00145) + the near-duplicate 00165 'VRF System' also out of scope (owner decision: re-commit ONLY 145/150/166's committable sets, nothing more). **§9 NEW this cycle:** none new as numbered findings (the level-less + X bugs were caught + fixed in-arc, not logged as open). **§9 CLOSED:** none. **§9 UPDATED:** none. **MEMORY:** the Phase-5-pipeline-complete state + the X/Option-A decision + the level-less rule + the corrected note-attachment understanding are durable-decision candidates. **FUTURE SCOPE (logged, not built, not blocking):** (1) **grid-padding trim** — committed grids carry trailing all-None padding rows (`_extract_grid_rows` reads to `ws.max_row` with no content-trim; e.g. `make` ~1000 rows); harmless (round-trip keys per-row, padding folds to blank) but a future trim shrinks the footprint; (2) **Excel formula-passthrough** (owner-raised this cycle) — preserve Excel FORMULAS (not just computed values) through parser → review row → DB commit so committed subtotals/computed cells carry computation logic; requires a parser-stage formula-read pass (openpyxl `data_only=False`) + cell-address-to-row translation + new fields; deferred, own arc; (3) resizable ReviewTree columns; (4) hub "Parsed" status time component (pending `parsed_at` confirm); (5) version-table growth prune. **PROCESS this cycle:** recon-first / answer-key-first held throughout and repeatedly caught real problems before mutation (the level-less full case set; the per-area shape; the separated re-commit answer key removing self-agreement risk); the dogfood on REAL data caught two bugs ALL prior (synthetic) testing missed — real fixtures are not substitutable; chat-Claude MISS owned — omitted the verbatim Checklist A from CC build prompts TWICE despite the standing memory item (corrected both times; the X prompt embedded the full A1–A25); CC also skipped the by-number self-report once even when the list WAS present (the work was fine, the report format failed — watch for needing the self-report instruction more prominent); the "lost notes" false-positive lesson — read the RIGHT field (`attached_notes`, not resolved columns), and the `attached_notes` verification column was added to the round-trip so attachment is visible/checkable going forward. **§3:** a consolidated "Phase 5 — the commit arc (pipeline + dogfood + level-less + X)" sub-phase record should be ADDED below the Phase-4 record at the next §3 fold. **Per agreement #45:** v5.52 carries its own CHANGES (this header) + Trigger state v5.52 + EOS inline; v5.51 + v5.50 remain recent prior cycles; **v5.49's CHANGES + Trigger-state block rotates OUT to the archive this cycle (→ archive v1.18; 3-recent window now v5.52/v5.51/v5.50).** **PK housekeeping (agreement #17): upload v5.52, delete v5.51; re-upload `boq-upload-plan.md` + root `CLAUDE.md` + frontend `CLAUDE.md` (current as of `3f5d2932` — copied to Desktop this cycle; NOTE: frontend `CLAUDE.md` was NOT touched by the Phase-5 backend slices, so it is unchanged since `9dae681d` — re-upload only if PK's copy is older); upload `BoQ_Phase5_Commit_Arc_Plan_v1_3.md` (replaces v1.2 — keep only v1.3); upload archive v1.18.** **OWNER ACTION STILL OWED (carried, NOT done this cycle):** amend `BoQ_Review_Screen_Locked_Design_v1_0` to record `append_to_notes` as a DISPLAY role (the append-to-notes-columns slice reversed the non-display design — still owed). **MANDATORY FIRST ACTION at next resume (agreement #41):** Checklist C audit against the Trigger state v5.52 dashboard below. **NEXT (owner-confirmed):** the remaining commit-arc slices — **Slice 4** (committed-side marker + hub **"Committed"** status + the commit picker UI — the user-facing entry point; today the pipeline is script-driven), **Slice 5** (the re-commit confirm flow — the freeze-exercising live cert), **Slice 6** (the formalized dogfood slice as the repeatable E6 exit proof — the PROOF is already banked this cycle). Recon-first still applies. Note: F1's "new general-specs doctype" is SATISFIED — the grid layer IS the general-specs home; no separate doctype needed.) --- > **[NOTE — historical, v5.52 fold]** Everything between the v5.52 "Last updated" line and this note was the v5.52 top-matter. Below it, the v5.51 "Last updated" block becomes the most-recent-prior cycle (keep it inline). Then update the **Tip commit / Lineage / Origin status / test-count** block (next section) and PREPEND the new **Trigger state (v5.52 standing dashboard)** before the v5.51 dashboard, demoting v5.51's dashboard to "(SUPERSEDED by v5.52 above)". Rotate the v5.49 CHANGES + dashboard OUT to archive v1.18 per agreement #45. --- ## Tip / lineage / origin / tests block (v5.91 — the rate-master subsystem; REPLACES the v5.90 version below) **Tip commit:** `a5151ec5` (docs, RM-4b) — **PUSHED** (`21086546..f101df58` then `f101df58..a5151ec5`); lane 48 ahead of its cut `6da6b207`; origin/develop UNMOVED since the merge train (rev-list 0/48 verified at both pushes). **Slice lineage this cycle (all pairs feat+docs unless noted):** RM-1 -> RM-2 -> RM-2b (data-only benchmark re-import + the loader-constant rename) -> RM-3 -> RM-3a -> RM-3b -> RM-3c -> RM-4a -> RM-4a-filter (owner-run ad-hoc) -> RM-4b. **MIGRATE:** four new doctypes this cycle (Rate Master Item, Rate Category Config, Suggestion Event, Suggestion Run) — all migrated locally; **the Abhishek heads-up covers the gate-arc columns + these four, OWED.** **TESTS (report-sourced figures — BENCH-VERIFY IN-SESSION BEFORE QUOTING, per the facts rule):** backend rate-master module 38 OK; test_pricing 230 OK; test_classify last verified at ST-1 (77) — STALE for the current tree; vitest last full figures from the RM-4 reports — re-baseline at the next test-running prompt. **The FIVE STANDING GOLDENS** (also stored as config data since RM-4b): COPPER/UNARMOURED/1C/6 -> 120/20 + term 80/20 + BCS 87; COPPER/ARMOURED/3C/2.5 -> 200/28 + 70/20 + 150; ALUMINIUM/ARMOURED/4C/16 -> 210/44 + 130/40 + 160; COPPER/ARMOURED/3C/50 -> term 940/240; COPPER/UNARMOURED/3C/10 -> 630/40 + BCS 469. **AI:** toggle ON (Anthropic / claude-opus-4-8); extraction mirrors the classification voter; live runs banked on BOQ-26-00106 (run BRSR-26-00007 active). **Audit residue (intended):** the subsystem's Version docs from the RM-4 certs. ---
 The decisive answers this arc: (a) **the client CAN reproduce the server's blank count exactly** (Recon 5 — every field needed already on the row; no payload change), which is what made a live count possible at all; (b) **the cell and the server agree on blank/non-blank in every case** (Recon 6 — both run one ladder, both treat whitespace as empty), which is what made "the gate follows what the user sees" a safe rule rather than a hope; (c) **both carry paths are fully replayable and idempotent**, which is what made locking them costless rather than destructive; (d) **the AI voter runs and the toggle held** across a full operational run. The one thing the arc could NOT settle in-house is the non-admin on-screen case — no such account exists in the dev environment. ### Deferred-work budget (5-item ceiling) — C2 **STILL 5 of 5 — AT CEILING, unchanged in composition:** (a) §3 sub-phase records; (b) narrative catch-up; (c) editor-perf cleanup (the A/B toggle retirement after the stability window); (d) merge new-doctype inventory unread (compounded by the train; per the v5.81 ruling the other lanes self-document, consolidation at the final as-built doc); (e) **Set-3 trigger undefined — STILL THE LOAD-BEARING ITEM.** NOTE: the arc-close items (PK re-upload, design docs, the §6 correction, the stale comment, the Abhishek heads-up) are NOT C2 carries — they are one fold's housekeeping and discharge together tomorrow. NEW backlog items (not carries): the `freeze_classification` stamping slice; the live worker-failure test; the non-admin on-screen check; the uncited `test_pricing` SQL-traceback noise (called pre-existing without a citation at G2a — needs an entry or a look); the `rate_editable` population mode, RETAINED but with no consumer since G2e (kept because Slice 8's helpers operate on exactly that population); the AI-abstains-on-Preambles question. ### Dogfood gap (C3) PASS — and this arc was unusually well dogfooded. The owner's own corrections drove three of its biggest turns: the amber highlight fact (chat had inferred it wrong from payload shape), the "empty is empty" re-specification that widened the gate late and cost real rework, and the multi-engine correction to CC's single-discipline reading of re-classify. The gate itself has NOT yet met the team — it is pushed but unmerged, so first real contact is still ahead, and the honest expectation is set by the arc's own numbers: a good classify pass does NOT open the gate (5 auto-accepted, 3 to review on the operational run); human review does. ### Architecture integrity (C4) / Schema-ahead-of-consumer (C7) C4: **PASS, and improved.** The arc consolidated rather than sprawled: ONE blank-count helper in the service layer, ONE gate condition consumed by three write paths, ONE frontend predicate driving four surfaces, ONE qty test shared across the language boundary's server half. Two pre-existing defects of exactly the opposite kind were corrected on the way — a stale docstring asserting a guard path that did not exist, and a message family describing a superseded rule. C7: **PASS** — G2b's four columns shipped in the SAME slice as the gate that reads them and the endpoints that write them; nothing landed ahead of a consumer. The one deliberate exception is disclosed and bounded: G2a's two payload keys shipped one slice ahead of G3a's banner, to keep G3a pure-frontend. ---  ## Trigger state (v5.88 standing dashboard) — recent prior > Supersedes the v5.87 dashboard below. v5.88 = **THE MERGE TRAIN LANDED + THE SLICE-8 LANE IS CUT.** Develop (`6da6b207`, v2.11.16) now carries the entire classification programme, the pricing-module lane, Abhishek's template/revised-upload work, and Madhu's items; both solo lanes retired; `feature/boq-pricing-helper` cut from the exact post-merge tip (local-only, zero commits, 0/0). The stale-test guard fix `6b6d1577` is on develop (not test-verified). AI toggle ON; the HV-11 attribution tracker armed, zero flips logged. **OWNER DECISIONS THIS FOLD:** the new arc = Slice 8 rate-guidance (order 2 -> 1 -> 3 stands; all helpers DISPLAY-ONLY); Slice 8 re-sequenced AHEAD of the eval cockpit arc (decision by action — branch cut + go; chat surfaced the fork; the cockpit's charter stands, deferred). **SLICE-8 HONEST SCOPE:** helper 2's entry stage (classification) is BUILT AND LIVE; the unbuilt remainder = attribute extraction -> base-price lookup (the pricing workbooks) -> the display-only rate suggestion in the editor. **NEXT ACTION (strict order): (1) THE OPS SYNC if not yet run — `bench migrate` (model sync for the merged lanes) + stack restarts + relogin; nothing builds before it. (2) The LOWSIDE supersession run (cheap ride-along once migrated — 121 rule-only rows supersede on a real classify). (3) THE SLICE-8 SCOPING SESSION (helper 2 remainder; sets the arc's exit criterion [C1] + time-box [C6], both currently undefined) -> recon (#56) -> build. (4) Production-era carries unchanged: Set-3 accrual live; Set-3 trigger UNDEFINED (C2 (e), load-bearing); demotion re-derivation (Ducting first); the electrical debt register's ONE round; VRF WORKS labels (4th ask); ladder v2 telemetry-gated; the eval cockpit (deferred behind Slice 8); ELV.** Checklist C owed fresh at next resume against this dashboard. ### Deferred-work budget (5-item ceiling) — C2Unchanged in count, at ceiling: (a) §3 sub-phase records; (b) narrative catch-up; (c) editor-perf cleanup (the A/B toggle retirement after the stability window); (d) merge new-doctype inventory unread — NOW COMPOUNDED by the train (Abhishek's + Madhu's merged doctypes also unread; per the v5.81 owner ruling they self-document, consolidation at the final as-built doc); (e) **Set-3 trigger undefined — STILL THE LOAD-BEARING ITEM** (the debt register, the Ducting call, demotion cadence, ladder v2, and the LOWSIDE supersession check all ride it). ### Dogfood gap (C3)PASS — production is the dogfood (HVAC live since HV-9; Set-3 accruing). The next contact: the team classifying real multi-trade BoQs on the merged develop, and — once Slice 8 ships its first helper — the first rate suggestion a pricer actually sees. ### Architecture integrity (C4) / Schema-ahead-of-consumer (C7)C4: PASS — no architecture doubts; the N-generic editor + resolved-read/legacy-read split survive the merge untouched (recon showed no conflicts on our lanes' territory — the train merged clean). C7: PASS — no schema this fold; the Slice-8 scoping must keep it that way (no rate-suggestion schema before its display consumer is designed). --- ## Trigger state (v5.87 standing dashboard) — ROTATED OUT to archive v1.48 (2026-07-29) > Full text now lives in `BOQ_Handover_archive.md` v1.48 (the v5.91 fold rotation). One-line summary: the post-merge sync + Slice-8 design fold — the merge train landed (tip `6da6b207`), the pricing-helper lane cut, the rate-helper UX locked via the owner-reviewed mockup, UI-first D7 with earthing-U2 (later superseded by the RM reshape). ---
> Supersedes the v5.52/v5.51 dashboards below. v5.53 = **PHASE 5 COMMIT ARC — THE USER-FACING COMMIT UI + PER-SHEET PARTIAL-FAILURE REPORTING ARE BUILT, LIVE-CERTIFIED, AND PUSHED.** Slices 4a (the `get_committed_state` read endpoint), 4b (the hub Commit button + `CommitDialog` + the per-card "Committed" badge/timestamp + the "Committed: N" tally + the B-lite re-commit warning moved here from the planned Slice 5), and 5 (`commit_boq` → `{committed, failed}` per-sheet catch-rollback-continue + the `CommitResultsModal`) landed on the rebased branch `feature/boq-phase-3-new` and pushed (origin in sync at `a0feda2a`). Two owner-confirmed pivots: committed-ness is a SEPARATE marker not a `wizard_status`; the re-commit confirm moved into 4b, freeing Slice 5 for partial-failure reporting. This was a backend + frontend span; the Checklist C audit is OWED FRESH at the next resume against THIS dashboard. ### Phase-level exit criterion (agreement #36) **E6 (active):** Wizard v1 ships (upload→parse→review→commit proven, parsed BoQ data committed to the DB). Module 1 ✅ + Module 2 Hub ✅ + Module 3 spoke ✅ + the parse-run surface ✅ + the review-screen backend ✅ + the read surface ✅ + the value-edit loop ✅ + the structural-edit loop ✅ + the ergonomics/tail layer ✅ + Slice D ✅ + Group A ✅ + the amount field-set arc ✅ + append-to-notes-as-columns ✅ + the STRUCTURAL AUDIT Phases 0/1/2 ✅ + STRUCTURAL AUDIT PHASE 4 ✅ + the PHASE-5 COMMIT PIPELINE ✅ (dogfood-proven, v5.52) + **the USER-FACING COMMIT UI ✅ (Slices 4a/4b) + per-sheet PARTIAL-FAILURE REPORTING ✅ (Slice 5) — built, live-certified, pushed (this cycle).** **The decisive E6 deliverable — "parsed BoQ data commits to the DB" — is MET in substance AND now has its user-facing entry point:** a user drives the commit from the hub, sees committed badges + timestamps, re-commits with a naming warning, and gets an explicit committed/failed results modal. **Remaining for the FULL commit arc:** ONLY **Slice 6** (the formalized/repeatable dogfood as the E6 exit proof; the proof itself is already banked). E6 is met in spirit and very nearly in full. ### Decisive-path progress Strong — the hardest part (the first real-data write into the committed schema) was proven at v5.52; this cycle put the user-facing surface on top of it and made the commit action report honestly. The commit UI is wired against the proven pipeline (lower-risk wiring, not first-data-write risk); the one genuinely subtle piece — making `commit_boq` report per-sheet outcomes — was handled correctly: the partial-failure recon caught that catching a per-sheet exception suppresses Frappe's request-level rollback (an orphan trap on re-commit), the mandatory `frappe.db.rollback()` was added, and a test PROVES it non-vacuously (neuter the rollback → the test fails with the exact orphan). What remains (Slice 6) is the formalized, repeatable dogfood — a certification pass, not new build risk. ### Deferred-work budget (5-item ceiling, agreement #36 / Caveat H) — C2 The DEFERRED-ITEMS TRIAGE REGISTER (section below) remains the exhaustive INVENTORY; **C2 counts only the curated carries**, which after this cycle are: (a) **Slice 6** — the formalized/repeatable dogfood as the E6 exit proof (the ONLY remaining commit-arc slice; the proof is already banked); (b) the Wizard-Design fold (the Phase-5 pipeline + two-layer model + level-less + X from v5.52, AND now the commit-UI surface + the `{committed, failed}` contract from this cycle — folded into Wizard Design v1.25 THIS docs cycle); (c) the re-parse completion modal (cosmetic, parked, recon-done — D5). **DISCHARGED this cycle:** ~~the user-facing commit UI~~ (Slices 4a/4b, built + certified + pushed); ~~the re-commit confirm flow~~ (built INTO Slice 4b); ~~the per-sheet partial-failure reporting~~ (Slice 5, built + certified + pushed); ~~the `append_to_notes` owner action~~ (was already discharged at v5.51's third addendum — struck as completed). **FUTURE-SCOPE items (logged, NOT C2 carries, not blocking):** the grid-padding trim; Excel formula-passthrough (own arc); resizable ReviewTree columns; the hub "Parsed" time component; version-table prune; "view prior committed versions" picker (data supports it); a "copy-forward tendering edits on re-commit" feature; Excel write-back (tendering build); the PENDING removal of `BoQ_Wizard_Design_Spec_v1_0.md` from PK (after Slice 6 / Phase-5 close). UNDER the 5-item ceiling. **C2 review owed at next resume** (expected disposition = PASS — the carries are one remaining slice + a doc fold + a cosmetic park, not feature debt). ### Dogfood gap (C3) **CLEARED (banked at v5.52, unchanged this cycle).** Real BoQ data commits to the four-tier schema, verified two ways (the every-row CSV round-trip + the owner's eyeball pass against source) on 145/150/166. This cycle's Slice-5 partial-failure work is backed by unit tests (incl. the proven orphan-prevention T2); the commit-UI happy path was live-certified on `BOQ-26-00145`. Slice 6 will formalize the dogfood into a repeatable exit proof. ### Architecture integrity (C4) / Schema-ahead-of-consumer (C7) PASS. C4: this cycle's slices were recon-gated (a Slice-4 surface recon, a post-merge re-recon after the rebase, the partial-failure recon before Slice 5); the committed-state marker reuses the existing committed-tier `committed_at` rather than inventing a status; `commit_boq`'s reporting change left the commit-per-sheet transaction boundary + the three tier-writers untouched, adding only the loop wrapper + reason helper; the mandatory rollback is test-proven non-vacuous. The two design pivots (marker-not-status; re-commit-confirm-into-4b) were deliberate and are recorded so they don't read as drift. C7: the BOQ-Nodes projection boundary remains RESOLVED (v5.52); nothing this cycle reopened it (4a/4b/5 are read + UI + reporting, no schema change to the committed tiers). The mixed-state partial-commit outcome is an explicit, owner-confirmed semantic (a per-sheet committed record is independently valid). ### Checklist C audit (agreement #41) — OWED AT NEXT RESUME Build+certify span. Expected shape at the next resume: C1 PASS (E6 met in substance; only Slice 6 + the wider review→commit polish remain); C2 PASS (under ceiling — one slice + a doc fold + a cosmetic park); C3 PASS (banked); C4 PASS; C5 PASS (the commit UI certified on real `BOQ-26-00145`); C6 no active time-box (Slice 4 ran ~1 session, Slice 5 ~1 session — both inside box; a fresh box for Slice 6 is a candidate); C7 PASS (the projection boundary resolved; no schema touch this cycle). ### Time-box / override status No active time-box (a fresh box for Slice 6 is a candidate at the next resume). C2 under ceiling — no override needed. Owner-confirmed next: Slice 6 (the formalized dogfood). Per C6 observed this cycle: Slice 4 and Slice 5 each ran ~1 session; Slice 5 grew an extra frontend piece (the results modal) from the owner's post-Slice-4-cert UX catch. ## Trigger state (v5.52 standing dashboard) (SUPERSEDED by v5.53 above) > Supersedes the v5.51/v5.50 dashboards below. v5.52 = **PHASE 5 COMMIT ARC — THE PIPELINE IS BUILT, DOGFOOD-TESTED ON REAL DATA, AND PUSHED.** The commit pipeline (Slices 2/2.5/3a/3b) writes reviewed BoQ data into the two-layer committed schema (faithful GRID for every row + a NODE TREE for finalized sheets); the dogfood on real BoQs 145/150/166 caught and fixed two bugs (the level-less preamble guard + "X" = commit all classified rows except spacer as nodes); a re-commit + round-trip re-verify came back clean and the owner's eyeball pass confirmed correct; the whole arc is pushed (origin in sync at `3f5d2932`). This was a backend build + DATA span; the Checklist C audit is OWED FRESH at the next resume against THIS dashboard. ### Phase-level exit criterion (agreement #36)
| D3 | Visual row-diff after re-parse (§9 #146). | §9 #146 | ❌ RETIRE (2026-06-18 — re-parse correctness is covered by the review screen + the D2/#166 verification; row-level parse diffs are noisy/low-value. INSIGHT RELOCATED: the diff concept has a real future home in Phase 5 committed-version diffing [v{n} vs v{n+1}], where the stakes are real. §9 #146 cross-ref'd.) || D4 | Merged-header preview/config off-by-one. | parked | ⏸️ DEFER — await NEW-USER FEEDBACK (2026-06-18 — owner judges it does NOT impact workflow + is quickly internalized; wait to see if fresh users stumble. Paired with D1.) || D5 | Re-parse completion modal does not appear — owner reports the behavior is VERY INCONSISTENT/intermittent (NOT a clean footer-works/re-parse-fails split). | v5.48 session | ➡️ FOLD INTO PHASE 4 (2026-06-18 — verify + fix while on BoqHubPage during Phase-4 work. The "inconsistent" signature points to a RACE, not a missing handler — RE-OPEN the 2026-06-13 "timing-collision disproven" conclusion; capture WS-frame + console evidence.) | ### Group E — Parked to the Phase 4 AI layer (strategic decision §9 #99 — fuzzy parser logic out of deterministic scope) > **TRIAGE 2026-06-18:** all of Group E CONFIRMED AS PHASE 4 BACKLOG — to be triaged in detail at the Phase-4 (AI auto-mapping) design conversation, NOT individually now. Grouping for that conversation: **E1–E4 = AI-classification/parenting targets** (the core of Phase 4's value); **E5–E6 = model/naming cleanup riding along** (granularity unification + `rate_subkey` rename). The roadmap's **6 deferred Cycle-3 walkthrough findings** join this set as Phase 4's seed backlog. **§9 #46** (hierarchy resolver lowercase-letter cascade) is ADDED to this Phase-4 backlog (same parser-classification class as E1–E4). | # | Item | Source | Disposition ||---|---|---|---|| E1 | Bug 12 — SUB HEAD-adjacent mis-parenting. | §9 #88 | PARKED (AI layer) || E2 | Bug 14 — deferred to AI layer. | §9 #90 | PARKED (AI layer) || E3 | Bug 15 — SG HVAC priced-sub-section-header mis-classification. | §9 #102 | PARKED (AI layer) || E4 | SUB HEAD structural limitation: a numeric preamble right after a SUB HEAD lands as its peer, not child (needs stateful section-awareness = AI layer). | §9 #100 note | PARKED (AI layer) || E5 | Full amount/rate/qty granularity UNIFICATION — the v5.49 amount field-set arc fixed only the AMOUNT side (made it symmetric with rate); the deeper three-way unification of how single-vs-per-area is modelled across all three families is design debt for the Phase-4/unification layer. | v5.49 arc | PARKED (Phase-4 design debt) || E6 | `rate_subkey` NAMING DEBT — the generic descriptor/edit third-hop `rate_subkey` now also carries AMOUNT kinds (2a/2b reused the rate plumbing for amount). Works correctly; the name is misleading. Rename when the unification layer is touched. | v5.49 (2a/2b decision) | PARKED (Phase-4 naming debt) | ### Group F — Sequenced-after (locked ordering, now eligible) | # | Item | Source | Disposition ||---|---|---|---|| F1 | Failure-mode register sweep. | v5.41 overlay lock | 🔧 SLOTTED PRE-PHASE-4 (2026-06-18 — a robustness pass before AI builds on the config/parse surface; BUNDLED with D2 + §9 #166. The HOW [scope, method, output] to be designed at pickup; §23 failure-mode taxonomy is the seed. §9 #166/D2/the silent-parse-failure class are seed entries.) | ### Group G — Cosmetic / standing quirks (permanent log, no fix planned) | # | Item | Source | Disposition ||---|---|---|---|| G1 | U+FEFF literal glyph in `exportReviewCsv.ts` (the edit tooling re-normalizes the escape; functionally identical). | v5.47 | LOGGED (no fix) || G2 | exceljs deprecated TRANSITIVE deps (uuid@8, fstream, old glob) — install warnings only; exceljs itself current. | v5.47 | LOGGED (watch) || G3 | `src/utils/exportToCsv.ts`'s 8 baseline tsc errors — retired as N/A for BoQ; remains REPO-WIDE baseline debt, not BoQ's. | v5.47 retired carry | LOGGED (not ours) |
**master_preamble field ✅ COMPLETE (feat `8db5a8d8` + docs `cde8c862`) — later SUPERSEDED by Slice 2c.** Added `BOQs.master_preamble` (Long Text, read_only, in `parser_metadata_section` after `general_specs_sheet`) and a worker Step-6 that writes `parsed.master_preamble` when non-empty (falsy-skip so a re-parse with no general-specs sheet doesn't blank an existing value). `bench migrate` + `frappe.db.has_column` verified. **+1 test (test_parse_run → 45).** NOTE: this scalar existed only between `8db5a8d8` and Slice 2c `b5381c0c` — Slice 2c replaced it with the `BOQs.general_specs_sheets` child table (multi-general-specs). Recorded for lineage completeness. **Precedence fix ✅ COMPLETE (feat `e997028b` + docs `56ee92b9`).** Reordered the rules in `assemble_mapping_config` — the `general_specs_sheet` pointer check moved from Rule 2 to **Rule 1, before the Skip/Hidden branch.** Real-data root cause on `BOQ-26-00145`: sheet "SOW" had `wizard_status="Skip"` (set via the hub before the general-specs designation), so the old rule order short-circuited to `skip=True` and never consulted the pointer, leaving `master_preamble` empty after parse. The fix makes the general-specs designation win regardless of `wizard_status`. (This is WHY, in the v5.38 Slice-2c live test, SOW correctly routed as a general-specs sheet despite showing "Skip" status.) **+2 tests** (one unit `assemble_mapping_config`, one worker-level mirroring the exact production scenario; test_parse_run → 47). Also recorded the deferred multi-general-specs requirement (which Slice 2c then delivered). **Slice 2c — multi-general-specs (scalar→child-table) ✅ COMPLETE + LIVE-CERTIFIED (feat `b5381c0c` + docs `445ceea5`).** Full detail in the v5.38 header + the §6 2026-06-03 decisions block. In brief: reversed M2.16; new child doctype `BoQ General Specs Sheet` on `BOQs.general_specs_sheets` (one row per general-specs sheet: `source_sheet_name` + `preamble_text`); dropped the `general_specs_sheet` pointer + `master_preamble` scalar; idempotent migration `migrate_general_specs_to_child_table.py`; parser A-narrow (`ParsedBoq.master_preamble` → `master_preambles: dict[str,str]`); worker two-read-site update (set-membership routing + the mark-Parsed gate) + Step-6 per-sheet delete-then-insert preamble loop; `set_general_specs_sheet` replace-all, `wizard_status` untouched (M2.23). **Live-certified on `BOQ-26-00145`** (two general-specs sheets SOW 10652 + MEP Make List 6210 → two distinct child rows, replace-not-duplicate; HVAC re-parsed 144 rows not doubled, marked Parsed; SOW/MEP 0 Review Rows + stayed Skip). Multi-select designation UI deferred to Slice 2b. **Slice 2-fix — the "Parsed" frontend gap ✅ COMPLETE + live-confirmed (feat `24312cab` + docs `4aac7b07`).** Full detail in §9 #140 + the §6 decisions block. Frontend-only: taught `WizardStatus` union + `STATUS_PILL` (green pill) + `SheetCard` button branches about the `Parsed` value (latent since Slice 1; the backend wrote it, the frontend never handled it → fake-Pending pill + empty card body). NOT a 2c regression. **Parse-run test counts (bench-verified 2026-06-03 via §9 #142 canonical commands):** parser **588 OK**; wizard total **141 OK** = `test_parse_run` 50 + `test_update_sheet_draft` 49 + `test_update_boq_draft` 7 + `test_sheet_preview` 23 + `test_upload_file` 12. (Supersedes every earlier "89" / "99" / "83" wizard figure in this doc — those predate the parse-run suites.) **Slice 2b-backend — dirty marker ✅ COMPLETE (feat `7795582f`).** `set_sheet_config` drops a Parsed sheet to Reviewed when the saved config ACTUALLY changes (semantic compare via `json.dumps(sort_keys=True)`; no-op saves do not fire). Decision: the marker means "config changed," not "config touched" (a false dirty flag erodes trust + risks discarding edits on re-parse). **Slice 2b-backend-2 — parse-history fields ✅ COMPLETE (feat `896f3a3c` + docs `e8d379af`).** Added `has_prior_parse` (Check 0|1) + `last_parsed_at` (Datetime) to `BoQ Sheet Draft`; the worker stamps both on a successful parse. These are HISTORICAL FACTS (write-once) — never cleared by any status change (survive the config-change drop AND set-pending). `wizard_status` = current intent; these = history. Dirty-detection contract: `wizard_status=="Reviewed" AND has_prior_parse==1`. General-specs sheets never stamped (guarded). +5 `test_parse_run`, +4 `test_update_sheet_draft`. **Slice 2b-frontend-i — hub Parse button + ParseRunDialog ✅ COMPLETE (feat `c9fc37fd` + docs `ed966506`; frontend-only, tsc+Vite verified).** Wired the hub "Parse workbook" button to `run_parse` (the parse-run, NOT the DB commit). New `ParseRunDialog.tsx`: four-list confirm (Will Parse w/ all-ticked checkboxes, Skipped, Pending, Already Parsed); subset parse via the ticked list; warn-before-reparse fires only when the ticked set includes a `has_prior_parse==1` sheet. `boq:parse_run_done` socket listener (mirrors BoqUploadScreen; guards `payload.boq_name===boqId`). Dirty "needs re-parse" badge + `last_parsed_at` on cards. (Inline result panel — superseded by the planned completion modal, §9 #144.) **Slice 2b-backend-3 — multi-select endpoint ✅ COMPLETE (feat `e996d097` + docs `9bec5bb5`).** `set_general_specs_sheet(boq_name, sheet_name_or_none: str)` → `set_general_specs_sheet(boq_name, sheet_names)` — a LIST, replace-all-to-many, validate-all-before-write (mirrors `set_sheet_work_packages`; inline JSON-or-list normalize, no shared helper). Empty list = clear all. Non-Hidden validation (only Hidden rejected; Reviewed/Pending/Skip/Parsed all designatable). **Option A clean break** (list-only; no backward-compat shim — recon confirmed only 2 callers: the test file + the frontend hub, both updated). The bypass test `test_two_designated_sheets_produce_two_rows_with_distinct_names` REWRITTEN to actually call the endpoint; +6 new tests (multi-designate, multi-then-fewer, clear-with-[], hidden-rejection-no-partial-write, one-hidden-among-two, multi-status-untouched). 60→66 `test_update_sheet_draft`. NOTE: intentionally left the frontend caller broken (fixed in 2b-frontend-ii). **Slice 2b-frontend-ii — multi-select checklist + caller fix ✅ COMPLETE (feat `d1672c6f` + docs `8735d088`; frontend-only).** Replaced the single-select dropdown with a multi-select CHECKLIST (candidate set = non-Hidden only — the old dropdown wrongly listed Hidden; backend now rejects them); mirrors the ParseRunDialog checklist pattern. ONE combined warn-on-newly-designated-Reviewed (names them; Cancel leaves local ticks). FIXED `doSetGeneralSpecs` to send `{ sheet_names: [...] }` — **un-breaks the branch** from 2b-backend-3. Dead `Select*` imports / `NONE_SENTINEL` / `generalSpecsValue` / single-select handlers removed. Closes the C7 schema-ahead-of-UI interim. BoqHubPage.tsx now ~674 lines (watch: extract checklist component if it crosses 700). **Parse-run test counts (bench-verified 2026-06-04 via §9 #142):** parser **588 OK**; wizard total **163 OK** = `test_parse_run` 55 + `test_update_sheet_draft` 66 + `test_update_boq_draft` 7 + `test_sheet_preview` 23 + `test_upload_file` 12. **Parse-run Phase-1 status (v5.40): EXIT-CRITERION-B FULLY CERTIFIED.** All four checks proven live on `BOQ-26-00145` (end-to-end manual round, full bench+Vite de-stale): Check 4 (Pending reported-not-parsed) ✅; Check 5 (dirty marker — column-config change drops Parsed→Reviewed + badge — the REAL config-change drop, not the v5.38 DB-write shortcut) ✅; Check 6 (re-parse warning fires; silent on clean first parse) ✅; Check 7 (subset re-parse — Electrical-only ticked, HVAC left dirty+unticked → Electrical Parsed w/ fresh timestamp, HVAC stayed Reviewed+dirty untouched) ✅. **Check-7 row-level note:** the literal row-replace diff was NOT frontend-observable (no row-viewer UI yet) — certified INDIRECTLY (unchanged, previously-live-certified worker replace logic + backend tests + consistent observed behavior); the visual row-diff is DEFERRED to the review screen (§9 #146). **Module 2b COMPLETE. The entire parse-run surface (trigger + subset + dirty + parse-history + multi-general-specs) is built and proven.** NEXT: Bucket-1 hub-polish (§9 #143), then the review SCREEN / Phase-2c DB-commit (which still owes the Phase-5 skip-filter, parser-check G, §9 #135). **Bucket-1 hub-polish ✅ COMPLETE (feat `f5dcfdd6` + docs `b3e8febc`; frontend-only, tsc+Vite verified).** Three items (§9 #143; item 3 "selector→checklist" wording DROPPED as too minor): (1) hub "Parsed: N" count appended to the parse-gate footer status line (`parsedCount` filters `dataSheets` by `getEffectiveStatus==="Parsed"`; "reviewed" stays the lead metric); (2) general-specs checklist → 2-col responsive grid (`grid-cols-1 sm:grid-cols-2`); (3) spoke Option-B section grouping in `SheetConfigPanel.tsx` — two zones: "Parsing configuration — changes here require re-parsing" (Sections 1-3) vs "Sheet details — saved without re-parsing" (Section 4); review gate confirmed state-driven not DOM-coupled (recon), so the wrapping is safe.
 **Field properties:**1. **Plural list-typed for forward-compat.** Single-element list `[2]` is the Phase 1.9d immediate-use case (Raheja HVAC); multi-element list `[1, 2]` or `[1, 2, 3]` is the future Pattern 6 case.2. **Phase 1.9d implementation supports single-element lists only.** Orchestrator uses `override[0]` when set. Multi-element behaviour is documented as the Pattern 6 future shape; parser code for multi-row concatenation is deferred.3. **Validators (Phase 1.9d):**   - `@field_validator("top_header_rows_override")` — entries ≥ 1, entries unique, empty list normalized to None for fallback simplicity.   - Model-level cross-field check in existing `validate_sheet_config` model_validator — each entry must be `< header_row` (raises ValidationError with sheet-name-prefixed message otherwise).4. **Orchestrator behavior.** In `parse_boq()` (lines 225-228 — NOT `_apply_multi_area_post_pass` as the prompt initially assumed; agreement #11 verify-before-changing surfaced the correct location): if `sheet_config.top_header_rows_override` is set, uses `top_row_idx = override[0]` for the `reader.iter_rows(sheet_name, start_row=top_row_idx, end_row=top_row_idx)` call; else falls back to existing `header_row - 1` behaviour. Single-element semantics; multi-element list is future Pattern 6 work (parser uses `override[0]` for now, with inline comment pointing at §7.39 + §17.13).5. **No schema migration when Pattern 6 lands.** The list typing is the migration path — Pattern 6 implementation just extends the orchestrator's branch to read all rows in the override list and apply concatenation logic. Existing MappingConfigs with single-element lists continue to work without change. **Pattern 6 future shape (locked, but parser implementation deferred).** Per §7.4 + §7.12 + §7.19 (Société Générale, decided 2026-05-12):- **2-level concatenation rule.** Top-row value + bottom-row value, joined by underscore separator. Each value gets parenthetical-suffix stripping (case-insensitive trailing `(Qty)` / `(Quantity)` / `(Amount)` / etc. — any trailing parenthetical containing a reserved keyword).- **Worked example.** Top `Voyager (Qty)` → strip `" (Qty)"` → `Voyager`; bottom `Ground +MF` unchanged → compound `Voyager_Ground +MF`. Similarly `Victor_Level-02 Office`, `Victor_Level-02 Cafeteria`.- **3+ level concatenation is a known boundary.** No real fixture across the 24 committed BoQs currently exhibits 3+ rows of area-name data. Build when a real fixture surfaces.- **Re-evaluation triggers for Pattern 6 parser implementation:** (a) Phase 1.9e surfaces a 2nd Pattern 6 fixture in the 24-fixture corpus (currently only Société Générale exhibits the shape and it's absorbed into the 224-keyword reserved list at the detection-blocker level); OR (b) a real user needs Société Générale upload before Phase 3 ships. Until then, Pattern 6 parser support is deferred; Phase 3 wizard may absorb the shape via user mapping rather than parser detection (per §17.13 declarative-first wizard direction). **Migration semantics.** When Phase 1.9d ships, `MappingConfigs` can author `top_header_rows_override=[2]` for HVAC-shape sheets. When Pattern 6 lands later, the same MappingConfig field accepts `[1, 2]` for compound area names — no breaking change to the field type. **Test coverage in Phase 1.9d:**- `test_config.py` `TestTopHeaderRowsOverride` class — 6 tests: defaults-to-None, valid-single-element-list-accepted, empty-list-normalises-to-None, duplicates-rejected, zero-or-negative-rejected, entry-at-or-above-header_row-rejected (cross-field).- `test_multi_area_detection.py` `test_pattern_2_rate_detects_rates_plural_header` — in-memory RawRow test for the F3b regex widening (companion test, not strictly F5-b coverage but landed in same commit).- `test_orchestrator.py` `TestPhase19cRealFixturesRahejaHVAC.test_hvac_pattern_2_rate_with_header_gap` — real-fixture integration test, now passes naturally (was `@unittest.expectedFailure` before Phase 1.9d). **Cross-references.** §7.4 + §7.12 + §7.19 (Pattern 6 / Société Générale design history); §17.13 in plan doc (wizard-load review pending parking entry); §9 #63 F5 (closed by this design); §9 #69 LESSON (the hash-paradox surfaced during design-lock commit for this feature). ### 7.38 — `_NULLABLE_NUMERIC_FIELDS` normalization pattern for audit/diff comparison on Frappe Currency/Int fields (NEW 2026-05-17, Phase 1.8.1 precedent) **Context.** Phase 1.8.1's F2 fix added a no-op-save guard in `_write_audit` (`if not changed: return`). Phase 1.8.1's new test `test_audit_entry_not_written_when_no_fields_change` failed on first run — saving a `BOQ Nodes` document twice without changing any tracked field produced a phantom audit entry. **Root cause.** Frappe stores `Currency` and `Int` doctype fields with `None` value as `0` in PostgreSQL. On reload (via `get_doc_before_save()`), the DB-state `old_doc` returns `0` for those fields while the in-memory `doc` still has `None`. The diff comparison `old_doc.get(field) != doc.get(field)` evaluates `0 != None` → `True`, producing a spurious "change" record on every no-op save. This is a pre-existing Frappe storage behaviour, not specific to BOQ Nodes — it affects ANY doctype controller doing diff-based audit logging on Currency/Int fields.
27. **Metric-parser co-evolution.** (NEW v5.18, codified 2026-05-19.) Diagnostic metrics that classify line items by role-name membership (e.g., `_QTY_FAMILY_ROLES`, `_RATE_FAMILY_ROLES`, `_AMOUNT_FAMILY_ROLES`) must be re-validated whenever role-assignment logic changes. Specifically: any sub-phase that modifies `_HEADER_KW`, the role-matching precedence in `_auto_guess.py`, or the set of recognized role enum values must include a metric-impact review as a mandatory checklist item. **Rationale:** Phase 1.9l Mode D's longest-match-wins precedence silently shifted `Total Qty` headers from role `qty` to role `qty_total`. The Phase 1.9j metric's `_QTY_FAMILY_ROLES = frozenset({"qty"})` did not include `qty_total`, causing three single-area BoQs to read as fully unassigned in the 1.9n diagnostic when they were actually correctly parsed with the new role. STOP triggered, investigation required, corrective metric fix landed as part of 1.9n. A second instance (amount-family metric over-counting based on role assignment rather than data presence — §9 #75) remains latent and is queued for diagnostic repair as a follow-on chore. **Scope:** add to sub-phase prompt template a "Metric impact review" section enumerating any role-name changes and the corresponding metric definitions that need verification. The diagnostic itself should ideally compute role-family membership from a single authoritative source (e.g., a per-family registry in `_auto_guess.py`) rather than hard-coded frozensets in the diagnostic script, but this is a future improvement, not a hard requirement. Applies to any future diagnostic, observability script, or test suite that compares parser output against role-name expectations. Cross-reference: §22.7 (1.9n investigation), §9 #75 (amount-family metric over-counting), Phase 1.9n metric correction commit `3af8e828`. 28. **Ground-truth validation requirement.** (NEW v5.18, codified 2026-05-19.) Diagnostic-script outputs are insufficient evidence that the parser is correctly handling a given BoQ. Every significant sub-phase cycle must include at least one ground-truth pass where a sample of real BoQ fixtures is opened in Excel and compared, column-by-column, against the parser's `auto_guessed_column_role_map` AND a sample of resolved line item values is verified against source cells. Frequency: minimum once per cycle of 3-5 sub-phases; mandatory before any cycle's strategic re-evaluation. **Rationale:** the 1.9j-1.9n cycle's diagnostic measurement systematically misread three of nine targets as "clean" when in fact they were silently dropping half their priced economic content per line item (Tier A-merged shape: Bill of Quantities, Paytm HVAC, Electrical Unpriced). The singleton-guard in `_auto_guess.py` Phase 1 correctly refused to assign duplicate roles, but the parser landed at a worse outcome (data loss) than even a "wrong" assignment would have been. The metric reported `rate: real=N/N` because rate_combined was assigned, masking that rate_install data was being dropped. Only ground-truth inspection of the actual xlsx files surfaced this — the diagnostic could not. **Scope:** sample at least one target from each apparent verdict bucket (clean, partial, residual) per cycle. Open each sampled fixture in Excel; verify column headers, merged-cell structure, sample 5-10 line item values per sheet. Ground-truth pass should happen in a chat-Claude review session, not inside Claude Code execution. Findings from ground-truth review feed into the strategic re-evaluation chat (per existing §22.8 framing). Cross-reference: §22.10 (v5.18 deep-dive session); Phase 1.9j-1.9n ground-truth findings. 45. **Rolling-window archive convention.** (NEW v5.30, codified 2026-05-28.) Per-cycle audit-checklist + Trigger state sections live in the handover doc for the CURRENT cycle only. Previous cycles' audit-checklist + Trigger state sections move to `BOQ_Handover_archive.md` (kept on Nitesh's Desktop, **NOT in project knowledge** — re-upload as one-off attachment if deep history is needed in-session) rather than preserved inline. The aggressive rotation is applied at every housekeeping cycle: when handover version v(N+1) is drafted, v(N)'s "CHANGES SINCE v(N-1)" section + "Trigger state vN" section get moved to archive with their headers demoted to `## Historical: CHANGES SINCE v(N-1) — AUDIT CHECKLIST (vN cycle)`. The handover doc then carries only v(N+1)'s own audit-checklist + Trigger state inline. **Companion discipline — promotion before archive (operational pattern, not separate agreement):** rule-level content from the archived section (new working agreements, strategic decisions, §9 known-issue status flips for LANDED bugs, §9 new entries for newly-discovered bugs, caveat status changes) must be promoted to canonical sections (§6, §8, §9) BEFORE the archive move. Session-level narrative (test counts, calibration counts, code-path mechanics, fixture-by-fixture walkthrough details, operational push/commit details) stays in archive without promotion. **Rationale:** v5.21-v5.28 cycles let the handover doc grow to ~8500 lines, most of it cycle-by-cycle changelog redundancy already promoted to §6/§8/§9 canonical sections. v5.29 → v5.29a applied a 58% reduction (8,477 → 3,564 lines) by archiving 17 historical CHANGES sections + 12 EOS sections + compressing §23. This agreement codifies that operation as standing convention going forward — prevents re-accumulation of redundant content + keeps the live doc grep-fast and chat-context-friendly. **Archive location policy:** archive file lives on Nitesh's Desktop only; project knowledge holds the slim live handover doc only. If a session needs deep historical lookup, Nitesh uploads the archive as a one-off chat attachment for that session. **Scope:** every handover housekeeping cycle from v5.30 forward. **Cross-references:** §10 resume prompt mentions archive availability; §11 critical pointers note that archive is searchable on demand via one-off upload; v5.29 → v5.29a reduction is the first instance. 46. **Wizard scope discipline.** (NEW v5.31, codified 2026-05-29.) When a wizard-feature decision presents two paths — (a) build the capability inside the wizard, or (b) defer to or extend an existing app-wide flow — chat-Claude pauses and surfaces the fork explicitly in chat before drafting code. **Default lean: if the capability has reach beyond the Upload BoQ flow (i.e., other Nirmaan features would benefit from the same capability), keep it outside wizard scope.** The lean is a starting point only; final call is case-by-case after explicit discussion with Nitesh. **Origin: Module 1a 2026-05-29.** `create_tendering_project` was initially scoped as a wizard backend endpoint (Wizard Design v1.1 §6.1.11 endpoint A); during CC prompt drafting, the principle surfaced when chat-Claude noted that tendering-project creation has reach beyond the wizard (sales pipeline, tender notification handler, standalone create-tendering-project page would all benefit). Architectural call: wizard never creates Projects rows in production code; "Create new project" picker path defers to existing Nirmaan new-project workflow at `frontend/src/pages/projects/project-form/` with `?wizard_resume_with_project=<project_id>` URL parameter for resumption. Wizard Design v1.2 §4.7 codifies the principle as a cross-cutting design decision; this agreement makes it standing operational discipline for wizard build going forward. **Application:** every subsequent wizard module's design conversation must pass through this discipline at every decision that creates new functionality. **Rationale:** wizards tend to accrete features ("while you're in here, we could also..."); without active scope discipline, the wizard becomes a feature-creep magnet that competes with rather than complements the rest of the app. The principle keeps the wizard tightly scoped to its core job (Excel → structured BoQ) and routes general-purpose capabilities to general-purpose homes. **Scope:** every wizard module design conversation and every wizard module CC prompt from v5.31 forward. **Cross-references:** Wizard Design v1.2 §4.7 (cross-cutting decision); §6 decisions log 2026-05-29 entry; CLAUDE.md root convention codification commit `91d603eb`. 47. **Boundary-crossing slices must be live-tested before "done".** (NEW v5.32, codified 2026-05-30.) Any slice that crosses a system boundary — frontend↔backend, app↔job-queue, app↔socket, app↔third-party plugin (e.g. `frappe_s3_attachment`), app↔external storage — is NOT considered "done" until it has been **live-tested against the running system**, not merely built and unit-tested. A green unit suite and a clean build are necessary but NOT sufficient for a boundary-crossing slice. **Origin: Module 1b 2026-05-30.** The entire upload pipeline (frontend → upload POST → queue → worker → parser → socket → frontend) was built and committed across many sub-slices (1b-i through 1b-ii-b) without ever being run live until the end. Five+ integration bugs then came due in one sitting: wrong endpoint path (417), S3 storage-path assumption, `source_file_url` field-length overflow, stale-session CSRF rejection, plus a phantom socket bug that turned out to be downstream of the insert crash. Every one of these existed only *between* the pieces, at runtime — invisible to unit tests and clean builds, which passed truthfully throughout. **Rule:** sequence boundary-crossing work as a series of small "build it → run it live → fix the one thing → move on" cycles, rather than building the whole pipeline and integration-testing once at the end. A slice's exit criteria include a live run against the running system that exercises the boundary it crosses. **Companion rule (from the same session):** treat "verified the contract" / "this is the root cause" claims as HYPOTHESES until proven against the running system with concrete evidence (console output, Network/WS frames, server logs, `tsc`/migrate output). Session 1's confident CSRF diagnosis was wrong; only live evidence (the Projects page failing app-wide, the WS frame showing the event arrive) revealed the true causes. **Scope:** every boundary-crossing slice from v5.32 forward — wizard modules especially (each Module 1b+ slice touches frontend↔backend↔queue↔socket). **Cross-references:** §6 decisions log 2026-05-30 entry; §9 #118-#123; the two debrief docs (`BoQ_Upload_Debug_Session_2026-05-30.md`, `BoQ_Upload_Session_Addendum_2026-05-30_night.md`). 48. **Pre-send prompt gate + canonical command blocks (forcing-function for environment-specific command discipline).** (NEW v5.38, codified 2026-06-03.) **Problem this fixes:** the project repeatedly banked environment-specific command knowledge (the bench path, the `cd &&`-not-`-w` rule, the test invocations — §9 #139/#142) as a "standing convention for all future prompts," but it kept slipping: prompts were drafted with loose test instructions ("run the test suite") and CC then burned session time rediscovering invocations already documented in the env notes. A convention that lives in a doc but isn't wired into the drafting step regresses to default — same failure the project fixed for the strategic audit by making Checklist C mandatory-at-resume (#41). **The mechanism (two parts):** **(a) Canonical command blocks** — the exact, bench-verified test + build commands live in §9 #142 (tests) and §9 #139 (Vite build) as copy-paste artifacts. Any CC prompt that runs tests or the build includes the relevant block(s) **VERBATIM**, sourced from those entries — a COPY action, never a retype/paraphrase. Copy doesn't drift; recall does. **(b) The pre-send gate** — before presenting ANY CC prompt to Nitesh, chat-Claude runs a fixed 4-item check: (1) if the prompt runs tests/build → is the canonical command block present verbatim? (2) is the Files-in-scope exclusive list present? (3) is the 25-item self-report required (agreement #20)? (4) is the two-commit feat+docs shape stated (agreement #9/#13e)? Any "no" → fix before sending. This catches omissions at draft time, before they cost a round-trip. **KILL CRITERION (per the process-artifact-discipline principle — all discipline mechanisms must have a kill criterion to avoid bureaucratic drift):** if the canonical command block goes **10 consecutive test-running prompts without needing a correction**, downgrade the pre-send gate from a written check to a spot-check (trust-but-verify). If a correction is ever needed again after downgrade, the full gate is reinstated. **Scope:** every CC prompt from v5.38 forward. **Cross-references:** §9 #142 (canonical test commands), §9 #139 (canonical Vite build), agreement #20 (25-item self-report), agreement #44 (prompt hygiene), agreement #41 (the precedent: intention → forcing-function). 49. **Recon-first on EVERY code-touching change.** (NEW v5.40, codified 2026-06-04.) Before drafting any CC prompt that will touch code (not just before a large/architectural one), chat-Claude runs a read-only recon — or relies on a recon already run this session — to confirm the actual current contract/state of the code being changed, rather than asserting it from a handover or dashboard summary. **Problem this fixes:** in the v5.40 session, a 2b-frontend-ii prompt asserted "backend done — this is frontend-only" on the strength of the Trigger-state "schema-ahead-of-UI" summary; the endpoint was in fact still single-sheet. CC's verify-first/A20 caught it before any code was written, but it cost a round-trip and forced an unplanned backend slice (2b-backend-3). Every OTHER code change that session ran a recon first and went clean; the one that skipped it produced the wrong premise. Root cause = trusting a summary over the authoritative source (the plan-doc line literally said "Full multi-select semantics are Slice 2b") + breaking the session's own recon-first pattern. **Rule:** "X is done / X works this way" about code is a HYPOTHESIS until confirmed against the source this session; the cheap fix is a read-only recon prompt before the build prompt. Companion to agreement #47 (diagnose-first / claims-are-hypotheses) — #47 is for runtime behavior, #49 is for code-state premises at draft time. **Scope:** every code-touching CC prompt from v5.40 forward. **Cross-references:** §6 v5.40 decisions; agreement #47; the stale-summary failure mode (a recurring chat-Claude weakness — confident assessment on stale document versions). 50. **Checklist A / B / C are three distinct instruments — do not conflate (Caveat W discipline).** (NEW v5.40, codified 2026-06-04; corrects a drift that surfaced this session.) **Checklist A (the FIXED 25 items A1–A25) is CC's SELF-REPORT structure** — the CC prompt instructs CC to report against the fixed A1–A25 from the handover, by number, stating how each was satisfied or "N/A — reason." It is NOT an open-ended "list 25 things you did" (that freeform form IS the Caveat W failure). **Checklist B (22 items) is chat-Claude's REVIEW of CC output.** **Checklist C (7 items) is chat-Claude's resume audit** (agreement #41). **Chat-Claude's pre-send gate is the agreement #48 4-item gate, NOT Checklist A.** **Additional discipline locked this session:** the FULL Checklist A must be satisfied IN THE DRAFT before the prompt is sent — so CC's A1–A25 returns as clean confirmations, not a catalogue of the prompt's omissions. (In the v5.40 session a prompt went out failing ~9 A-items because the rebuilt prompt was overtaken by the already-running original; the work was sound but the prompt hygiene wasn't.) **Genuine N/As for a frontend slice:** A5 (no doctype JSON), A6/A13/A22 (no Frappe-test harness — tsc/build-verified + manual matrix), A14 (no `from frappe import _`). **Scope:** every CC build prompt from v5.40 forward. **Cross-references:** agreement #20 (25-item self-report origin), agreement #48 (pre-send gate), agreement #41 (Checklist C). 51. **Surgical hash-backfill — match ONLY the slice's own line, never a global replace.** (NEW v5.41, codified 2026-06-05.) When CC backfills the feat hash into the docs commit (Option B), it must edit ONLY the placeholder line(s) for the slice being committed — never a blanket find-replace of any "feat TBD" / "TBD" token across the doc. **Problem this fixes:** in the prior session a global `feat TBD` backfill corrupted two OLDER slice records (stamping them the current hash instead of their real `16a6a4dc` / `9f8fb6f7`). **Rule:** the backfill target is the specific line just written by THIS docs commit; if multiple placeholders exist, disambiguate by the slice name, not by the token alone. Companion to Caveat D (one amend cycle only). **Scope:** every docs commit with a hash backfill. (Held clean across all of v5.41's slices once codified.) 52. **Verify a backgrounded build by waiting for the completion notification + reading the redirect file ONCE — do not poll or launch a second build.** (NEW v5.41, codified 2026-06-05.) The canonical Vite build is run backgrounded with output redirected to a file; the build's exit signal (the completion notification) is authoritative even though Docker buffers the redirected stdout until flush. **Problem this fixes:** CC repeatedly re-read an empty redirect file mid-build, concluded "Docker is buffering," and launched a SECOND foreground build — burning ~15 min and risking two concurrent builds, on two separate slices this cycle. **Rule:** after launching, wait for the completion notification, then read the redirect file ONCE; if a build is genuinely still running, `pgrep -f vite.js` confirms it — do not poll the output file in a loop and do not start a concurrent build. **Scope:** every backgrounded build verification. (A tooling-efficiency rule, not a correctness rule — the builds were always fine.) 53. **Push from HOST PowerShell git, not `docker exec`.** (NEW v5.41, codified 2026-06-05.) `git push` run via `docker exec ... git push` fails with `could not read Username for 'https://github.com'` — the container's git has no GitHub credentials and no interactive terminal to supply them. The host Windows git (with Git Credential Manager / a cached token) is where the auth lives. **Rule:** run `git push origin <branch>` from a HOST PowerShell session in the repo dir (`cd C:\Users\nites\Documents\frappe_docker\development\frappe-bench\apps\nirmaan_stack`); the host path is the bind-mount source for `/workspace`, so it's the same `.git` and pushes exactly what the container committed. (Fetch/status checks are fine either way; only the credentialed push needs the host.) **Scope:** all pushes. Owner = Abhishek normally; Nitesh runs it from host when Abhishek is unavailable. Add to the Environment Runbook. 54. **Frappe Int NULL→0 coercion trap — use an explicit `-1` sentinel, never None-via-default.** (NEW v5.42, codified 2026-06-05; TWICE-BITTEN.) Any nullable `Int` field persisted on a Frappe doctype where `0` is itself a meaningful value (notably a row INDEX) must carry an explicit out-of-band sentinel — `-1` — written explicitly at the DB boundary. Frappe coerces `Int None → 0` on insert, so a field relying on `None` for "absent" silently becomes `0`, indistinguishable from a real `0`. **Problem this fixes:** on `BoQ Review Row`, `parent_index`/`human_parent` use `0` for "parent is row 0" and `None` for "no parent" — after Frappe coercion, 618 real rows all read `0` with ZERO NULLs, making root detection impossible. The bug bit twice (first `human_parent` → an interim `has_human_parent` Check field; then `parent_index`), which is what forced the unify. **Rule:** (a) pick a sentinel that can never be a valid value (`-1` for a row index); (b) write it EXPLICITLY at the persistence boundary (`flatten_resolved_row`, `save_review_edit`) — do NOT rely on a field `default`, because the default is the unreliable layer this bug lives in; (c) translate the sentinel back at a SINGLE read chokepoint (`resolve_effective`: `-1 → None`) so all downstream logic stays sentinel-unaware; (d) keep the sentinel confined to the DB boundary — the in-memory/parser layer keeps using `None`. Companion to §7.38 (the Phase-1.8 `_NULLABLE_NUMERIC_FIELDS` comparison-normalization, which addressed the SAME None→0 artifact for audit-diff purposes — #54 is its persistence-layer generalization). **Scope:** every nullable-Int field on a Frappe doctype from v5.42 forward. **Cross-references:** §9 #148 (the trap), §6 v5.42 decision (the unify), §3 Slice-A record, §7.38 (the numeric-field precedent). 55. **Parser-test fixture byte-churn — revert before stage, never commit.** (NEW v5.42, codified 2026-06-05; long-standing artifact promoted to a forcing-function.) Running the parse-run (or any parser) tests opens and re-saves the `synthetic_*.xlsx` fixtures, producing harmless zip-metadata byte-churn (`git diff --stat` reports `0 insertions, 0 deletions`; sizes byte-identical or off by a few bytes). **Problem this fixes:** if the churn slips into a broadened `git add`, it silently alters the committed parser-test inputs — corrupting the test baseline. **Rule:** every CC prompt that runs parser tests must (a) include a fixture-revert step `git checkout -- nirmaan_stack/services/boq_parser/tests/fixtures/` IMMEDIATELY BEFORE the commit's `git add` (before staging, so the commit cannot capture the churn), and (b) carry an explicit Out-of-Scope declaration that these fixtures are pre-declared churn — report if dirty, never commit, unless the slice INTENTIONALLY changed fixture content (then STOP and flag). The fixture-revert step is now part of what the agreement #48 pre-send gate checks for test-running prompts. **Scope:** every parser-test-running CC prompt from v5.42 forward. **Cross-references:** §9 #150 (the note), §9 #129 (the original dev-server-staleness/fixture-churn observation, v5.35), agreement #48 (pre-send gate), agreement #26 (synthetic-xlsx restore discipline).
**Scope.** Wizard-side detection refinement. The parser, given a correctly-declared `area_dimensions = ["Voyager GF", "Voyager 1F", "Voyager 2F"]` with column D / E / F mapped to qty/Voyager GF / qty/Voyager 1F / qty/Voyager 2F, parses correctly — this was confirmed in the Sequence E2 experiment. Wizard just needs to produce the right SheetConfig. **Status:** OPEN, queued for Phase 3 wizard design spec. --- ### §9 #93 NEW v5.21 — MSYS path conversion on Windows docker exec (operational) **Symptom.** Bash tool / Claude Code on Windows translates `/tmp/...` paths to `C:/Users/.../Temp/...` when passing them as docker exec arguments. The container then sees a non-existent path because the translation produces a host-system path, not a container path. **Discovered.** During Sequence E2 setup when chat-Claude prompted Claude Code with `docker cp src.json /tmp/dest.json`. The cp landed at `C:/Users/.../AppData/Local/Temp/dest.json` on the Windows host, not at `/tmp/dest.json` in the container. **Workaround.** Prefix `MSYS_NO_PATHCONV=1` on ALL docker exec/cp commands when passing UNIX-style paths through Git Bash / MSYS environments. Example: ```bashMSYS_NO_PATHCONV=1 docker cp src.json container_name:/tmp/dest.jsonMSYS_NO_PATHCONV=1 docker exec container_name bash -c "ls /tmp/"``` Companion to §9 #78 (`docker exec -w` flag failure) — same family of Windows/MSYS quoting/translation issues. **Status:** DOCUMENTED, mitigated via the env var workaround. Add to standard docker-command prefix in Claude Code prompt templates. --- ### §9 #94 NEW v5.21 — Docker cp over heredoc corrupts ownership (operational) **Symptom.** Writing files into container `/tmp/` via heredoc through `bash -c` (i.e., the docker exec runs a bash command that contains `cat > /tmp/file << 'EOF' ... EOF`) results in the file being created as root-owned. Subsequent operations from the frappe user (the standard container user) fail with permission errors. **Discovered.** During Sequence E2 setup when chat-Claude's first prompt attempt used heredoc to write config files into `/tmp/`. Later cleanup of `/tmp/` failed because the files were owned by root. **Workaround.** Always use the Write-tool → docker cp INTO container pattern: 1. Write the file to a host temp file via chat-Claude's `create_file` tool (or similar local-FS write).2. Use `docker cp host_path container_name:/tmp/dest_path` to copy INTO the container.3. Verify ownership with `docker exec container_name ls -la /tmp/dest_path` — should be `frappe:frappe`. Never use heredoc through `bash -c` for container file creation. The user-ID context inside the bash subprocess inside docker exec is incorrectly inherited from the docker exec context (root by default for some Docker configurations). **Status:** DOCUMENTED. Pattern established. Add to standard Claude Code prompt template for container file-creation operations. ---
 **Root cause:** the upload screen subscribes to the `boq:wizard_parse_done` socket event on mount. The hang occurred on a session where the backend had just been restarted and the browser had done the clear-site-data dance mid-flight — which disrupts/recreates the socket connection. The `parse_done` event fired into a connection the screen wasn't cleanly listening on, so the screen never advanced even though the data was saved. **Confirmed NOT a code bug:** re-tested on a clean, freshly-rebooted session (no mid-flight restart, no clear-data dance) with the same file against a different existing project — upload advanced to the hub on its own, normally. The hang reproduces only on a *disturbed* session, not a clean one. (This is the happy-path analogue of §9 #122, which covered the error-path notification and was resolved.) **What to do if it recurs:**1. Don't assume the parse failed. Check `worker.log` for the `_upload_file_worker ... Job OK` line, and/or confirm the BoQ row exists in the DB (§4).2. If the BoQ is there, the data is safe — just navigate directly to `http://localhost:8080/upload-boq/hub/<BOQ-name>`. The hub loads fine from the saved state.3. Avoid triggering a parse/upload immediately after a backend restart + clear-site-data on the same screen. **Settle the socket first:** after the de-stale, load the bare root, navigate to the hub, and let it sit ~20-30s (the socket re-establishes and goes quiet) BEFORE triggering — a freshly-reconnecting socket is the worst timing and is itself a common cause of the missed event. If it still sticks, the data is safe: hard-refresh the hub (the v5.41 on-mount `parse_in_progress` read makes the HUB recover true state on a fresh mount), or navigate directly to the hub URL. **Promoted to an AGREED slice (handover §9 #147, 2026-06-05):** the socket-reconnect self-heal — on socket RECONNECT, re-check job/BoQ status and reconcile screen state so a missed done-event self-corrects WITHOUT a manual refresh, on BOTH the hub (`boq:parse_run_done`) and the upload screen (`boq:wizard_parse_done`). **Hard requirement:** the upload screen must recover IN PLACE preserving the BoQ-name/version edit step (NOT skip-to-hub). It also folds in a NEW defect found 2026-06-05: **the screen DISABLES during a parse** (can't navigate away — contradicts the don't-block intent), which must be fixed so navigation during parse is possible. Was previously "low-priority, candidate hardening"; now prioritized for PRODUCTION robustness (network blips / sleep-wake / ws timeouts over a ~5-10 min parse window have the same missed-event effect as the dev restart+clear-data dance). This is the agreed NEXT slice, before the review screen. --- ### 9.5.9 Git push — from HOST PowerShell, NOT `docker exec` (handover agreement #53) **The failure:** running `git push` inside the container —```docker exec frappe_docker_devcontainer-frappe-1 bash -c "cd .../nirmaan_stack && git push origin feature/boq-phase-3"```— fails with `fatal: could not read Username for 'https://github.com': No such device or address`. The container's git has **no GitHub credentials** and no interactive terminal to supply them; the HTTPS remote wants a username + token it cannot get. **The fix:** push from a **HOST PowerShell** session in the repo dir, using the host's git (which has Git Credential Manager / a cached token):```powershellcd C:\Users\nites\Documents\frappe_docker\development\frappe-bench\apps\nirmaan_stackgit push origin feature/boq-phase-3```The host path is the **bind-mount source** for the container's `/workspace`, so it is the SAME `.git` — pushing from the host pushes exactly what the container committed. Confirmed working (2026-06-05): `56ee92b9..b2ca0895`, 27 commits, after the `docker exec` form failed. **Notes:**- **Fetch / status / log are fine either way** — only the credentialed PUSH needs the host. A pre-push `git fetch origin && git status -sb` (either context) confirms ahead/behind before pushing; "ahead N, behind 0" = clean fast-forward.- **Owner:** Abhishek normally handles pushes. Nitesh runs it from host when Abhishek is unavailable (a one-off deviation that has become routine across the recent unavailable stretch).- **Branch note:** `feature/boq-phase-3` also carries Abhishek's Tendering commit `509a4dfe` — it is not pure-BoQ. ---
 `nirmaan_stack/integrations/controllers/boqs.py`, `boq_nodes.py` (15 tracked audit fields, `_NULLABLE_NUMERIC_FIELDS` frozenset per §7.38), `boq_node_qty_by_area.py` (~280-line helper module per §7.37; NOT a standalone Document class). ### Docker container quick-reference- Container: `frappe_docker_devcontainer-frappe-1`- Python in container: `/workspace/development/frappe-bench/env/bin/python`- Parser test invocation: `docker exec frappe_docker_devcontainer-frappe-1 bash -c "cd /workspace/development/frappe-bench && env/bin/python -m unittest discover -s apps/nirmaan_stack/nirmaan_stack/services/boq_parser -v"`- Canonical 7-module command (used by v5.23 + v5.24 + v5.25 cycles): `MSYS_NO_PATHCONV=1 docker exec -w /workspace/development/frappe-bench/apps/nirmaan_stack/nirmaan_stack/services/boq_parser frappe_docker_devcontainer-frappe-1 /workspace/development/frappe-bench/env/bin/python -m unittest test_classifier test_orchestrator test_auto_guess test_hierarchy test_multi_area_detection tests.test_config tests.test_reader 2>&1 | tail -5`- **v5.21 §9 #93:** Prefix `MSYS_NO_PATHCONV=1` on ALL docker exec/cp commands when passing UNIX-style paths through Git Bash / MSYS on Windows. The bash tool translates `/tmp/...` to `C:/Users/.../Temp/...` otherwise.- **v5.21 §9 #94:** For container file creation, ALWAYS use Write-tool → host temp file → `docker cp` INTO container. NEVER use heredoc through `bash -c` (corrupts ownership; files become root-owned).- §9 #65 LESSON: Docker outage recovery (Quit Docker Desktop UI → `wsl --shutdown` → wait for Stopped → relaunch).- §9 #76 mitigation: avoid PowerShell `Select-Object` pipe with docker exec — use file-redirect Option B form. ### Status**v5.35 close.** Tip `225a43ff` (origin `72052a45` → `225a43ff`, **PUSHED** this cycle — 38 commits, fast-forward). Parser tests **588** (source untouched; BENCH-VERIFY). Wizard tests **BENCH-VERIFY** (prefill added ~3 to `test_upload_file`; v5.34 carried 64). **MODULE 3 config-authoring UI (Sections 1/2/3) COMPLETE + GROUND-TRUTH-VALIDATED 8/8** across 3c → 3d-iii (3c Sections 1+2 `16a6a4dc`; 3c-fix `9f8fb6f7`; prefill auto_guess `5356b471`; 3d-i state-lift `0c297e24`; 3d-ii column-role list `f24ac4fe` + fixes `cbb704ce`/`f541e428`; 3d-iii grid annotations + freeze `83b63b7b`). All 8 cycle-3 sheets reconfigured blind in the UI and diffed vs hand-built configs: 8/8 match, zero UI defects. **7 parser-checks resolved** (A single top-header row; B amount_total/combined output-equivalent; C row/append-notes distinct; D qty/qty_total NOT equivalent — Phase-2c-relevant; E area names pure labels; F skip rows clean; G wizard→parser skip handoff missing — Phase-5 requirement). §9 #128 CLOSED; §9 #130-#135 NEW (6 batched UI findings + Phase-5 skip requirement). **NEXT: batched UI-fixes slice (6 findings) → auto-guess-emits-amount_combined micro-check → 3e review gate → 3f work-package assignment/schema.** Archive ledger CLEAN (v1.3) — v5.34 EOS rotated this cycle; v5.31/v5.32/v5.33 were done at v1.2. **[v5.34 close — prior cycle, retained]** Tip `84cfe2b5` (feature tip `25462509`; origin at `72052a45`, LOCAL AHEAD at the time). Parser tests **588 unchanged**. Wizard tests **41 → 64** (+23 `sheet_preview`). **MODULE 3 (per-sheet config spoke) — shell + preview + navigation BUILT + PROVEN LIVE** across five slices (3a schema incl. the M2.22 `work_packages` multi-link conversion; 3a-fix; 3b-i preview endpoint; 3b-ii spoke shell + grid + load-more; 3b-iii sticky header + gridlines). **BoQ-list tab + B2 tendering section** pulled forward from Phase 6 (feat `25462509`). Settled: `useParams()` returns decoded DB-verbatim names (SheetSpokePage "RR does NOT decode" comment WRONG, fixed in 3c, §9 #128); "stuck on parsing" is environment (§9 #129); `useFrappePostCall` sanctioned for paginating reads. --- **v5.31 close.** Tip `91d603eb` (origin in sync). Parser tests **588 unchanged** (Module 1a touches Frappe layer only). Frappe tests **679 → 696** (+17 wizard tests from Module 1a `06f38e8d`). **MODULE 1a (wizard backend + schema) SHIPPED** — first wizard slice delivered: new `feature/boq-phase-3` branch (from `feature/boq-phase-2` tip `2e338b36`); `BOQs.wizard_state` Select + `BOQs.sheet_drafts` Table + new `BoQ Sheet Draft` child doctype with 4 fields (`sheet_name`, `sheet_order`, `wizard_status`, `work_package`); `upload_file` (async, Socket.IO push `boq:wizard_parse_done`) + `update_boq_draft` (partial-update) endpoints under `api/boq/wizard/`. **Manual testing PASS on real BoQ** (project `BENGALURU-PROJ-00101` + `Bill of Quantities.xlsx` → `BOQ-26-00127` with 13 sheet drafts, hidden sheets marked, Electrical Work Header auto-detected on 2 sheets, attachment `ATT-00101-001` linked). **NEW working agreement #46** (Wizard scope discipline) CODIFIED v5.31 after `create_tendering_project` endpoint was dropped from Module 1a scope mid-execution. **NEW agreement #13e** extension (doc-update explicit discipline — every CC prompt must explicitly state whether plan-doc + CLAUDE.md get updated; ask user consent if CLAUDE.md is NOT touched). **Caveat D Option B clarified** (§9 #117 — one amend cycle only; do NOT loop amends to converge; mathematically infinite). **Wizard Design v1.2** supersedes v1.1 in project knowledge (§4.7 NEW wizard scope discipline cross-cutting decision; §6.1.2 picker 3 → 2 sub-paths; §6.1.3 rewritten as project creation handoff; §6.1.10 schema reflects what shipped; §6.1.11 endpoints 3 → 2; M1.38-M1.42 DROPPED; M1.52 + M1.53 NEW). **3 new §9 entries:** #115 sheet-to-Work-Headers cardinality (Module 2 design item); #116 existing new-project workflow extension dependency (Nirmaan team independent work); #117 Caveat D Option B clarification. **Caveats KK retired v5.31** (held cleanly across cycles 3 + 4 + 1a). **NEW caveats QQ + RR + SS v5.31** (frappe.utils.now() truncation pattern; Projects fixture project_scopes requirement; Customers gst_no nullability latent bug). **NEXT WORK ITEM: Module 1b (wizard frontend) — design conversation + CC prompt drafting.** Per Wizard Design v1.2 §6.1: sidebar nav + in-project BoQ tab + project picker (2 sub-paths) + upload screen (two-pane) + Continue stub + Socket.IO listener + `wizard_resume_with_project` URL param handling. Open coordination with new-project workflow team (Tendering support, URL param convention). **Time-box for wizard build: 3-4 sessions estimated for Modules 1a + 1b + 2; 1 used (Module 1a); 2-3 remaining.** --- ## 14. End-of-session status (rotated to archive) > Per agreement #45, end-of-session sections rotate to `BOQ_Handover_archive.md` once superseded. The **v5.34 EOS** (Module 3 spoke shell/preview/nav close) was rotated into the archive (v1.3, 2026-06-02) when v5.35 superseded it. The v5.31/v5.32/v5.33 EOS sections were rotated at archive v1.2 (2026-06-01). The current cycle's status lives in the **v5.35 audit-checklist + Trigger state (top of this doc)**; the v5.35 EOS-equivalent is the "Tomorrow's plan" sub-block there. Archive ledger is clean as of v1.3 — no open rotations. ---
 - **v5.33** — 2026-05-31 IST (Module 2, the Hub, DESIGNED + BUILT + PROVEN LIVE end-to-end). **Code commits this cycle: 8 on `feature/boq-phase-3`, NOT pushed** — 2a feat `5cdbbd16` + docs `55bc59ca`; 2b-i feat `81568df9` + docs `16857a55`; 2b-ii feat `459f85ae` + docs `9e54cafd`; 2b-iii feat `57152c52` + docs `6efbde5d`. Tip `6efbde5d`; feature tip `57152c52`. Parser tests 588 unchanged; wizard tests 41 (16 pre-2a + 25 new in `test_update_sheet_draft.py`). **Substantive content:** the workbook-overview hub was designed (Wizard Design §6.2, M2.1–M2.23, landed as the v1.5 doc) and built the same span across four slices. **2a backend:** `BoQ Sheet Draft.wizard_status` extended to 6 values (`Pending`/`Hidden`/`Reviewed`/`Skip`/`General specs`/`Parse failed`), `sheet_label` Data, `BOQs.general_specs_sheet` Data pointer; three POST endpoints (`set_sheet_status` rejecting "General specs", `set_sheet_label`, `set_general_specs_sheet` pointer-only); 25 wizard tests; bench migrate clean; **live endpoint test 10/10** (key finding T2: Frappe does NOT strip whitespace from `sheet_name` — stored/matched VERBATIM, so the frontend passes it exactly and trims only for display). **2b-i static hub:** `/upload-boq/hub/:boqId` route, `BoqHubPage` + `SheetCard` + `boqTypes.ts`, parse-gate computation, Continue repointed to the hub. **2b-ii wired interactions:** `useFrappePostCall` + SWR `mutate()` server-authoritative writes (first wizard use; raw fetch retained only for file upload), per-card saving, inline errors, status-dependent buttons, general-specs selector with the M2.23 confirm flow; plus the 2b-i pill-color convention-drift fix. **2b-iii visual polish:** 2-col responsive grid, solid high-contrast status pills with dark: variants, amber `AlertTriangle` likely-non-data cue, detailed footer breakdown. **FULL HUB FLOW PROVEN LIVE** on `BOQ-26-00145` (real 22-sheet BoQ): card render, Mark-reviewed (spinner → green pill → footer update), Skip+label, general-specs designation incl. the M2.23 confirm dialog + auto-release of the previous general-specs sheet, parse-gate denominator math (22 → 21 when one sheet set aside, M2.11), Cancel-reverts-clean, refresh persistence. **M2.23 AMENDED** — general-specs designation is a SINGLE pointer write that never touches `wizard_status`; "General specs" is purely derived while the pointer points; release reverts the sheet to its TRUE prior status (a Reviewed sheet comes back Reviewed, not Pending). Less destructive, simpler, no schema field. **New §7.41** captures it; **§6 received a 2026-05-31 decisions block** (Module 2 built+proven; M2.23 amended; `useFrappePostCall` pattern; socket-hang finding; test-count correction); **§3 received the Module 2 sub-phase record + a v5.33 refresh-log entry**; **§9 #126 NEW** (happy-path parse-done socket hang — SESSION ARTIFACT not a bug; the worker logged `Job OK` and the BoQ was in the DB, but a mid-session backend restart + clear-site-data had disrupted the upload screen's socket so the `parse_done` event was missed; confirmed NOT a code bug by a clean-session re-test; workaround = open the hub URL directly). **3 new standing caveats XX/YY/ZZ** (`:8080` served by a separate `yarn dev` Vite window not `bench start`'s watch.1; harmless `{{ boot }}` SyntaxError on :8080; parse-done-socket-hang-after-restart). **PK docs refreshed this cycle:** Environment Runbook **v1.1** (replaces v1.0 — §1.5 exact two-window cold-start commands, §3.5 `{{ boot }}` note, §8 socket-hang finding) + Wizard Design **v1.5a** (replaces v1.5 — M2.23 amended; Module 2 marked BUILT) + `boq-upload-plan.md` + `frontend/CLAUDE.md`. Root `CLAUDE.md` updated by the 2a backend slice only (docs `55bc59ca`); the three 2b frontend slices touched `frontend/CLAUDE.md` only. **Per agreement #45 (Option A rotation):** v5.32's "CHANGES SINCE v5.31" + "Trigger state v5.32" + full v5.32 EOS rotate to archive — archive file not in hand, so a ⚠️ ROTATION ACTION OWED note (Archived-sections block) lists exactly what to paste from the v5.32 handover file; v5.33 carries its own audit-checklist + Trigger state + EOS inline. **§13 quick-reference card refreshed** (it had drifted to v5.31 state — branch/tip, test counts, direction queue, most-recent-commits all brought current). **Wizard build time-box: Module 1a 1 session + Module 1b ~2 + Module 2 ~1 span = ~4.** **Next: Module 3 (the per-sheet config spoke) — fresh design pass first.** v5.33 replaces v5.32 in project knowledge — **delete v5.32 from PK** per agreement #17, but **keep the v5.32 file on Desktop until the ⚠️ rotation paste is done** (only copy of the rotated sections). Archive file does NOT go into project knowledge. - **v5.32** — 2026-05-30 IST live-integration close (Module 1b wizard frontend SHIPPED + existing-project upload PROVEN LIVE end-to-end). **Code commits this cycle: Module 1b frontend (6) + 3 integration fixes + docs**, all on `feature/boq-phase-3`, **NOT pushed** (origin AHEAD by 12). Module 1b sub-slices: `3b69d00d` (1b-i sidebar/tab/picker/routing) + `74741417` (1b-i PE-access fix) + `b13c7b9c` (1b-modal Tendering create-modal) + `0c066902` (1b-modal dropdown-in-dialog fix) + `d1f3b5cd` (1b-ii-a static upload screen/store/drop-zone/panel) + `273e7fab` (1b-ii-b live upload/socket/Continue-gate), plus docs. Integration fixes: `3c7e7556` (endpoint-path, 417 resolved) + `db56ae96` (S3-compat tempfile) + `3815ea3f` (source_file_url Data→Small Text) + docs `43eabdc4`. Tip `43eabdc4`; feature tip `3815ea3f`. Parser tests 588 unchanged; 17/17 wizard tests pass post-fix. **Substantive content:** across two live-debug sessions on one IST day (debriefs: `BoQ_Upload_Debug_Session_2026-05-30.md` + `BoQ_Upload_Session_Addendum_2026-05-30_night.md`), the upload pipeline was run live for the first time. **End-to-end PROVEN:** clean session + fresh worker → `upload_file` 200 → worker `Job OK` → `BOQ-26-00132` created for `TestCity-PROJ-00001` with 4 `BoQ Sheet Draft` children (Summary / low side / csu unit / make), `source_file_url` persisted at 190 chars, `boq:wizard_parse_done` socket event received in browser (verified in Network → WS frames), UI populated. **Three integration bugs fixed** (all surfaced only on the live run, none catchable by unit tests/clean builds): wrong endpoint path (417), S3 storage-path assumption (worker now writes in-hand bytes to a NamedTemporaryFile), and `source_file_url` 140-char overflow (`Data` → `Small Text`, bench-migrate verified live as `text`). **Module 1b SCOPE CUT** — existing-projects-only; create-new-project (1b-modal, present in frontend) DEFERRED until happy path proven (now is). **Session-1 CSRF root-cause CORRECTED** — the session-1 "BoQ-specific blank-token bug, switch to frappe-react-sdk" diagnosis was WRONG; real cause was stale-session-after-restart (CSRF failed app-wide on Projects too), fixed by clear-site-data + re-login; the blank header is a real quirk but not fatal alone (blank header still returned 200 on a fresh session); the actual blocker was the field-length bug. **New working agreement #47 CODIFIED** — boundary-crossing slices must be live-tested against the running system before "done" (not just built + unit-tested); companion rule: treat root-cause claims as hypotheses until proven with live evidence. **New §7.40** — source_file_url Data→Small Text design constraint (S3 generate_file URLs ~190 chars). **§9 #118-#123 NEW:** #118 source_file_url overflow (CLOSED), #119 DB is PostgreSQL not MariaDB (operational), #120 CSRF-breaks-after-restart→clean-login (operational), #121 bench-start collision / clean-restart procedure (operational), #122 error-path notification UNVERIFIED (open, narrowed — success path proven, error paths never triggered live), #123 session-1-CSRF-diagnosis-was-wrong (lesson). **Doctype-JSON sanctioned exception** added to root CLAUDE.md (single field, reviewed/committed CC edit + migrate; surfaced + consented per #13e). `frontend/CLAUDE.md` NOT touched (no frontend convention change). **Canonical sections promoted per Option C hybrid:** §6 received a 2026-05-30 decisions block (Module 1b shipped+proven + scope cut + CSRF correction + doctype-JSON exception + push deferral + agreement #47 context); §7 received §7.40; §8 received agreement #47; §9 received #118-#123; §3 received a wizard-build sub-phase-records sub-section + a v5.32 refresh-log entry. **Per agreement #45 (Option A rotation):** v5.31's "CHANGES SINCE v5.30" audit-checklist + "Trigger state v5.31" rotate to archive — **but the archive file was not in hand this cycle**, so those two sections were REMOVED from the live doc and a ⚠️ ROTATION ACTION OWED note (in the Archived-sections block) lists exactly what to paste into `BOQ_Handover_archive.md` from the v5.31 handover file. v5.32 carries its own audit-checklist + Trigger state v5.32 inline. **Standing caveats TT/UU/VV/WW NEW v5.32** (PostgreSQL-not-MariaDB; CSRF-after-restart; bench-collision clean-restart; manual-DB-inspect-from-sites-dir). **Wizard build time-box: Module 1a 1 session + Module 1b ~2 sessions = ~3 of 3-4; Module 2 remaining.** **Next: exercise happy path on more real BoQs → error-path notification slice (diagnose-first) → Module 2. v5.32 replaces v5.31 entirely — delete v5.31 from project knowledge** per agreement #17 — **EXCEPT** keep the v5.31 file on Desktop until the ⚠️ rotation paste into the archive is done (it's the only copy of the rotated sections). Archive file does NOT go into project knowledge. - **v5.31** — 2026-05-29 IST chat-side + Claude Code housekeeping close (Module 1a wizard backend + schema SHIPPED). **Code commits this cycle: 3** on new `feature/boq-phase-3` branch (branched from `feature/boq-phase-2` tip `2e338b36`). Chain: `06f38e8d` (feat: Module 1a wizard backend + schema + 17 wizard tests) → `6aa9c1ae` (docs: plan-doc Module 1a record) → `91d603eb` (docs(claude): root CLAUDE.md convention codification — 4 items: Phase 3 branch convention, wizard scope discipline cross-reference, Projects test fixture pattern, `api/<feature>/` subdirectory pattern). Parser tests 588 unchanged (Module 1a touches Frappe layer only); Frappe tests **679 → 696** (+17 wizard integration tests). Origin in sync at `91d603eb`. **Substantive content:** Module 1a delivered the first slice of the wizard build per Wizard Design v1.2 §6.1 — new schema (`BOQs.wizard_state` Select + `BOQs.sheet_drafts` Table + new `BoQ Sheet Draft` child doctype with 4 fields `sheet_name` / `sheet_order` / `wizard_status` / `work_package`), new endpoints (`upload_file` async with Socket.IO push `boq:wizard_parse_done` + `update_boq_draft` partial-update) under `nirmaan_stack/api/boq/wizard/`. **Mid-execution scope reduction:** `create_tendering_project` endpoint DROPPED after the wizard scope discipline principle surfaced — tendering-project creation has reach beyond the wizard, belongs in the existing Nirmaan new-project workflow at `frontend/src/pages/projects/project-form/` (Nirmaan team is independently extending it to support Tendering alongside Won). Wizard's "Create new project" picker path defers to that flow with `?wizard_resume_with_project=<project_id>` URL parameter for resumption. **Manual testing PASS** end-to-end on real BoQ (project `BENGALURU-PROJ-00101` + `Bill of Quantities.xlsx` → `BOQ-26-00127` with 13 sheet drafts, hidden sheets correctly marked, Electrical Work Header auto-detected on 2 sheets, attachment `ATT-00101-001` linked). **New working agreement #46 CODIFIED** — wizard scope discipline (when a decision presents "build inside wizard vs defer to existing flow," chat-Claude pauses and surfaces the fork; default lean keep outside wizard if reach extends beyond Upload BoQ; case-by-case after discussion). **Agreement #13 EXTENDED with #13e** — doc-update explicit discipline (every CC prompt must explicitly state whether plan-doc + CLAUDE.md get updated; if CLAUDE.md is NOT touched, prompt must explain why and chat-Claude must ask user consent first). **Caveat D Option B CLARIFIED** (§9 #117 NEW) — one amend cycle only for hash backfill; do NOT loop amends to converge (each amend changes the hash creating an infinite cycle); accept one-step staleness. **§9 §115-#117 NEW** (sheet-to-Work-Headers cardinality → Module 2 design item; existing new-project workflow extension dependency → independent team work; Caveat D Option B clarification → operational discipline). **Wizard Design v1.2 supersedes v1.1** (folded in: §4.7 NEW wizard scope discipline as cross-cutting decision; §6.1.2 picker 3 → 2 sub-paths; §6.1.3 rewritten as project creation handoff; §6.1.10 schema reflects what shipped; §6.1.11 endpoints 3 → 2; M1.38-M1.42 DROPPED; M1.52 + M1.53 NEW; decisions table revised). v1.1 deleted from project knowledge. **Canonical sections promoted per Option C hybrid:** §6 received 10 new decision entries dated 2026-05-29 (Module 1a SHIPPED + create_tendering_project DROPPED + agreement #46 codified + agreement #13e codified + Caveat D Option B clarification + Wizard Design v1.2 + Module 1b confirmed next + sub-split rationale + Phase 3 branch convention + Checklist C audit); §8 received new agreement #46 (wizard scope discipline) + #13 extension with #13e (doc-update explicit discipline); §9 received 3 new entries (#115 sheet-to-Work-Headers cardinality, #116 existing-flow extension dependency, #117 Caveat D Option B clarification). **Per agreement #45:** v5.30's "CHANGES SINCE v5.29a" + "Trigger state v5.30" + the stale-inline v5.27 EOS (which stayed across v5.28-v5.30 because those cycles had no new EOS sections) all rotate to archive at v5.31 close. v5.31 carries its own audit-checklist + Trigger state v5.31 + v5.31 EOS inline. **Archive location policy (carried forward from v5.29a/v5.30):** archive file `BOQ_Handover_archive.md` lives on Nitesh's Desktop only, **NOT in project knowledge** — project knowledge holds the slim live handover doc only. Re-upload archive as one-off chat attachment if deep history is needed in-session. **Caveats KK retired v5.31** (held cleanly through cycles 3 + 4 + Module 1a); **QQ, RR, SS NEW v5.31** (frappe.utils.now() 26-char truncation pattern; Projects fixture `project_scopes=[]` requirement; Customers `gst_no` nullability latent bug). **Wizard build time-box: 3-4 sessions for Modules 1a + 1b + 2; 1 used (Module 1a); 2-3 remaining.** **Next: Module 1b (wizard frontend) — design conversation + CC prompt drafting. v5.31 replaces v5.30 entirely — delete v5.30 from project knowledge** per agreement #17. Archive file does NOT go into project knowledge. - **v5.30** — 2026-05-28 IST chat-side housekeeping (orientation reset + strategic re-sequencing). **Pure documentation cycle. No code commits.** Tip commit unchanged (`2e338b36`); parser tests unchanged (588); origin in sync. **Substantive content:** activity 2b-3 DROPPED (schema rename no longer needed because wizard becomes sole SheetConfig authoring surface); wizard UI codified as sole authoring surface (no separate internal-contributor form); E5 reframed to 2 components and SATISFIED at v5.29 close; working agreement #45 codified (rolling-window archive convention). **Canonical sections promoted per Option C hybrid:** §6 received 4 new decision entries dated 2026-05-28 (cycle 4 close + 2b-3 drop + wizard sole-authoring + E5 reframe + agreement #45 + Option C operational pattern + cycle 4 context + Checklist C audit); §8 received new agreement #45 (rolling-window archive convention); §9 received 5 status flips for #105-#109 (all FIX QUEUE → LANDED, with Bug 18/20-anchors/20-ext flipped to v5.28 and Bug 19/19-ext flipped to v5.29) plus 3 new entries #112 (Bug 20-ext-v2 NEW+LANDED v5.29) + #113 (Bug 23 NEW+LANDED v5.29) + #114 (Bug 24 NEW+LANDED v5.29). **v5.29a's top sections moved to archive** per agreement #45: "CHANGES SINCE v5.28" + "Trigger state v5.29" rotated out; v5.30 carries only its own audit-checklist + trigger state inline. **Archive location policy (carried forward from v5.29a):** archive file `BOQ_Handover_archive.md` lives on Nitesh's Desktop only, **NOT in project knowledge** — project knowledge holds the slim live handover doc only. Re-upload archive as one-off chat attachment if deep history is needed in-session. **v5.30 replaces v5.29a entirely — delete v5.29a from project knowledge** per agreement #17. Archive file does NOT go into project knowledge. - **v5.29a** — 2026-05-28 IST chat-side housekeeping reduction. **No code changes; no commits.** Doc-only structural reduction from 8,477 lines to ~3,900 lines (~54% reduction). **Cut to archive file** (`BOQ_Handover_archive.md`, kept on Nitesh's Desktop, NOT in project knowledge): 17 historical "CHANGES SINCE vX.Y — AUDIT CHECKLIST" sections (v5.27 → v5.7, ~2,780 lines); 12 historical EOS sections (§14.5 → §22, v5.26a → v5.10, ~1,690 lines); full text of §23 Phase 1.9i (510 lines, compressed to ~70-line taxonomy reference in this doc). **Untouched:** §1 Overview, §2 Goals, §3 Phase plan, §4 Data model, §5 Parser architecture, §6 Decisions log, §7 Design decisions, §8 Working agreements, §9 Known issues, §10 How to resume, §11 Critical pointers, §12 Glossary, §13 Quick reference, §14 EOS v5.27 (most recent EOS), §24 Document history. **Top-of-doc preserved:** v5.29 audit checklist + v5.29 Trigger state dashboard. **Rationale:** doc was eating project knowledge budget on every search; ~half was per-cycle changelog whose durable content was already promoted into the canonical sections (§6/§7/§8/§9). Archive preserves session narrative for grep-on-demand. **New convention going forward:** AUDIT CHECKLIST + EOS sections live in this doc for the current cycle only; at next cycle handover, previous cycle's audit-checklist + EOS get archived rather than preserved inline. Prevents re-accumulation. **v5.29a replaces v5.29 entirely — delete v5.29 from project knowledge** per agreement #17. Archive file does NOT go into project knowledge. - **v5.27** — 2026-05-26 IST chat-side housekeeping close. Tip commit `6e485dda` (8 commits ahead of v5.26a close `ec2b5ae8`). Parser tests **523** (484 → 487 → 490 → 502 → 523, +39 net across cycle); Frappe tests 91 unchanged. **8 commits across 3 fix sessions + 1 off-budget toggle + 1 read-only diagnostic + 1 parked attempt:** Bug 22 LANDED v5.27 cluster 2 session 1 (feat `4e5561d3` + docs `55aafc4a`, token-based `pattern_signature` collapse via `re.sub(r"D+", "D")` in `hierarchy.py`, STRICTLY ADDITIVE — 21 rows / zero classification changes confirmed via snitch round-trip diagnostic, sec 9 #110 CLOSED); Bug 22 toggle session 1.5 off-budget (feat `a2ce8a0d` + docs `fb09b6b4`, `BUG_22_COLLAPSE_ENABLED` module constant for regression isolation, +3 tests); snitch round-trip diagnostic Outcome B Acceptable (NO commits per agreement #30 — read-only, 21 rows differ ON/OFF, ZERO classification changes); Bug 19 + Bug 19-ext attempt PARKED (NO commits, two abort cycles in session 2-a, empirical fixture walk did not validate canonical fixtures, deferred to post-cycle-3-rerun session 7); Bug 16 LANDED v5.27 cluster 1 session 2-b (feat `68cfc57d` + docs `7ccae741`, **mid-cycle reframe** from post-pass to in-classifier flow after user pushback, two clauses in `classify_row()` for SPACER broadening + LINE_ITEM unit gate, +12 new + 12 existing-test calibrations same-commit, 132 sg_hvac + 190 snitch + 2 VRF reclassifications, sec 9 #103 CLOSED); Bug 17 LANDED v5.27 cluster 1 session 3 (feat `30b6045b` + docs `6e485dda`, **approach (a) sub-variant** — `iter_rows()` takes `text_role_columns: set[str] | None` parameter derived from `SheetConfig.column_role_map` via new orchestrator `TEXT_ROLE_ROLES` frozenset; `_format_numeric_as_displayed()` helper with `_PRECISION_FMT_RE = r"0\.(0+)"` regex; single commit, no separate plumbing-prep; pre-flight via raw xlsx XML inspection because Docker down; +21 tests; sec 9 #104 CLOSED). **NEW working agreement #44 CODIFIED v5.27** — Prompt hygiene principles (10 principles from Bug 19 abort + Bug 16 painful path + Bug 17 clean landing): prompts as goals not procedures; length matches complexity not anxiety; no phased phases unless needed; trust Claude Code with small surprises; listen to user instinct on first warning; empirical first spec second; calibration updates aren't scope creep; abort early not late; chat-Claude drafts CC executes don't collapse roles; heavy machinery for high-risk only. **Stale framing cleaned up:** agreement #41 (Checklist C mandatory at every resume) was already codified at v5.25 — the v5.26a header's "pending formal codification" wording was leftover stale text; v5.27 confirms #41 codified and honored across this session's Checklist C audit + Bug 19 abort + Bug 17. **Cycle 3 budget status (agreement #43):** 3 of 7 budgeted sessions used (Bug 22, Bug 16, Bug 17). Off-budget: Bug 22 toggle, snitch diagnostic, Bug 19 parked attempt. 4 budgeted sessions remaining for 4 work items (Bug 18, Bug 20 combined, cycle 3 validation re-run, Bug 19 re-spec). **Zero buffer.** C6 PASS-with-pressure-flag. **E5 progress:** 3 of 9 deterministic fixes landed; 6 remaining + validation re-run. **§9 status flips:** #103 (Bug 16) OPEN → LANDED `68cfc57d`; #104 (Bug 17) OPEN → LANDED `30b6045b`; #110 (Bug 22) NEW v5.26 → LANDED `4e5561d3` + toggle `a2ce8a0d`. **Caveat ledger:** **V CLOSED v5.27** (GitHub push of `ec2b5ae8..6e485dda` 8 commits completed; origin in sync at `6e485dda`); **P closing v5.27** (handover doc re-upload to project knowledge happening this cycle; plan-doc + CLAUDE.md re-upload already completed); **W NEW v5.27** (25-item self-report drift — Bug 17 CC produced ~8 freeform sections instead of structured 25-item per agreement #20; process gap, not code defect; surface in next Bug 18 prompt). Standing v5.27: L / N (sharpened, recurring — restored 11 synthetic xlsx twice this cycle) / O / R / T (sharpened — lands as activity 2b-3) / U candidate (Docker-down instance added to watch-list). Q EXTENDED v5.26. Closed history: C / D / D-1 (v5.26a); S (v5.26). **Standing operational followups (cosmetic):** plan-doc header `Latest commit:` line still reads pre-Bug-17 hash `68cfc57d` (should be `30b6045b`); Bug 17 decisions log entry has `sec 9 #__` placeholder where Bug 17 is sec 9 #104. Fix at next docs touch. **Sequence locked v5.27:** Bug 18 (cluster 1 session 4, gating pre-flight Excel verification of safron r41 merge structure) → Bug 20 anchors 1+2+3 + Bug 20-ext combined (cluster 2 final) → cycle 3 validation re-run against 8 fixtures → Bug 19 + Bug 19-ext re-spec from empirical findings → activity 2b-3 schema migration → activity 2b-4 wizard spec cross-walk → activity #4 wizard scoping (Phase 2c body kickoff). **§14 demotion chain:** v5.26 + v5.26a EOS preserved as §14.5 (HISTORICAL); v5.25 → §14.6; v5.24a → §14.7; v5.23 → §14.8; v5.21 → §14.9; v5.20 → §14.10; v5.19 → §14.11; v5.18 → §14.12. **Repo state at v5.27 close:** branch `feature/boq-phase-2` at `6e485dda`; **origin in sync** at `6e485dda` (Nitesh pushed `ec2b5ae8..6e485dda` 2026-05-26 — 8 commits in one push). All 523 parser tests verified green via canonical 7-module command (62s) post-Docker-restart. Working tree pre-existing baseline noise only. **v5.27 replaces v5.26a entirely — delete v5.26a from project knowledge** per agreement #17. - **v5.26a** — 2026-05-25 IST chat-side housekeeping addendum (same-day continuation of v5.26 close session). Tip commit `ec2b5ae8` (1 docs commit ahead of v5.26 close `72a146c1`). Parser tests **484 unchanged**; Frappe tests 91 unchanged. **Two-track housekeeping cycle landed:** **Track 1 (Claude Code, repo)** — single docs-only commit `ec2b5ae8` per agreement #42 on `feature/boq-phase-2`. 8 doc-refinement edits: (1) Caveat D plan-doc Last-updated refresh; (2) Caveat D-1 CLAUDE.md Last-updated refresh; (3) Bug 17 spec — Inovalon r26 cosmetic float passthrough excluded; (4) Bug 19 / 19-ext spec — ±20-row symmetric window + min-3 PREAMBLE safety threshold + asymmetric tunable reserve; (5) Bug 20 anchor 3 spec — Reading B locked single-step recursive + data-loss-criticality framing; (6) Bug 20-ext spec — keystone observation (text-vs-numeric signature non-equivalence); (7) Inovalon subtotal-reset-and-continue spec — out-of-scope wizard-handled disposition; (8) decisions-log entry at top of plan-doc. 25-item Checklist B self-report PASS. Caveat N synthetic xlsx restore performed per agreement #26; ASCII-only commit message per recent memory edit. **Known limitation:** in-commit hash self-reference broken — plan-doc `Latest commit:` line points at pre-amend `3a055c88` (orphan) not post-amend `ec2b5ae8`; accepted as cosmetic per Option B convention change (docs commits going forward will reference only the previous landed commit, not self-reference). **Track 2 (chat-Claude, handover doc — THIS DOC)** — agreement #35 file-upload-and-edit workflow. v5.26 doc uploaded, edits applied, v5.26a saved for download. 8 edits: (1) header Last-updated refresh; (2) Caveat C harmonization — bug taxonomy table + §12 glossary Bug 10 entry corrected to 73 (historical v5.21/v5.22 summary blocks preserved as-is); (3) §12 Glossary extended with 15 new v5.26 terms + 2 v5.26a entries (cluster 1, cluster 2, Bug 16-22 spec one-liners, Reading B locked, keystone observation, cycle 3 deep dive, cycle 3 re-run validation, activity 2b-3, activity 2b-4, agreement #43, exit criterion E5, Bug 12 SPLIT, LINE_ITEM-as-missing-ancestor, Inovalon subtotal-reset-and-continue, v5.26a Track 1, v5.26a Track 2); (4) §13 Quick reference card — tip refreshed to `ec2b5ae8`, commits-stack updated, origin-in-sync status, hash self-reference caveat noted; (5-6) §9 #104 / #106 / #108 / #109 — appended Track 1 spec-refinement mirror notes; (7) §9 #111 NEW — Inovalon subtotal-reset-and-continue out-of-scope disposition; (8) §14 EOS — Standing caveats list updated (C / D / D-1 CLOSED v5.26a; V CONFIRMED OPERATIONAL; U candidate retired); §14 v5.26a subsection added; §24 entry (this entry). **Caveat ledger closures:** C / D / D-1 CLOSED v5.26a. V CONFIRMED OPERATIONAL v5.26a. U candidate retired (workflow design flaw replaced by Option B convention). S CLOSED v5.26 (carry-forward). Standing v5.26a: L / N / O / P / R / T / Q EXTENDED. **Cycle 3 fix work readiness:** plan-doc §17.44 specs complete with Track 1 refinements; first fix prompt drafting (Bug 22, cluster 2 leads) unblocked. **Sequence locked v5.26a:** Cluster 2 first (Bug 22 warmup → 19 + 19-ext combined → 20 anchors + 20-ext combined) → Cluster 1 (Bug 16 → 17 → 18) → Cycle 3 re-run validation → activity 2b-3 → activity 2b-4 → activity #4. 5-7 session time-box, hard cap at 7 per agreement #43. **Repo state at v5.26a close:** branch `feature/boq-phase-2` at `ec2b5ae8`; origin in sync (Nitesh pushed `2c332e03..ec2b5ae8` at v5.26a Track 1 close, covering both v5.26 docs commit `72a146c1` + v5.26a housekeeping commit `ec2b5ae8`). Working tree: pre-existing baseline noise only. **v5.26a replaces v5.26 entirely — delete v5.26 from project knowledge** per agreement #17. - **v5.26** — 2026-05-25 IST chat-side working session close. Tip commit `72a146c1` (1 docs commit ahead of v5.25 close `43346961`). Parser tests **484 unchanged** (docs-only commit per agreement #42); Frappe tests 91 unchanged. **1 docs-only commit landed** — cycle 3 deep dive findings + agreement #43 + E5 + 9-fix queue. **In-chat work**: deep-dive walk across 7 fixture configurations (sg_hvac, safron, inovalon, bill_of_quantities, alorica 2row+1row A/B joint, snitch, raheja_commerzone_hvac; multi_area_merged_header_v1 DROPPED from reference set after confirming synthetic stress test). **9 deterministic parser fixes identified and queued** in plan-doc §17.44: Bug 16 (LINE_ITEM-without-unit demotion — classifier.py), Bug 17 (reader auto-trim text-role columns + IEEE 754 precision noise handling — reader.py), Bug 18 (merged-cell banner false LINE_ITEMs — reader.py + classifier.py), Bug 19 (priced-PREAMBLE via sl_no signature LINE_ITEM step — hierarchy.py), Bug 19-ext (same logic at PREAMBLE attachment step — hierarchy.py), Bug 20 anchors 1+2+3 (section-header NOTE → PREAMBLE — classifier.py + hierarchy.py), Bug 20-ext level-0 (section-header PREAMBLEs at level 0 — closes v5.25 §17.41 PARKED — hierarchy.py), Bug 22 (token-based pattern_signature — hierarchy.py). All meet agreement #40 deterministic-unambiguous bar. **5 Phase 3+ AI items consolidated** at C2 5-item ceiling: Bug 12 residual (Inovalon mid-section uppercase NOTE clusters with no positional anchor), Bug 14 (letter-suffix cosmetic carry-forward), Bug 15 (sl_no semantic repeats carry-forward), LINE_ITEM-as-missing-ancestor (alorica r35→r37 NEW v5.26), skip-then-ingest sheet type (carry-forward). **8 wizard-handleable items recognized** per Caveat Q extension (Pattern 6 compound names, BMS schedule skip, vendor-compare, hidden-sheet, per-row attribution UX, merged-title-banner, priced-PREAMBLE wizard flow, multi-row header gap) — NOT in Phase 3+ AI inventory; surface during wizard scoping. **NEW working agreement #43 CODIFIED**: *"Parser-fix work is bounded. One round of deterministic fixes per identified-bug-batch + one validation cycle. New bugs surfacing post-validation go to Phase 3+ AI resolution layer, unless something strikingly deterministic and high-impact emerges. Handwritten BoQs have infinite error combinations; we accept parser limitations and rely on AI for residual disambiguation."* Extends agreements #34 (cycle termination) and #40 (no fuzzy parser rules). **Phase exit criterion E4 (v5.25) SUPERSEDED by E5 NEW v5.26**: *"All 9 deterministic fixes from cycle 3 deep dive landed AND cycle 3 re-run validates against the 8 fixtures (multi_v1 dropped) AND activity 2b-3 schema migration started."* Three binary measurable components. **Time-box codified at 5-7 working sessions** for all 9 fixes + cycle 3 validation re-run, batched ~4 fixes per cluster (cluster 1 classifier.py + reader.py ~3 sessions; cluster 2 hierarchy.py ~3 sessions; validation ~1 session). Hard cap at 7. **Bug 12 (sec 9 #88) disposition SPLIT v5.26**: deterministic portion via Bug 20 anchors 1-3 + level-0 to FIX QUEUE; residual stays Phase 3+ AI per agreement #43. **§9 changes**: #88 PARKED v5.25 → SPLIT v5.26; **8 NEW §9 entries** (#103 Bug 16, #104 Bug 17, #105 Bug 18, #106 Bug 19, #107 Bug 19-ext, #108 Bug 20 anchors 1-3, #109 Bug 20-ext level-0, #110 Bug 22) — all FIX QUEUE. **Checklist C audit at v5.26 close all 7 items dispositioned**: C1 PASS-with-tightening (E4→E5), C2 PASS at 5-item ceiling, C3 PASS (power-user framing), C4 PASS (declarative wizard Option 3b confirmed), C5 PASS (cycle 3 done, next check in E5 component 2), C6 PASS-with-time-box (5-7 sessions), C7 PASS (wizard spec exists, deep dive validated mapping; Bug 17 + Bug 18 layering notes flagged for fix-prompt drafting time). **Active overrides FORMALIZED v5.26**: Caveat H deferred-budget framing FORMALIZED as 5-item ceiling at C2 (accepted as Phase 3+ AI scope budget per agreement #43); Caveat H C6 time-box FORMALIZED as 5-7 working sessions at C6. v5.25 "Checklist C not re-run mid-cycle" override SUPERSEDED by codification of resume-prompt Checklist C as agreement #41. **Strategic calls v5.26**: (1) AI-upfronting for Phase 2c body wizard scoping NOT NEEDED — 9 deterministic fixes cover bulk of issues; (2) multi_area_merged_header_v1 DROPPED from cycle 3 / wizard-design reference set (synthetic stress test); (3) Bug 12 disposition SPLIT (deterministic portion to FIX QUEUE; residual stays Phase 3+ AI). **Caveat ledger**: S CLOSED v5.26 (bundle backup refreshed); D-1 NEW v5.26 (CLAUDE.md Last-updated placeholder mismatch — cosmetic, inline-fix at next housekeeping cycle); U candidate NEW v5.26 (Claude Code methodology substitution watch-list); V operational NEW v5.26 (Nitesh direct GitHub push deviation — confirm operational owner); Q EXTENDED v5.26 (8 wizard-handleable items recognized — NOT in Phase 3+ AI inventory). **§14 demotion chain**: v5.25 EOS preserved as §14.5 (HISTORICAL); v5.24a → §14.6; v5.23 → §14.7; v5.21 → §14.8; v5.20 → §14.9; v5.19 → §14.10; v5.18 → §14.11. **Repo state at v5.26 close**: branch `feature/boq-phase-2` at `72a146c1`; origin in sync per Caveat V (Nitesh direct GitHub push). Synthetic xlsx fixture working-tree noise standing per Caveat N — pre-flight restore required before any next commit work. **v5.26 replaces v5.25 entirely — delete v5.25 from project knowledge.** - **v5.25** — 2026-05-24 IST chat-side housekeeping close (capturing 2026-05-23 working session). Tip commit `43346961` (7 commits ahead of v5.24a close `a1131744`; 25 commits ahead of origin `fe18b337`). Parser tests **484** (was 440 at v5.24a close; A1+A2-reframed +24, SUB HEAD + universal subtotal-reset +20); Frappe tests 91 unchanged. **7 commits across 4 sub-phase clusters plus one throwaway diagnostic:** **Cluster 1 — Approach A-reframed audit** (feat `16647958` + docs `6fecd78b`, 2026-05-23): read-only diagnostic `approach_a_audit.py`; Snitch '6. Electrical' 21/21 A1 firings correct + 0 A2 firings (single-section cascade); Bill of Quantities 'ELECTRICAL & ELV BOQ' 0 A1 firings + 5/5 A2-reframed firings correct (Bug 11a canonical case). Low-misfire evidence sufficient for LAND decision. **Cluster 2 — A1+A2-reframed LAND** (feat `8f960a2b` + docs `ea60a03f`, 2026-05-23): Rule A1 in `_determine_preamble_level` (lowercase-letter cascade fix via first-numbered-ancestor stop) + Rule A2-reframed in LINE_ITEM attachment step (stack-top-only proximity, pattern_signature equality + first_numeric_token inequality reparenting) + two new helpers (`pattern_signature` + `first_numeric_token`) + `approach_a_enabled` kwarg. +24 tests in `test_hierarchy.py` (440 → 464). `snitch_electrical_expected.json` regenerated with A1+A2-applied baseline (one LINE_ITEM caveat row documented + accepted). §9 #99 EXECUTED. §9 #87 (Bug 11) CLOSED v5.25 via A1+A2 land. **Cluster 3 — SUB HEAD detection + universal subtotal-reset LAND** (feat `25a43617` + docs `6bb60c3f`, 2026-05-23): `_SUB_HEAD_RE` + `_is_sub_head_marker()` helper + SUB HEAD branch BEFORE Rule A1 forces level=1 (SUB HEAD rows treated as de facto section roots); `_MID_SHEET_RESET_RE` gating REMOVED from SUBTOTAL_MARKER stack-reset (now unconditional on every fire); regex preserved with deprecation comment per Option A. +20 tests in `test_hierarchy.py` (464 → 484). §9 #100 + §9 #101 CLOSED v5.25. **Cluster 4 — Bug 11 closure recognition** (docs `43346961`, 2026-05-23): post-hoc recognition that A1+A2-reframed LAND structurally subsumed Bug 11a (→A2-reframed signature-sibling LINE_ITEM attachment) and Bug 11b (→A1 numbered-ancestor cascade). Documented as separate docs-only commit per Caveat L (don't bundle caveat-cleanup or recognition work with feature-fix docs). **Throwaway diagnostic — Bug 12 6-fixture cross-fixture validation** (no commit per agreement #30 throwaway exception): confirmed Bug 12 (NOTE-as-header) requires fuzzy structural judgment across 6 fixtures including Inovalon; §9 #88 PARK to Phase 3+ AI layer confirmed. **STRATEGIC SHIFT v5.25 EXECUTED — parser-layer fuzzy rules park.** Bug 12 + Bug 14 + Bug 15 (NEW v5.25) all PARKED to Phase 3+ AI review layer; §9 #99 fully EXECUTED. **Working agreement #40 CODIFIED** (latest landed): *"No more parser fuzzy rules. Future parser-layer fixes must meet the Bug 13 deterministic-unambiguous bar: binary criterion, no carve-outs, no fuzzy structural judgment. Fuzzy structural decisions belong at Phase 3+ AI review layer."* Four closing examples (Bug 13 + Rule A1 + Rule A2-reframed + SUB HEAD + universal subtotal-reset) and three parked examples (Bug 12 + Bug 14 + Bug 15). **Two more working agreements NEW v5.25:** **#41** Checklist C audit MANDATORY at every fresh-chat resume (codifies §10 update); **#42** Caveat-cleanup follow-up chores NOT bundled with feature-fix docs commits (codifies Caveat L lesson; first instance: Caveat S). **§9 changes:** #87 (Bug 11) PARKED v5.23 / DEEPENED v5.24 → CLOSED v5.25 via A1+A2 land; #88 (Bug 12) DEFERRED v5.24 → PARKED v5.25 to Phase 3+ AI layer; #99 (parser-layer fuzzy rules park strategic decision) → EXECUTED v5.25; **Three NEW §9 entries:** #100 (SUB HEAD detection) CLOSED v5.25; #101 (universal subtotal-reset) CLOSED v5.25; #102 (Bug 15 SG HVAC priced-sub-section-header) PARKED v5.25. **Bug taxonomy flips:** Bugs 1/2/3/4/8 RESOLVED v5.25 via wizard-resolved-bug recognition (declarative wizard architecturally subsumes detection-layer bugs whose only failure path was auto-detect ambiguity — confirmed v5.21 §17.13 via Sequence C2+E2 schema acceptance). Findings 15/16 EXCLUDED from this recognition (wizard *design specs*, not detection-layer bugs). **NEW §10 + §11 update — MANDATORY Checklist C audit at every fresh-chat resume.** All 7 items (C1-C7), surface failed items, user call on each, only then proceed. Codified into §10 Option A and §11 Pointer #0 + working agreement #41. **Phase 2c bug-fix cycle CLOSED v5.25.** Phase 2c body is now next direction (E4 trigger — wizard scoping sub-phase, declarative wizard Option 3b per §17.13). Phase 3+ AI review layer inherits: Bug 12 (§9 #88), Bug 14 (§9 #90), Bug 15 (§9 #102), §17.9, §17.10. **Caveat ledger A-S:** Retired v5.25: A, B, F, G, I, J, K, M. Standing v5.25: C (cosmetic Société Générale 73 vs 74), D (hash-fixup-vs-amend standing), H (partial — Bug 11 framing SUPERSEDED but C2/C6 active), L (codification candidate standing — applied successfully v5.25), N (synthetic xlsx fixture working-tree noise — recurring; flagged as standing pre-flight requirement). NEW v5.25: P (handover §3 Phase plan refresh deferred to next housekeeping when Phase 2c body work begins), Q (wizard-resolved-bug recognition scope clarification — detection-layer bugs only; Findings 15/16 wizard design specs EXCLUDED), R (`_MID_SHEET_RESET_RE` preserved in source with deprecation comment — full removal chore after grace period), S (bundle backup stale 2+ days + 25 commits — refresh chore separate from feature-fix docs per Caveat L). **Repo state at v5.25 close:** branch `feature/boq-phase-2` at `43346961` (25 commits ahead of origin `fe18b337`); tech-side person handles GitHub push, pending. Bundle backup stale by 2+ days and 25 commits — Caveat S follow-up. Synthetic xlsx fixture working-tree noise + `classifier_audit_output.json` modified + `.claude/settings.local.json` modified + `Handover doc.pdf` untracked — pre-flight restore required per agreement #26. **§14 demotion chain:** v5.24a EOS preserved as §14.5 (HISTORICAL); v5.23 → §14.6; v5.21 → §14.7; v5.20 → §14.8; v5.19 → §14.9; v5.18 → §14.10. **Standing reminder for next chat:** Re-upload v5.25 handover doc + refreshed `boq-upload-plan.md` (post-`43346961`) + refreshed `CLAUDE.md` (post-`43346961`) per agreement #17. **v5.25 replaces v5.24a entirely — delete v5.24a from project knowledge.**
--- ## End of v5.46 handover doc (prior cycle) --- ## §14. End-of-session status — v5.45 (prior cycle) **For the next chat:** This is the **v5.45** handover — **THE RESTRUCTURE SURFACE IS COMPLETE: C-classification AND C-parenting are BUILT, FULLY LIVE-CERTIFIED on `BOQ-26-00145` (+ `BOQ-26-00150`), delivered as ONE unified surface (the row-detail-panel front door → the `RestructureModal` with the in-modal Excel picker), covering child placement AND the row's own position, with-children AND childless paths, with a mutation-proven extended cycle-guard and real backend error messages. LC10 is FORMALLY RETIRED. The review screen's EDIT LOOP is COMPLETE — values, text, remarks, classification, parenting.** [Superseded by v5.46 — retained as the recent-prior-cycle EOS per agreement #45.] **[v5.45 close state — superseded:]** HEAD `20c642c2`; origin `733adf9c` — the 1b-beta2b pair (`20e1f5a7` feat + `20c642c2` docs) was UNPUSHED at v5.45 close (pushed early in the v5.46 cycle). The consolidated verification pass owed at v5.45 (Layout-Part-A MA4 + the C-values rate-edit vehicle) was CLOSED at v5.46. **KEY CONVENTIONS introduced at v5.45 (still standing):** agreement #58 (the slice-composition framework — blast radius / hard dependency / default-separate); §9 #161 (`getFrappeError()` is THE error-extraction pattern for every new catch block); §9 #163 (the bundle-marker diagnostic when a frontend change "isn't there" after the de-stale ritual — Vite restart, then check the marker). --- ## §14. End-of-session status — v5.44 (prior cycle) **For the next chat:** This is the **v5.44** handover — **Slice C COMPLETE: the entire C-values EDITING arc is BUILT, FULLY LIVE-CERTIFIED on `BOQ-26-00145` (+ `BOQ-26-00150`), and PUSHED (origin in sync at `1a5df5b8`).** The review screen is now a working EDITOR: a human can inline-edit the seven flat numeric fields (confirm-gated), unit/make_model text (silent save), per-row remarks (separate write path — stays "Original"), and per-area numeric values (qty/amount/rate, flat and NESTED), each auto-saved on confirm, provenance-stamped, and surfaced as "Edited". Run the **MANDATORY Checklist C audit** (agreement #41) against the v5.44 Trigger-state dashboard as your FIRST action. **⚠️ Honest certification status:** the entire VALUE-edit surface is fully live-certified — including the hardest case (nested per-area rate modify-existing, proven via a manufactured three-edit sequence on Electrical: create / second-area-create / modify-with-siblings-intact). The edit-provenance UI built read-only in B2c is now LIT by real edits (green "Edited" pill/tint + populated history confirmed live). C-v2d-fix was a LIVE-CERT-driven correction: the per-area guard now validates against `sheet_config.area_dimensions` (not the row's keys), so editing a defined-but-empty area works. Frontend slices tsc-clean IN-CONTAINER (0 wizard errors) + Vite-exit-0 IN-CONTAINER; backend slices unit-test-certified + Checklist-B PASS. The classification/parenting edits are NOT yet built — that's next. **NEXT work item: Slice C-classification — inline classification editing** (the pill menu; childless = 1-click confirm, with-children = full confirm surfacing the children's fate), THEN **C-parenting** (drag/type-to-pick; cycle-guard HARD BLOCK; the only structural edit), THEN **C-flag-dismissal** (the deferred persistent "looks ok" dismissal — DB-backed, logged; open Qs in the v5.43/v5.44 headers). **Each must be scoped recon-FIRST (agreement #56); tsc/build verified IN-CONTAINER (agreement #57).** **THEN: Slice D** (integrity-gate UI warn-and-confirm + "Parsed Check Done" marking + per-sheet CSV export — Remarks gets its own CSV column; `exportToCsv.ts` carries pre-existing out-of-scope tsc errors — recon before that build) **→ Phase 3** (BOQ Nodes + DB commit; still owes the Phase-5 skip-filter / parser-check G / §9 #135 + the FINDING-A structural sweep). E6 is not met until parsed data commits to the DB. **Owed, non-blocking (carries — see the v5.44 deferred-budget dashboard):** persistent flag-dismissal (tail slice); FINDING A (`[object Object]` per-area-qty-on-single-area — pre-DB structural sweep); FINDING B (panel visual separation); the nested-rate modify-existing cert against a PARSER-PRODUCED nested vehicle (proven on a manufactured dict — negligible residual); Obs B (the batch-apply question — would reopen the LOCKED auto-save-on-confirm decision, do NOT fold without an explicit decision); the Phase-5 skip-filter (#135); the multi-fixture output-quality sweep; the resizable-columns + expand/collapse-ALL tree enhancements; the `exportToCsv.ts` tsc errors (Slice-D-adjacent). **DISCHARGED this cycle:** the 618-row re-parse; the `rate_by_area` TS type bug (FIXED); the nested-rate modify-existing live-cert (DONE). **CORPUS FINDING (banked, §9 #157):** both HVAC fixtures (`BOQ-26-00145` HVAC + `BOQ-26-00165`) produce per-area QTY but FLAT SCALAR rates/amounts — per-area NESTED rate is rarer in the real corpus than the schema assumes; relevant when choosing test vehicles for future per-area-rate work and for downstream pricing. **ENVIRONMENT (now in §9.5 / §9 #154/#155):** in-container tsc is canonical (host tsc is a false-green trap); use `$?` not `${PIPESTATUS[0]}` in `sh -c`; a backgrounded/redirected build looks hung but isn't (prefer foreground + `echo BUILD_DONE`; ~6.5 min is normal). The standalone Environment Runbook is now FOLDED into §9.5 and RETIRED from PK. **Repo:** HEAD `1a5df5b8`; **origin IN SYNC** (pushed from HOST PowerShell — agreement #53; range `83cb681f..1a5df5b8`, the 14-commit C stack; the branch also carries Abhishek's Tendering commit `509a4dfe`). **Test counts (bench-verify in-session — never quote stale):** parser 588; wizard `test_review_screen` 110 (full-suite total 276 DERIVED — verify before quoting). ---
**⚠️ Honest certification status:** Slice 1 (backend flag) is fully LIVE-CERTIFIED; the Slice-2 completion modal (#3) is confirmed live; the ~10-min note is confirmed present. BUT **the on-mount recovery (#2 — navigate-away-and-back) is BUILT + Checklist-B-clean but NOT yet live-verified** — testing it was BLOCKED because the hub screen DISABLES during a parse (the user cannot navigate away). That screen-disable is a NEW defect (§9 #147) and it contradicts the don't-block intent (§9 #144c). Do not treat #2 as proven until the §9 #147 slice fixes the screen-disable and the navigate-away-and-back test passes. [v5.42 NOTE: §9 #147 is now CLOSED — the screen-disable is fixed and #2 is reachable.] **NEXT work item (agreed, production-facing): the §9 #147 socket-reconnect self-heal slice** — on socket RECONNECT, re-check job/BoQ status and reconcile screen state so a missed done-event self-heals WITHOUT a manual refresh, on BOTH the hub (`boq:parse_run_done`) and the upload screen (`boq:wizard_parse_done`). **Hard requirement:** the upload screen must recover IN PLACE, preserving the BoQ-name + version edit step (NOT skip-to-hub). **Folds in:** (B) the screen-disable fix (so navigation during parse is possible — the precondition for testing #2), and (C) the live-verification of the Slice-2 on-mount recovery. This slice comes BEFORE the review-screen design pass. Recon-first (agreement #49) — it needs diagnosis of WHY the screen disables and HOW each screen's socket listener is wired. [v5.42 NOTE: the hub self-heal + the screen-disable fix LANDED (`193327b1`); the review-screen build then proceeded — Slice A backend is done.] **THEN: the review SCREEN / Phase-2c DB-commit** — its own design pass + a fresh C6 time-box (the v6.4 mockups are the starting point; the per-row draft doctype, ex-M4.15, is the C7 watch — design first, land schema with its UI). E6 is not met until parsed data commits to the DB; that path still owes the Phase-5 skip-filter (parser-check G, §9 #135). **Owed, non-blocking (next docs/CC cycle):** (1) a SMALL CC docs prompt to fix three in-repo doc items — plan-doc "Latest commit:" line still `21e56963` → `b2ca0895`; frontend/CLAUDE.md stale-hash entries (3c → `16a6a4dc`, 3c-fix → `9f8fb6f7`) left by a prior global-replace; the A21 token-source label; (2) the Environment Runbook additions (the de-stale ritual; the build-verify-once rule = agreement #52; the push-from-host rule = agreement #53; the migrate-lock-is-a-collision note); (3) the C5 multi-fixture output-quality sweep when Phase 1 nears done. [v5.42 NOTE: item (1) DISCHARGED — the in-repo doc-pointer fixes landed (`d13afea9`/`59e988cb`) + the Slice-A docs commit.] **Repo:** HEAD `b2ca0895`; **origin IN SYNC** (pushed from HOST PowerShell — see agreement #53; the branch also carries Abhishek's Tendering commit `509a4dfe`, so it is not pure-BoQ). **Tests bench-verified in-session:** parser 588; wizard 168. **PK housekeeping (agreement #17):** upload v5.41, delete v5.40; re-upload `boq-upload-plan.md` + root `CLAUDE.md` + frontend `CLAUDE.md` AFTER the CC docs-fix prompt lands; upload archive v1.7; upload Wizard Design v1.12. **Archive rotation (agreement #45):** v5.38's CHANGES + Trigger-state block rotated OUT to the archive (→ v1.7); 3-recent window now v5.41/v5.40/v5.39. Ledger clean. --- ## End of v5.41 handover doc (prior cycle) --- > The block below is a RETAINED prior-cycle EOS footer (v5.38) for continuity. It is historical — the CURRENT close is the v5.41 block above. (The v5.39 + v5.40 EOS narratives live in their top-of-doc header blocks per the v5.39/v5.40 convention.) ## §14. End-of-session status — v5.38 (prior cycle) **For the next chat:** This is the **v5.38** handover — **parse-run Phase 1 is in progress.** Slice 2c (multi-general-specs) is BUILT + LIVE-CERTIFIED and the pre-existing Slice-2 "Parsed" frontend gap is FIXED + live-confirmed. ⚠️ **READ THE LINEAGE NOTE in the v5.38 header first:** this handover was last folded at v5.37 (before the parse-run effort) and is ~6 slices behind — Slice 1, Slice 2, master_preamble, and the precedence fix are NOT fully detailed here; the authoritative source is `BoQ_ParseRun_Plan_of_Record` in PK. **Folding that gap is the top docs task at next resume.** The old "Module 4 = review screen next" plan is SUPERSEDED by the parse-run re-sequencing (Phase 1 = parse run + BoQ Review Row, THEN the review screen). The archive file `BOQ_Handover_archive.md` sits on Nitesh's Desktop (NOT in PK) — re-upload only if deep history is needed. **PK housekeeping (agreement #17):** delete v5.37 from project knowledge once v5.38 is uploaded. **Re-uploaded to PK this cycle:** `boq-upload-plan.md` + root `CLAUDE.md` + frontend `CLAUDE.md` (latest in-repo state). **Wizard Design + archive updated alongside this handover** (M2.16 reversal → multi-general-specs; the "Parsed" frontend-handling note). **Module 1 + 2 + 3 PROVEN LIVE; parse-run Slices 1/2/2c done + 2c live-certified on `BOQ-26-00145`.** **Repo:** HEAD `4aac7b07` (LOCAL/unpushed); origin still `87d9cac4`; the whole stack since `d842aa71` is unpushed and LARGE — **push is overdue.** ⚠️ **Test counts: BENCH-VERIFY in-session** — this fold lacks current totals (588 parser is a 2c-era figure; wizard totals are the stale v5.37 picture). **MANDATORY FIRST ACTION at next resume (agreement #41):** Checklist C audit against the Trigger state v5.38 dashboard (NOT re-run this build+certify session). **Next work item (re-sequenced):** fold the parse-run gap → Phase-1 exit-criterion-B lifecycle checks (per-sheet re-parse status drops Parsed→Reviewed→Parsed on config change; warn-before-reparse) → Slice 2b (hub Parse button + status summary + multi-select general-specs designation UI — closes the 2c-schema-ahead-of-UI interim) → THEN the review SCREEN (ex-"Module 4"; mockups in Wizard Design §6.4/v1.8, M4.15 per-row draft doctype is the biggest call, keep C7 on it). **Still owed (carried):** the Phase-5 Skip/Hidden parser-filter (parser-check G, §9 #135), the Environment Runbook additions (§9 #138/#139/#141 + the synthetic-xlsx-churn note), push the local stack, litter cleanup. **New this cycle (§9):** #140 (the "Parsed" frontend-gap, FIXED + the teach-the-frontend-a-new-status lesson), #141 (de-stale-before-first-judgment). **Archive rotation (agreement #45):** v5.35 + v5.34's CHANGES + Trigger blocks were rotated OUT to the archive (→ v1.5; 3-recent window is now v5.38/v5.37/v5.36). Ledger clean. --- ## End of v5.38 handover doc (prior cycle) --- > The blocks below are RETAINED prior-cycle EOS footers (v5.37) for continuity. They are historical — the CURRENT close is the v5.38 block above. ### Historical EOS footer — v5.37 **For the next chat:** This is the v5.37 handover — a **docs-only bookkeeping correction** over v5.36 (Module 3 is COMPLETE 3a–3f, and the 6 batched UI-fixes #130–#134 are LANDED + pushed). The archive file `BOQ_Handover_archive.md` (v1.4) sits on Nitesh's Desktop with the historical changelog content — re-upload as a one-off attachment only if deep history is needed; it is NOT in project knowledge. **Archive ledger CLEAN** (v1.4; v5.37 added no rotation — docs-only cycle). **PK housekeeping (agreement #17):** delete v5.36 from project knowledge once v5.37 is uploaded. **No plan-doc / CLAUDE.md / Wizard Design re-upload owed by this cycle** — those were current at v5.36 and v5.37 changed only the handover. (If the v5.36 PK upload of `boq-upload-plan.md` / `CLAUDE.md` / Wizard Design v1.9 had not yet been done, that v5.36-flagged re-upload still stands.) **Module 1 + 2 + 3 are all PROVEN LIVE.** **Repo:** origin at **`87d9cac4`** (the batched-UI-fix push); the local Module-3-closeout stack **`87d9cac4`→`d842aa71`** is unpushed (11 commits) — push at a sensible point (Module 3 complete is one). **MANDATORY FIRST ACTION at next resume (agreement #41):** Checklist C audit against the Trigger state v5.37 dashboard. **Next work item: Module 4 (review-parsed-output screen) — the real design conversation** [⚠️ v5.38: this "Module 4 next" framing was SUPERSEDED by the parse-run re-sequencing — see the v5.38 header/EOS] (exploratory mockups in Wizard Design §6.4/v1.8). **Also owed (carried, NOT discharged by v5.37):** the Phase-5 Skip/Hidden parser-filter (parser-check G, §9 #135), the Environment Runbook additions (§9 #138/#139 + the synthetic-xlsx-churn note), push the local closeout stack, and litter cleanup (`run_parser_tests.sh`, `frontend/build-*.txt`, `Handover doc.pdf`, `tendering-won.patch`). **Discharged by v5.37 (no longer owed):** the 6 batched UI findings #130–#134 (LANDED + pushed) and the auto-guess-emits-`amount_combined` micro-check (resolved = keep the dropdown option).
